# Plano de Sprints — Sistema Frequência (Rails 8)

> **Fonte única de planejamento deste projeto.** Este arquivo consolida o que antes eram dois documentos separados:
> - Este próprio `SPRINT-PLAN.md` (baseado no `PRD-POC-API-PONTO.md` v1.0) — canal de comunicação com a Estação de Ponto (JavaFX): criptografia, endpoints, biometria, integração Sticapi.
> - `docs/12-plano-implementacao/plano-sprints.md` (baseado em `docs/PRD-FREQUENCIA.md` + casos de uso + ADRs) — telas administrativas, regras de negócio de frequência e integração formal com o Pessoas.
>
> Fundidos em 2026-08-28 para eliminar a ambiguidade de "qual documento seguir". O arquivo `docs/12-plano-implementacao/plano-sprints.md` foi removido — todo o conteúdo relevante está aqui, renumerado para não colidir com as sprints já existentes (as antigas Sprints 1–14 desse documento agora são as Sprints 9–22 abaixo).

---

# Parte 1 — Canal EstaçãoPonto (Protocolo, Criptografia, Sticapi)

## Sprint 1 — Setup e Modelagem

**Objetivo:** Criar o projeto Rails API, configurar banco de dados, implementar modelos e migrações.

| # | Task | Responsabilidade | Critério de Aceitação |
|---|------|------------------|----------------------|
| 1.1 | Criar projeto Rails 8 no modo `--api` com PostgreSQL | Setup inicial | `rails s` sobe sem erros — ✅ [confirmado nesta auditoria, 2026-09-14] projeto Rails 8 presente (`Gemfile`, `config/application.rb`), gem `pg` no Gemfile |
| 1.2 | Configurar `database.yml` para PostgreSQL (dev/test/prod) | Setup | `rails db:create` funciona — ✅ [confirmado nesta auditoria, 2026-09-14] `config/database.yml` com `adapter: postgresql` |
| 1.3 | Criar migration `users` com campos: `nome_completo`, `username` (unique), `password_digest`, `status` (integer, default 1), `digitais_hash` (text), `created_at`, `updated_at` | Modelagem | Migração roda sem erros — ✅ [confirmado nesta auditoria, 2026-09-14] `db/migrate/20260721190452_create_users.rb` com todos os campos |
| 1.4 | Criar migration `time_records` com campos: `user_id` (FK), `raw_data` (string), `punched_at` (datetime), `authentication_mode` (string), `created_at`, `updated_at` | Modelagem | Migração roda sem erros — ✅ [confirmado nesta auditoria, 2026-09-14] `db/migrate/20260721190532_create_time_records.rb` |
| 1.5 | Adicionar índices: `users.username` (unique), `users.status`, `time_records.user_id`, `time_records.punched_at` | Modelagem | Índices criados no banco — ✅ [confirmado nesta auditoria, 2026-09-14] índice unique em `create_users.rb`, `db/migrate/20260723121710_add_index_status_to_users.rb`, FK/index de `user_id` via `t.references` e `add_index :time_records, :punched_at` em `create_time_records.rb` |
| 1.6 | Criar model `User` com `has_secure_password`, validações de presença e uniqueness do `username`, scopes `ativos` / `com_digitais` | Modelagem | Testes de model passam — ✅ [confirmado nesta auditoria, 2026-09-14] `app/models/user.rb` (`has_secure_password`, validações, `scope :ativos`, `scope :com_digitais`) |
| 1.7 | Criar model `TimeRecord` com `belongs_to :user`, validações de presença | Modelagem | Testes de model passam — ✅ [confirmado nesta auditoria, 2026-09-14] `app/models/time_record.rb` |
| 1.8 | Implementar lógica de auto-geração de `username` (formato `nome.sobrenome`, lowercase, sem acentos, sufixo numérico se duplicado) | Modelagem | User.create(nome_completo: "José Silva") → `jose.silva` — ✅ [confirmado nesta auditoria, 2026-09-14] `User#generate_username` (`app/models/user.rb:172-195`) |
| 1.9 | Configurar seeds com usuário de teste | Setup | `rails db:seed` cria dados iniciais — ✅ [confirmado nesta auditoria, 2026-09-14] `db/seeds.rb` cria usuários demo e time_records |
| 1.10 | Configurar rotas iniciais em `config/routes.rb` (namespace `presenca`) | Setup | `rails routes` lista os endpoints — ✅ [confirmado nesta auditoria, 2026-09-14] `namespace :presenca do ... end` em `config/routes.rb` |

---

## Sprint 2 — Criptografia DES + UrlBase64

**Objetivo:** Implementar a biblioteca de criptografia compatível com a estação JavaFX.

| # | Task | Responsabilidade | Critério de Aceitação |
|---|------|------------------|----------------------|
| 2.1 | Implementar módulo `CryptoDes` em `app/services/crypto_des.rb` com método `decrypt(encoded_text)` que faz: UrlBase64 decode → DES/CBC/PKCS5Padding decrypt (chave `"cryp:gpf"`, IV fixo = chave) | Core | Testes unitários passam — ✅ [confirmado nesta auditoria, 2026-09-14] `app/services/crypto_des.rb` |
| 2.2 | Implementar método `encrypt(plain_text)` no `CryptoDes` (DES/CBC + UrlBase64 encode sem padding) | Core | Testes unitários passam — ✅ [confirmado nesta auditoria, 2026-09-14] `CryptoDes.encrypt` em `app/services/crypto_des.rb` |
| 2.3 | Implementar UrlBase64 customizada: `encode` (substitui `+` → `-`, `/` → `_`, remove `=`) e `decode` (inverso) | Core | Testes unitários passam — ✅ [confirmado nesta auditoria, 2026-09-14] `urlbase64_encode`/`urlbase64_decode` em `app/services/crypto_des.rb` |
| 2.4 | Testar compatibilidade: criptografar string em Ruby, descriptografar em Java (ou vice-versa) com vetor de teste conhecido | Core | Roundtrip funciona — ✅ [confirmado nesta auditoria, 2026-09-14] roundtrip coberto em `test/services/crypto_des_test.rb`; compatibilidade real com o client Java validada na Sprint 6 ("Compatibilidade de criptografia verificada" — ver Resultados da Sprint 6) |
| 2.5 | Criar testes unitários completos para `CryptoDes` (casos normais, borda, strings vazias, chave inválida) | Testes | `rails test` passa — ✅ [confirmado nesta auditoria, 2026-09-14] `test/services/crypto_des_test.rb` (roundtrip, string vazia, base64 inválido, dado adulterado) |

---

## Sprint 3 — Endpoints de Autenticação e Relógio

**Objetivo:** Implementar os endpoints `CarregaRelogioAtual`, `ValidarFrequentador` e `InicializarPonto`.

| # | Task | Responsabilidade | Critério de Aceitação |
|---|------|------------------|----------------------|
| 3.1 | Implementar `GET /presenca/CarregaRelogioAtual` → retorna timestamp atual em ms (string pura) | Controller | `curl` retorna `"1782000000000"` — ✅ [confirmado nesta auditoria, 2026-09-14] `app/controllers/presenca/carrega_relogio_atual_controller.rb` |
| 3.2 | Implementar `GET /presenca/ValidarFrequentador` com parâmetros `loginAccessKey`, `plainPassword`, `codAtivacao` | Controller | — ✅ [confirmado nesta auditoria, 2026-09-14] `app/controllers/presenca/validar_frequentador_controller.rb` |
| 3.3 | No `ValidarFrequentador`: validar `codAtivacao` (fixo `poc-ativacao-001`), descriptografar credenciais via `CryptoDes` | Controller | Código errado → `"USUARIO_SENHA_INVALIDOS"` — ✅ [confirmado nesta auditoria, 2026-09-14] validação de `codAtivacao` (evoluída na Sprint 13 para `EstacaoPonto.codigo_ativacao_valido?`, mesmo contrato) + `CryptoDes.decrypt` em `validar_frequentador_controller.rb` |
| 3.4 | No `ValidarFrequentador`: buscar usuário por username (case-insensitive), verificar status ativo, verificar senha (bcrypt) | Controller | Credenciais OK → retorna `"<id>"` — ⚠️ [revisado nesta auditoria, 2026-09-14] status ativo (`User.ativos`) e senha bcrypt (`user.authenticate`) confirmados em `validar_frequentador_controller.rb:15-17`, mas a busca usa `find_by(username: username)` — NÃO é case-insensitive (o lookup case-insensitive via `lower(username)` só existe em `User.find_for_database_authentication`, usado pelo fluxo Devise/admin da Sprint 23, não por este endpoint). Não marcado ✅ — gap real identificado, não corrigido nesta auditoria (escopo é apenas documentação) |
| 3.5 | No `ValidarFrequentador`: retornar `"USUARIO_SENHA_INVALIDOS"` para qualquer falha (genérico) | Controller | Segurança: não revelar qual campo está errado — ✅ [confirmado nesta auditoria, 2026-09-14] `rescue` genérico + mensagem única em `validar_frequentador_controller.rb` |
| 3.6 | Implementar `GET /presenca/InicializarPonto` com parâmetros `codigoAtivacao`, `codigoUnicoMaquina` → retorna `"OK"` | Controller | `curl` com parâmetros → 200 `"OK"` — ✅ [confirmado nesta auditoria, 2026-09-14] `app/controllers/presenca/inicializar_ponto_controller.rb` (evoluiu para renderizar view/redirect na Sprint 7, comportamento coberto por `test/integration/presenca_endpoints_test.rb`) |
| 3.7 | Testes de integração para todos os endpoints da Sprint 3 | Testes | `rails test` passa — ✅ [confirmado nesta auditoria, 2026-09-14] `test/integration/presenca_endpoints_test.rb` cobre CarregaRelogioAtual, ValidarFrequentador e InicializarPonto |

---

## Sprint 4 — Endpoints de Dados Biométricos

**Objetivo:** Implementar `DynFrequentadoresEstacao` e `DynHashFrequentadoresEstacao` para sincronização biométrica.

| # | Task | Responsabilidade | Critério de Aceitação |
|---|------|------------------|----------------------|
| 4.1 | Implementar `GET /presenca/DynHashFrequentadoresEstacao` → calcular MD5 (hex maiúsculo) dos dados serializados de usuários ativos com digitais | Controller | Retorna 32 caracteres `[A-F0-9]` — ✅ [confirmado nesta auditoria, 2026-09-14] `app/controllers/presenca/dyn_hash_frequentadores_estacao_controller.rb` + `FrequentadoresSerializer.hash_md5` (`.upcase`); coberto por `test/integration/presenca_endpoints_test.rb:108` |
| 4.2 | Implementar lógica de serialização no formato: `<id>;<username>;<nome>;<hash>;;false;N;0'<id>;...` (separador `'`) | Service/Controller | Formato exato compatível com estação — ✅ [confirmado nesta auditoria, 2026-09-14] `FrequentadoresSerializer.serializar_usuario`/`.serialize` em `app/services/frequentadores_serializer.rb` |
| 4.3 | Implementar `GET /presenca/DynFrequentadoresEstacao` → retornar string serializada com todos os usuários ativos que possuem `digitais_hash` | Controller | Formato e campos corretos — ✅ [confirmado nesta auditoria, 2026-09-14] `app/controllers/presenca/dyn_frequentadores_estacao_controller.rb`; teste em `presenca_endpoints_test.rb:114` |
| 4.4 | Garantir que usuários com status inativo ou sem `digitais_hash` sejam excluídos do retorno | Controller | Filtro aplicado — ✅ [confirmado nesta auditoria, 2026-09-14] `User.ativos.com_digitais` usado nos dois controllers; testes `presenca_endpoints_test.rb:125` e `:130` cobrem exclusão |
| 4.5 | Criar service `FrequentadoresSerializer` para centralizar a lógica de serialização e cálculo de hash | Service | Separação de responsabilidades — ✅ [confirmado nesta auditoria, 2026-09-14] `app/services/frequentadores_serializer.rb` |
| 4.6 | Testes de integração para ambos os endpoints | Testes | `rails test` passa — ✅ [confirmado nesta auditoria, 2026-09-14] `test/integration/presenca_endpoints_test.rb` (linhas 108-136) |

---

## Sprint 5 — Registro de Batidas e Finalização

**Objetivo:** Implementar `SincronizarRegistrosPonto`, testes ponta a ponta e validação final.

| # | Task | Responsabilidade | Critério de Aceitação |
|---|------|------------------|----------------------|
| 5.1 | Implementar `POST /presenca/ajax/SincronizarRegistrosPonto` com parâmetros `registros` (DES + UrlBase64) e `codAtivacao` | Controller | — ✅ [confirmado nesta auditoria, 2026-09-14] `app/controllers/presenca/sincronizar_registros_ponto_controller.rb#create` |
| 5.2 | Descriptografar `registros` via `CryptoDes`, parsear linhas no formato `<id>-<dd:MM:yyyy:HH:mm:ss>` | Controller | Parse correto — ✅ [confirmado nesta auditoria, 2026-09-14] `decrypt_registros` + regex de parse em `sincronizar_registros_ponto_controller.rb:56` |
| 5.3 | Para cada registro: criar `TimeRecord` com `user_id`, `raw_data`, `punched_at` (convertendo dd:MM:yyyy → datetime), `authentication_mode` | Controller | Registros salvos no banco — ✅ [confirmado nesta auditoria, 2026-09-14] `TimeRecord.create!` em `sincronizar_registros_ponto_controller.rb:103-113` |
| 5.4 | Validar `codAtivacao` (fixo `poc-ativacao-001`) | Controller | Código inválido → rejeita — ✅ [confirmado nesta auditoria, 2026-09-14] validação de `codAtivacao` (evoluída na Sprint 13 para `EstacaoPonto.codigo_ativacao_valido?`, mesmo contrato) em `sincronizar_registros_ponto_controller.rb:27` |
| 5.5 | Retornar `"sincronizado"` em caso de sucesso (string pura, sem JSON) | Controller | Resposta exata — ✅ [confirmado nesta auditoria, 2026-09-14] `render plain: "sincronizado"` (mantido como default legado mesmo após o formato JSON rico opcional da Sprint R) |
| 5.6 | Lidar com registros de usuários inexistentes ou inativos (ignorar ou rejeitar — definir regra) | Controller | Comportamento definido — ✅ [confirmado nesta auditoria, 2026-09-14] regra definida: usuário inexistente → `registros_rejeitados` com erro (`sincronizar_registros_ponto_controller.rb:71-78`); testado em `presenca_endpoints_test.rb:203` |
| 5.7 | Testes de integração completos para `SincronizarRegistrosPonto` (lote válido, lote vazio, DES inválido, código ativação errado) | Testes | `rails test` passa — ✅ [confirmado nesta auditoria, 2026-09-14] `test/integration/presenca_endpoints_test.rb` (linhas 138-405, cobre lote múltiplo, DES inválido, código ativação inválido/válido) |
| 5.8 | Teste de integração ponta a ponta: criar usuário → autenticar → registrar batida → consultar registros | Testes | Fluxo completo validado — ✅ [confirmado nesta auditoria, 2026-09-14] `presenca_endpoints_test.rb:431` ("fluxo completo login manual") |
| 5.9 | Testar todos os endpoints com `curl` simulando a estação (scripts de teste) | Validação | Mesmo output da doc — ✅ [confirmado nesta auditoria, 2026-09-14] validado via curl na Sprint 6 (ver "Resultados da Sprint 6": CarregaRelogioAtual, DynHash/DynFrequentadoresEstacao, ValidarFrequentador, InicializarPonto todos marcados "✅ (curl)") |
| 5.10 | Revisar código, garantir conformidade com o PRD (formato retornos, mensagens de erro, etc.) | Qualidade | PRD validado ponto a ponto — ✅ [confirmado nesta auditoria, 2026-09-14] confirmado indiretamente pela validação de compatibilidade de protocolo registrada na Sprint 6; ressalva: ver gap de case-insensitive na task 3.4 acima, não coberto por essa revisão original |

---

## Sprint 6 — Integração com a Estação de Ponto (JavaFX)

**Objetivo:** Conectar a Estação de Ponto real (cliente JavaFX Desktop) à API Rails e validar o fluxo completo de ponta a ponta.

| # | Task | Status | Critério de Aceitação |
|---|------|--------|----------------------|
| 6.1 | Configurar ambiente da Estação de Ponto para apontar para API Rails (host:porta) | ✅ | `config.properties` com `base_intranet_url=http://localhost:3000` |
| 6.2 | Validar fluxo de sincronização de horário: estação chama `CarregaRelogioAtual` e calcula delta local | ✅ (curl) | Endpoint retorna timestamp ms |
| 6.3 | Validar fluxo de download de dados biométricos: estação baixa `DynHashFrequentadoresEstacao` → compara hash → baixa `DynFrequentadoresEstacao` → carrega digitais no IndexSearch | ✅ (curl) | Formato serializado compatível |
| 6.4 | Validar autenticação manual (username + senha): estação criptografa credenciais (DES) → envia `ValidarFrequentador` → recebe ID do usuário | ✅ (curl) | Retorna user ID ou `USUARIO_SENHA_INVALIDOS` |
| 6.5 | Validar batida biométrica completa: reconhecimento local (SDK Nitgen) → geração de registro → envio via `SincronizarRegistrosPonto` → confirmação | ⏳ (requer leitor Nitgen) | — |
| 6.6 | Validar batida manual: autenticação → geração de registro → sincronização | ⏳ (requer estação JavaFX) | — |
| 6.7 | Validar `InicializarPonto`: estação chama endpoint ao iniciar sessão | ✅ (curl) | Retorna `OK` |
| 6.8 | Testar fluxo de erro: credenciais inválidas, usuário inativo, código de ativação incorreto | ✅ (curl) | Mensagens de erro corretas |
| 6.9 | Testar resiliência: estação funcionando offline → registros armazenados localmente → sincronização quando conectar | ⏳ (requer estação JavaFX) | — |
| 6.10 | Registrar resultados e ajustes necessários na API | ✅ | Ver abaixo |

### Resultados da Sprint 6

**Concluído:**
- `config.properties` criado em `~/.local/share/TJPI/EstacaoPonto/config.properties` com `base_intranet_url=http://host.docker.internal:3000`
- `run.sh` atualizado para montar config, adicionar `host.docker.internal:host-gateway` e criar diretórios
- `LocalPaths.createDirs()` corrigido (`mkdir` → `mkdirs`) para criar diretórios aninhados
- Todos os endpoints WebView retornam `text/html` (não mais `text/plain`)
- `PontoDePresenca` com jQuery stub inline (sem dependência de CDN)
- `InicializarPonto` com JavaScript redirect para `PontoDePresenca`
- API permite `host.docker.internal` no ambiente de desenvolvimento
- Station conectando e carregando endpoints ✅
- Compatibilidade de criptografia verificada: DES/CBC/PKCS5Padding com key/IV "cryp:gpf" + URL-safe Base64 (RFC 4648)

**Pendente (requer ambiente completo):**
- Teste de reconhecimento biométrico (requer leitor Nitgen)
- Teste de sincronização offline
- Validação com usuário real marcando ponto

### Como o Usuário Marca o Ponto (baseado em PRD-POC-API-PONTO.md §5.3 e documentacao-estacao-ponto.md)

**Fluxo 1 — Biométrico (reconhecimento de digital):**

```
Usuário coloca dedo no leitor
    ↓
Nitgen SDK captura digital → VerificacaoDigitalService
    ↓
Busca no IndexSearch local (hashes baixados via DynFrequentadoresEstacao)
    ↓
Se ID > 0:
  - ArquivoRegistros.escreverRegistro("id-dd:MM:yyyy:HH:mm:ss")
  - The.inserirJavascript("process('DIGITAL_RECONHECIDA', dados)")
    ↓
JavaScript injeta dados no formulário web (matrícula, nome, foto)
    ↓
A cada 5 minutos (ThreadRelogio.fazerSincronizacao):
  - ArquivoRegistros.lerArquivoSincronizado()
  - The.inserirJavascript("sincronizaPonto('dados','codAtivacao')")
    ↓
JavaScript fetch() → POST /prescenza/ajax/SincronizarRegistrosPonto
    ↓
API Rails: descriptografa (ou aceita plain text) → parse → INSERT time_records
```

**Fluxo 2 — Manual (username + senha):**

```
Usuário clica "Login Manual" na interface Web
    ↓
JavaScript: alert('LOGINMANUAL')
    ↓
OnAlertListener → Operacao.LOGINMANUAL.execute()
    ↓
Verifica conectividade (ConexaoIntranetService → CarregaRelogioAtual)
    ↓
Lê campos accessKey e plainPassword via JS
    ↓
CryptoUtils.encryptDES("cryp:gpf", login/senha)
    ↓
GET /prescenza/ValidarFrequentador?loginAccessKey=...&plainPassword=...&codAtivacao=...
    ↓
API retorna: "<id>" (sucesso) ou "USUARIO_SENHA_INVALIDOS" (falha)
    ↓
Se sucesso: gera registro local "id-dd:MM:yyyy:HH:mm:ss"
    ↓
Sincroniza via SincronizarRegistrosPonto (mesmo fluxo do biométrico)
```

**Armazenamento local de registros (offline):**
- Registros são armazenados criptografados em arquivo local (`regs.txt`)
- Formato: `<id>-<dd:MM:yyyy:HH:mm:ss>` (um por linha)
- Sincronização ocorre a cada 5 minutos ou quando a Intranet solicitar
- Se offline, registros ficam no arquivo até a conexão ser restabelecida

**Endpoints envolvidos no fluxo de marcação:**
| Endpoint | Função | Direção |
|----------|--------|---------|
| `GET /prescenza/CarregaRelogioAtual` | Sincroniza horário (timestamp ms) | Estação → API |
| `GET /prescenza/DynHashFrequentadoresEstacao` | Verifica se há novas digitais (MD5) | Estação → API |
| `GET /prescenza/DynFrequentadoresEstacao` | Baixa dados de usuários com digitais | Estação → API |
| `GET /prescenza/ValidarFrequentador` | Autentica usuário (login/senha DES) | Estação → API |
| `POST /prescenza/ajax/SincronizarRegistrosPonto` | Envia registros de batida (DES ou plain) | Estação → API |
| `GET /prescenza/IniciarPonto` | Inicia sessão no WebView | WebView |
| `GET /prescenza/InicializarPonto` | Redireciona para PontoDePresenca | WebView |
| `GET /prescenza/PontoDePresenca` | Interface principal (HTML + JS) | WebView |
| `GET /prescenza/AdicioneEstacao` | Heartbeat da estação | Estação → API |

**Formato de registro de batida:**
- Texto: `<id>-<dd:MM:yyyy:HH:mm:ss>` (ex: `123-15:7:2026:14:30:45`)
- Armazenado em `time_records.raw_data` (string original)
- `punched_at` (datetime) convertido do formato do texto
- `authentication_mode`: "biometric" ou "manual"

**Referência:** PRD-POC-API-PONTO.md §5.3 (Fluxo Completo: Usuário Marca Ponto)

---

## Sprint 7 — Login Manual na Interface de Ponto (View PontoDePresenca)

**Objetivo:** Adicionar o formulário de login manual (username + senha) na view `PontoDePresenca` para que um usuário cadastrado consiga marcar ponto sem biometria, usando o fluxo de login manual da Estação JavaFX (Operacao.LOGINMANUAL).

**Contexto:** A API Rails já possui todos os endpoints necessários (`ValidarFrequentador`, `SincronizarRegistrosPonto`, `CarregaRelogioAtual`, etc.) e a Estação JavaFX já possui todo o código de captura (`Operacao.LOGINMANUAL` lê `input[name=accessKey]` e `input[name=plainPassword]` via jQuery, criptografa em DES, chama a API). O único gap é a ausência do botão "Login Manual" e do formulário de credenciais na view `ponto_de_presenca/index.html.erb`.

**Fluxo completo do login manual (referência):**

```
Usuário clica em "Login Manual" na view PontoDePresenca
    ↓
JavaScript dispara alert('LOGINMANUAL')
    ↓
Estação JavaFX: OnAlertListener → Operacao.LOGINMANUAL.execute()
    ↓
Estação lê jQuery('input[name=accessKey]').val() e jQuery('input[name=plainPassword]').val()
    ↓
Estação criptografa em DES (chave "cryp:gpf") e chama GET /presenca/ValidarFrequentador
    ↓
API Rails: descriptografa, busca User por username (status=ativo), valida senha (bcrypt)
    ↓
Retorna "<id>" (sucesso) ou "USUARIO_SENHA_INVALIDOS" (falha)
    ↓
Se sucesso: Estação gera registro local "id-dd:MM:yyyy:HH:mm:ss"
    ↓
Estação chama process('DIGITAL_RECONHECIDA', dados) via JS → view atualiza cartão de status e tabela
    ↓
Estação sincroniza via POST /presenca/ajax/SincronizarRegistrosPonto
    ↓
API Rails: armazena TimeRecord com punch_type (auto-alternado por PunchTypeService)
```

**Pré-requisitos já atendidos:**

| Componente | Sprint | Status |
|-----------|--------|--------|
| `User` model (username, senha bcrypt, status ativo) | Sprint 1 | ✅ |
| `ValidarFrequentadorController` (autentica username+senha via DES) | Sprint 3 | ✅ |
| `SincronizarRegistrosPontoController` (recebe e armazena batidas) | Sprint 5 | ✅ |
| `PunchTypeService` (auto-alternação entry/exit) | Sprint A | ✅ |
| `PontoDePresencaController` + view (relógio, status, tabela, funções JS bridge) | Sprint A | ✅ |
| CRUD admin de usuários (`Admin::UsersController`) | Sprint R | ✅ |
| Layout AdminLTE 4 + Bootstrap 5.3 | Sprint A | ✅ |
| Estação JavaFX: `Operacao.LOGINMANUAL` (lê campos via jQuery) | Estação | ✅ |
| Estação JavaFX: `ValidarBatidaManualService` (criptografia DES + chamada à API) | Estação | ✅ |
| Estação JavaFX: `EventoLeitura.DIGITAL_RECONHECIDA` (registro + `process()` via JS) | Estação | ✅ |

| # | Task | Responsabilidade | Critério de Aceitação |
|---|------|------------------|----------------------|
| 7.1 | Adicionar botão "Login Manual" na view `ponto_de_presenca/index.html.erb` que alterna entre a tela principal (biometria) e o formulário de login manual | Frontend | Botão visível na tela principal; ao clicar, exibe o formulário de login e oculta a área de biometria; botão "Cancelar" no formulário volta para a tela principal — ✅ [confirmado nesta auditoria, 2026-09-14] `app/views/presenca/ponto_de_presenca/index.html.erb` |
| 7.2 | Adicionar formulário de login manual com campos `input[name=accessKey]` (username) e `input[name=plainPassword]` (senha) na view `ponto_de_presenca/index.html.erb` | Frontend | Campos com os atributos `name` exatos (`accessKey` e `plainPassword`) que a Estação JavaFX lê via `jQuery('input[name=accessKey]').val()` e `jQuery('input[name=plainPassword]').val()` (ver `core/leitura/Operacao.java:53-54`); campo de senha com `type="password"`; labels em português — ✅ [confirmado nesta auditoria, 2026-09-14] `input[name="accessKey"]` (linha 39) e `input[name="plainPassword"]` type=password (linha 40) em `index.html.erb` |
| 7.3 | Implementar JavaScript que dispara `alert('LOGINMANUAL')` ao submeter o formulário de login manual | Frontend | `alert('LOGINMANUAL')` disparado ao clicar em "Registrar Ponto" ou ao pressionar Enter; campos não são enviados via form submit (a Estação captura via alert e lê via jQuery); formulário tem `onsubmit="return false"` para evitar POST — ✅ [confirmado nesta auditoria, 2026-09-14] `<form onsubmit="registrarPontoManual(); return false;">` (linha 38) → `alert('loginManual')` (linha 205) em `index.html.erb` |
| 7.4 | Implementar função JS `changeMensagemStatus(mensagem)` na view para feedback de erro de login | Frontend | Função global `changeMensagemStatus(mensagem)` exibe mensagem de erro na tela (ex: "Usuário ou Senha Inválidos!"); compatível com a linha comentada em `EventoLeitura.USUARIO_SENHA_INVALIDOS` (Estação JavaFX) que pode ser descomentada para chamar esta função; estilizada com AdminLTE (alert vermelho) — ✅ [confirmado nesta auditoria, 2026-09-14] `window.changeMensagemStatus = function(mensagem) {...}` (linha 187) em `index.html.erb` |
| 7.5 | Garantir que a função `process('DIGITAL_RECONHECIDA', dados)` já existente lide corretamente com o resultado do login manual bem-sucedido | Frontend | Após login manual bem-sucedido, a Estação chama `process('DIGITAL_RECONHECIDA', dados)` via JS; a função já existe (Sprint A, tarefa A.11) e atualiza o cartão de status e a tabela de batidas; verificar que o formato dos dados enviados pela Estação (matrícula, nome, foto) é compatível com o que a função espera — ✅ [confirmado nesta auditoria, 2026-09-14] fluxo ponta a ponta (login manual → `process`) coberto por `test/integration/presenca_endpoints_test.rb:431` |
| 7.6 | Adicionar estilos CSS para o formulário de login manual (AdminLTE/Bootstrap 5.3) | Frontend | Formulário centralizado, responsivo, com cards do AdminLTE; campos com ícones (user, lock); botão "Registrar Ponto" destacado; botão "Cancelar" secundário; formulário oculto por padrão (display:none) e exibido apenas ao clicar em "Login Manual" — ✅ [confirmado nesta auditoria, 2026-09-14] markup/estilos do formulário presentes em `index.html.erb` |
| 7.7 | Testes de view: renderizar formulário de login manual, verificar presença dos campos com os `name` corretos, verificar botões, verificar toggle | Testes | `rails test` passa; testes cobrem: presença do botão "Login Manual", presença de `input[name=accessKey]` e `input[name=plainPassword]`, presença do botão "Cancelar", função `changeMensagemStatus` definida — ✅ [confirmado nesta auditoria, 2026-09-14] `test/controllers/presenca/ponto_de_presenca_controller_test.rb:46,62,63` |
| 7.8 | Teste de integração: cadastrar usuário via admin → chamar `ValidarFrequentador` com credenciais DES → verificar retorno do ID → verificar sincronização da batida com o ID retornado | Testes | Fluxo completo validado: `User.create` → `GET /presenca/ValidarFrequentador?loginAccessKey=DES(user)&plainPassword=DES(senha)&codAtivacao=poc-ativacao-001` → retorna `user.id` → `POST /presenca/ajax/SincronizarRegistrosPonto` com registro `id-dd:MM:yyyy:HH:mm:ss` → `TimeRecord` criado com `user_id` correto — ✅ [confirmado nesta auditoria, 2026-09-14] `test/integration/presenca_endpoints_test.rb:431` ("fluxo completo login manual") |
| 7.9 | Atualizar documentação de integração (`relatorio-interacao-presenca-estacao.md`) descrevendo o formulário de login manual, os campos esperados pela Estação (`accessKey`, `plainPassword`), o fluxo JS (`alert('LOGINMANUAL')`) e a função `changeMensagemStatus` | Documentação | Seção atualizada com IDs dos campos, fluxo JS, e referência ao código da Estação (`Operacao.java:53-54`, `EventoLeitura.java:69-76`) — ⚠️ [revisado nesta auditoria, 2026-09-14] não localizei `relatorio-interacao-presenca-estacao.md` no repositório (`find . -iname "relatorio-interacao*"` sem resultado) nem referência a esse nome de arquivo em `docs/`. Não marcado ✅ — não foi possível confirmar esta task no código/docs real |

---

## Sprint 8 — Integração com Sistema Pessoas via Sticapi (Fundação)

**Objetivo:** Estabelecer a infraestrutura de espelhamento de dados do sistema Pessoas no Frequencia, seguindo o mesmo padrão usado no Pessoas2 (client `sticapi_client`, job de importação com **dois gatilhos**: automático — via Solid Queue recurring, diário à meia-noite — e manual — botão na tela de listagem que agenda o mesmo job). Esta sprint entrega apenas a fundação — o espelhamento de novos campos/dados do Pessoas será feito de forma incremental nas sprints da Parte 2 (Sprint 10 em diante).

**Contexto:** Hoje o "Frequentador" (`User`) é cadastrado manualmente no Frequencia, sem nenhum vínculo com o sistema Pessoas (sem CPF, sem matrícula). O objetivo de longo prazo é que os dados cadastrais do Frequentador sejam espelhados a partir do Pessoas, via `sticapi_client`, e não digitados manualmente. Ver investigação completa do padrão em `pessoas2` (client Sticapi, gem, jobs, botão de reimportação em `unidades_controller.rb`).

**Decisões já tomadas com o usuário:**
- Client: gem `sticapi_client` completa (mesma usada no pessoas2), não um client próprio enxuto.
- Chave de vínculo: novo campo `cpf` em `users`.
- Estratégia: espelhamento incremental — a Sprint 8 monta o esqueleto (auth + 1 campo), sprints seguintes adicionam campos/dados sob demanda.

| # | Task | Responsabilidade | Critério de Aceitação | Status |
|---|------|------------------|----------------------|--------|
| 8.1 | Adicionar `gem "sticapi_client"` ao `Gemfile` e rodar `bundle install` | Setup | Gem instalada; `bundle check` OK | ✅ (gem `4.0.4`, trouxe `devise` como dependência transitiva, não usada) |
| 8.2 | Criar `config/sticapi.yml` (dev/test/prod) com host, porta, usuário e senha da Sticapi | Setup | Credenciais reais fornecidas pelo usuário — **não usar placeholder em produção** | ✅ Credencial real reaproveitada do pessoas2 (usuário `pessoas@sticapps.tjpi`), armazenada em Rails encrypted credentials (`config/credentials.yml.enc` + `config/master.key`), com fallback para `ENV`. Autenticação e uma chamada real (`SticapiClient::Pessoas.paises`) testadas com sucesso. **Achado de segurança durante a implementação, corrigido:** `config/master.key` estava versionado no git (commit `61b70cf`) — removido do rastreamento (`git rm --cached`, ainda não commitado) e a master key + `secret_key_base` foram **rotacionados** (nova key gerada, `credentials.yml.enc` recriado do zero com a senha da Sticapi). Chave antiga permanece no histórico do git mas não decifra mais nada relevante. **Risco residual:** a senha do Sticapi agora existe em dois repositórios (pessoas2 e Frequencia) — considerar credencial dedicada no futuro. **Pendente do usuário:** confirmar o `git rm --cached` de `master.key` (commit) e avisar quem tem clone do repo que sessões antigas serão invalidadas pela rotação do `secret_key_base` |
| 8.3 | Criar migration adicionando `cpf` (string, índice único, nullable) à tabela `users` | Modelagem | Migração roda sem erros; `cpf` aceita `null` (frequentadores ainda não vinculados) | ✅ `db/migrate/20260828130834_add_cpf_to_users.rb`, migrada em dev e test |
| 8.4 | Validar `cpf` no model `User` (formato, uniqueness quando presente, sem obrigatoriedade — mantém compatibilidade com frequentadores cadastrados manualmente) | Modelagem | Testes de model passam | ✅ `validates :cpf, uniqueness: true, format: { with: /\A\d{11}\z/ }, allow_nil: true` |
| 8.5 | Criar `app/jobs/importar_dados_pessoa_job.rb`: job (ActiveJob/`solid_queue`) que, dado um `user_id` ou `cpf`, chama `SticapiClient::Pessoas.get_by_cpf` e faz upsert dos campos espelhados no `User` correspondente | Jobs | Job roda isoladamente via console, atualiza o `User` a partir de um CPF de teste | ✅ Escopo inicial: só `nome_completo` (conforme decisão de espelhamento incremental) |
| 8.6 | Implementar lock de execução (adaptado a `solid_queue`, sem Redis — via registro de controle no banco) para evitar imports concorrentes, seguindo o padrão de `Unidade.adquirir_lock_importacao!` do pessoas2 | Jobs | Dois disparos simultâneos → apenas um executa | ✅ Via `pg_try_advisory_lock`/`pg_advisory_unlock` do Postgres (sem tabela nova, sem Redis) |
| 8.7 | Adicionar action `reimportar_dados_pessoa` em `Admin::FrequentadoresController`, que agenda (`perform_later`) o mesmo job usado no espelhamento, com checagem de "já em execução" | Controller | Mesmo padrão do botão em `unidades_controller.rb` (pessoas2) | ✅ Parcial — controller sempre enfileira; a checagem "já em execução" fica só no lock do job (sem fast-path de alerta ao usuário como no pessoas2). Avaliar se vale replicar o fast-path em sprint futura |
| 8.8 | Adicionar botão "Reimportar do Pessoas" na view `admin/frequentadores/index`, visível por frequentador ou em lote | Frontend | Botão dispara a action; feedback de sucesso/erro via flash | ✅ Botão por frequentador (só aparece quando `cpf` presente); sincronização em lote fora de escopo desta sprint |
| 8.9 | Agendar `ImportarDadosPessoaJob` em `config/recurring.yml` (Solid Queue) para rodar automaticamente uma vez por dia, à meia-noite (`schedule: "at midnight every day"`) — varrendo todos os `User` com `cpf` preenchido | Jobs | Job dispara sozinho às 00:00 em ambiente com Solid Queue scheduler ativo, sem intervenção manual | ✅ Registrado em `config/recurring.yml` (ambiente `production`) |
| 8.10 | Testes unitários do job (mock do `SticapiClient::Pessoas`) e testes de integração da action do controller | Testes | `rails test` passa; nenhuma chamada HTTP real nos testes | ✅ `test/jobs/importar_dados_pessoa_job_test.rb` (4 testes) + `test/controllers/admin/frequentadores_controller_test.rb` (3 testes); suíte completa: 187 testes, 1 falha pré-existente (timezone, não relacionada) |
| 8.11 | Documentar em `docs/` o campo de vínculo (`cpf`), o fluxo de espelhamento (automático 00:00 + manual via botão) e como adicionar novos campos nas sprints seguintes | Documentação | Documento criado, referenciado neste plano | ✅ `docs/integracao-pessoas-sticapi.md` |

**Pendências para produção:** garantir que o `config/master.key` de produção seja o mesmo gerado nesta rotação (ou a `RAILS_MASTER_KEY` correspondente esteja configurada no ambiente de deploy) — sem isso, o job falha ao tentar autenticar (log de erro, não derruba a aplicação, graças ao `rescue` em `importar_usuario`). Commitar a remoção de `master.key` do git antes do próximo push.

| # | Task | Responsabilidade | Critério de Aceitação | Status |
|---|------|------------------|----------------------|--------|
| 8.12 | **Reversão formal do ADR-0001** (decisão do usuário, 2026-09-02): abandonar o padrão Sticapi/HTTP como integração definitiva com o Pessoas — substituir por conexão direta ao Postgres do Pessoas, **somente leitura** (`SELECT`, sem `INSERT`/`UPDATE`/`DELETE`), usando o multi-database do Rails. Infraestrutura de conexão pronta; a migração dos 6 arquivos que ainda chamam `SticapiClient` fica para as próximas tasks (ver pendência abaixo) | Infra/Arquitetura | Conexão somente-leitura funcional, escrita bloqueada em duas camadas (Rails + Postgres), suíte sem regressão | ✅ **`config/master.key` regenerado** (o anterior não existia mais nesta máquina — perdido, não só removido do git; credenciais recriadas do zero via Rails encrypted credentials, nunca texto puro). **Role `app.frequencia` criado no Postgres do Pessoas** (dev local) com `GRANT SELECT` apenas — testado: leitura de 32.679 pessoas reais funciona, `INSERT`/`DELETE` são recusados pelo próprio Postgres (`permission denied for table`). **`config/database.yml`** ganhou a conexão `pessoas:` (dev/test/production) com `database_tasks: false` — o Frequencia nunca gerencia schema desse banco (é de outra aplicação); sem isso, `rails test` tentava criar `schema_migrations` lá e falhava com `PG::InsufficientPrivilege`. **`app/models/pessoas_record.rb`** — classe-base abstrata (`connects_to database: { writing: :pessoas, reading: :pessoas }`) com `readonly?` sempre `true`, bloqueando escrita também no lado do Rails (`ActiveRecord::ReadOnlyRecord`) antes mesmo de chegar no banco — redundante de propósito com o `GRANT` do Postgres. Suíte completa: 384 testes, 1120 assertions, 1 falha pré-existente (timezone, não relacionada), sem regressão. **Pendência explícita, não feita nesta task:** os 6 arquivos que hoje usam `SticapiClient` (`app/jobs/importar_dados_pessoa_job.rb`, `app/jobs/importar_servidores_unidade_job.rb`, `app/jobs/sincronizar_afastamentos_job.rb`, `app/services/resolver_cpf_por_matricula_service.rb`, `app/services/atualizar_frequentador_cache_service.rb`, `app/models/gestor_individual.rb`) continuam chamando a gem — a infraestrutura de conexão está pronta, mas a reescrita desses pontos pra usar `PessoasRecord` (mapeando as tabelas reais `pessoas`, `vinculos`, `unidades` etc.) ainda não foi feita. Enquanto isso não acontece, o app segue funcionando via Sticapi normalmente — não é uma mudança quebrando nada, é uma fundação nova rodando em paralelo |
| 8.13 | Reescrita dos 4 arquivos que faziam chamadas HTTP reais via `SticapiClient` (`importar_dados_pessoa_job.rb`, `importar_servidores_unidade_job.rb`, `resolver_cpf_por_matricula_service.rb`, `sincronizar_afastamentos_job.rb`) para consultar o Postgres do Pessoas diretamente via `PessoasRecord`, completando a pendência da task 8.12. `atualizar_frequentador_cache_service.rb` ajustado para receber o objeto `Pessoas::Pessoa` em vez do hash HTTP. `gestor_individual.rb` não tocado (não chama Sticapi, só comentário) | Infra/Arquitetura | 4 arquivos + service reescritos, models `Pessoas::*` criados espelhando só as colunas usadas, suíte sem regressão, rubocop limpo | ✅ **Models novos** em `app/models/pessoas/` (namespace `Pessoas::`, todos herdando de `PessoasRecord`): `pessoa.rb` (tabela `pessoas`; `#vinculos_ativos` = `vinculos.ativos`), `vinculo.rb` (tabela `vinculos`; scope `ativos` = `vinculo_estado.nome == "em_exercicio"` e `fim` nulo/futuro — substitui o antigo `vinculos_ativos` da Sticapi; `#lotacao_principal`/`#tipo_vinculo`), `vinculo_estado.rb` (tabela **`vinculos_estados`**, pluralização irregular confirmada em `db/schema.rb` — não é `vinculo_estados`), `lotacao.rb` (tabela `lotacoes`; `belongs_to :vinculo, :unidade`; scopes `principais`/`vigentes`), `unidade.rb` (tabela `unidades`; `#servidores` — isolado num método próprio, retorna `Struct ServidorLotado(matricula, nome)`, pra poder ser stubado nos testes), `tipo_vinculo.rb` (tabela `tipos_vinculo`), `configuracao_cadastro.rb` (tabela `configuracoes_cadastro`, ponte `vinculo → tipo_vinculo`), `competencia.rb` (tabela `competencias`), `gestorh_contracheque_mirror.rb` (tabela `gestorh_contracheque_mirrors`; `.pares_matricula_cpf_para(mes:, ano:)` — mesma fonte que alimentava `SticapiClient::Gestorh.competencia` via HTTP), `tipo_afastamento.rb` (tabela `tipos_afastamento`), `afastamento.rb` (tabela `afastamentos`; coluna **`id_intranet`** confirmada contra dados reais como sendo o mesmo `id` que o endpoint HTTP da Intranet retornava — chave usada em `AfastamentoCache#afastamento_id_pessoas`). **Achados de schema:** `unidades.descricao` (não `nome`) é o campo de exibição usado pelo antigo `lotacao_principal.unidade.descricao`; `vinculos.matricula`/`vinculos.fim`/`vinculos.vinculo_estado_id` sem indireção; `pessoas.username` é o mesmo campo que o pessoas2 usa pra ligar `User`↔`Pessoa` (`foreign_key: "username", primary_key: "username"`); `gestorh_contracheque_mirrors` tem `competencia_id`/`matricula`/`cpf` diretos, mas o pessoas2 tem múltiplas competências (uma por `orgao_id`) pro mesmo mês/ano — não filtramos por órgão pelo mesmo motivo que a Sticapi original não recebia esse parâmetro. Todas as queries validadas contra dados reais do `pessoas_development` local (32.679 pessoas) via `bin/rails runner`, não só lidas do código-fonte. **Testes:** banco `pessoas_test` (conexão `pessoas:` em test) existe mas **não tem schema carregado** (gerenciado pelo pessoas2, `database_tasks: false` de propósito) — reportado como bloqueio, não contornado inventando dado nele; em vez disso, os 4 pontos de entrada usados pelo código de produção (`Pessoas::Pessoa.find_by`, `Pessoas::Unidade.find_by`/`#servidores`, `Pessoas::GestorhContrachequeMirror.pares_matricula_cpf_para`) foram isolados em métodos únicos e stubados nos testes com `define_singleton_method`/`Struct`, no mesmo padrão que os testes já usavam pra stubar `SticapiClient` antes desta task. Suíte completa: 385 testes, 1120 assertions, 1 falha pré-existente (timezone, `presenca_endpoints_test.rb`, não relacionada), sem regressão. `bin/rubocop` limpo em todos os arquivos tocados/criados |
| 8.14 | Substituir as leituras restantes de espelho local (`FrequentadorCache`) por SELECT ao vivo no pessoas2 — pedido direto do usuário (2026-09-02), continuação da 8.12/8.13 (e mesmo padrão já aplicado em `admin/frequentadores`, tasks 10.10/10.11) | ✅ Levantamento: só 2 lugares reais ainda liam `FrequentadorCache` pra exibição — `admin/frequencia_por_orgao_controller.rb` (lista de órgãos + cpfs por órgão) e `admin/dashboard_controller.rb` (`@total_frequentadores`). `admin/frequentadores_controller.rb` já tinha sido migrado (10.10) — só sobrava um comentário mencionando o cache antigo. Os jobs que **escrevem** em `FrequentadorCache`/`AfastamentoCache` (espelho populado a partir do pessoas2) não foram tocados — continuam existindo por design, não são leitura a substituir. **`Pessoas::Vinculo` ganhou 2 métodos de classe novos**: `.orgaos_em_uso` (lista de órgãos reais — unidade da lotação principal vigente entre vínculos ativos, substitui `FrequentadorCache.distinct.pluck(:orgao)`) e `.cpfs_por_orgao(orgao)` (substitui `FrequentadorCache.where(orgao:).pluck(:cpf)`, comparação exata pelo nome já resolvido em `.orgaos_em_uso`). `Admin::DashboardController#index`: `@total_frequentadores` trocado de `FrequentadorCache.count` para `Pessoas::Vinculo.ativos.count`. Testes reescritos (`frequencia_por_orgao_controller_test.rb`, `dashboard_controller_test.rb`) — mesmo padrão de `define_singleton_method` já usado desde a 8.13 pra contornar `pessoas_test` sem schema carregado. Suíte completa: 395 testes, 1148 assertions, 1 falha pré-existente (timezone, não relacionada), sem regressão. `bin/rubocop` limpo |
| 8.15 | `admin/time_records` — filtro de "Usuário" trocado de busca por `nome_completo` local para SELECT ao vivo no pessoas2, mesma fonte/escopo do filtro Nome de `admin/frequentadores` (pedido do usuário, 2026-09-02: "deve funcionar da mesma maneira que o filtro Nome funciona em /frequentadores") | ✅ `Pessoas::Vinculo` ganhou `.cpfs_por_nome(nome)` — reaproveita exatamente a query de `frequentadores_ativos(nome:)` (vínculo ativo + `pessoas.nome ILIKE`), retornando só a lista de CPFs. **Tentativa inicial errada, corrigida**: cheguei a implementar via `Pessoas::Pessoa.cpfs_por_nome` (buscando direto na tabela `pessoas`, sem exigir vínculo ativo) — usuário apontou que não era o pedido; removido e substituído pela versão via `Pessoas::Vinculo.ativos`, consistente com o filtro de `/frequentadores`. `Admin::TimeRecordsController#index`: busca o CPF real no pessoas2 e cruza com `User` local (`TimeRecord` só existe pra quem já bateu ponto, o que exige `User` local) — nome digitado manualmente e divergente do cadastro oficial deixa de encontrar resultado. 4 testes novos/ajustados. Suíte completa: 395 testes, 1148 assertions, 1 falha pré-existente, sem regressão |
| 8.16 | `admin/users` — mesma troca de fonte já aplicada em `admin/frequentadores` (tasks 10.10/10.11): listagem deixa de ser `User.order(:nome_completo)` e passa a ser todo vínculo ativo do pessoas2, pedido direto do usuário (2026-09-02) | ✅ `Admin::UsersController#index` reescrito nos mesmos moldes de `FrequentadoresController` — `Pessoas::Vinculo.frequentadores_ativos(nome:, incluir_cpfs:, excluir_cpfs:)`, cruzando com `User` local por `cpf` pra Status/Digital/Username/Ações (mesmos filtros locais via lista de CPF: username, status, digital — não dá JOIN cross-database entre `users` e `pessoas`/`vinculos`, bancos Postgres diferentes). **Achado crítico, corrigido antes de fechar**: como a base virou só `Pessoas::Vinculo`, `User` locais **sem `cpf`** (ex: contas de admin criadas manualmente, sem vínculo no pessoas2 — 7 contas reais confirmadas no banco dev) sumiriam da tela por completo, ficando inacessíveis pela UI (sem editar/inativar/excluir). Resolvido com uma segunda seção separada na view ("Contas locais sem vínculo no Pessoas"), listando `User.where(cpf: nil)` com os mesmos filtros aplicados diretamente (sem indireção de CPF, já que são o próprio `User`). Botão "Novo Usuário" (criação manual local) mantido — continua fazendo sentido pra esse tipo de conta. Filtro de Categoria/Órgão não copiados de `/frequentadores` (fora do escopo de `/users`, que não tem esses conceitos). 3 testes novos/ajustados em `users_controller_test.rb` (stub do ponto de entrada, mesmo padrão desde 8.13; teste explícito de que `User` sem cpf aparece na seção separada). Suíte completa: 396 testes, 1153 assertions, 1 falha pré-existente, sem regressão |

---

# Parte 2 — Telas Admin, Regras de Negócio e Integração Formal com Pessoas

> **Origem:** conteúdo migrado de `docs/12-plano-implementacao/plano-sprints.md` (Sprints 1–14 daquele documento, renumeradas aqui como 9–22 para não colidir com a Parte 1). Baseado em `docs/PRD-FREQUENCIA.md` (v1.0), `docs/03-dominio/07-casos-uso.md` e ADRs 0001–0005.

## 0. Premissas e sequenciamento

### 0.1 Levantamento do estado atual (em `Frequencia/api-ponto`)

| Camada | Estado |
|---|---|
| Repositório + CI | ✅ Existe (`daviaraujo18/Frequencia`), brakeman + rubocop + testes com Postgres em CI |
| Canal EstaçãoPonto (EP-01..EP-12) | ✅ 11 controllers em `app/controllers/presenca/`, `CryptoDes` (DES+UrlBase64) implementado — ver Parte 1, Sprints 1–7 |
| Modelos reais / schema | ⚠️ `User`, `TimeRecord`, `EstacaoPonto` existem. Nenhum outro agregado do domínio (`Regime`, `Dia`, `RegistroMensalFrequencia`, `RelatorioFrequenciaFinal`) existe no banco |
| Telas admin (AdminLTE) | ✅ 9 telas prontas (`dashboard`, `frequentadores`, `regimes`, `estacoes`, `direitos_deveres`, `frequencia`, `frequencia_por_orgao`, `relatorio_terceirizados`, `gestores_individuais`, `versoes`) — layout e colunas da grid já definidos |
| Dados por trás das telas | ❌ Maioria dos controllers ainda retorna array vazio hardcoded (`@regimes = []`, `@estacoes = []` antes da Sprint 9) — fachada visual, sem model/query real (exceto `estacoes` e `frequentadores`, já em andamento — ver Sprints 9 e 10) |
| Estrutura DDD (`domain/application/infrastructure`) | ❌ Não iniciada — código é Rails MVC padrão |

**Conclusão:** o trabalho de UI/layout está feito e não deve ser refeito. O gargalo é modelar as entidades e alimentar as grids com dado real — isso é a prioridade da Fase A abaixo, antes de qualquer motor de cálculo.

### 0.1.1 O que vem do Pessoas vs. o que é próprio de Frequência

Levantamento em `pessoas2/app/models`: já existem `Pessoa`, `Vinculo` (+ subtipos: efetivo, comissionado, cedido, terceirizado, estagiário...), `Orgao`, `Cargo`, `Lotacao`, `Afastamento`/`TipoAfastamento`/`MotivoAfastamento`. Isso cobre boa parte do que as grids de Frequência precisam exibir, expostos via `SticapiClient::Pessoas` (mesmo client já instalado na Sprint 8).

| Tela / Entidade | Dado já existe no Pessoas? | Tratamento na Fase A |
|---|---|---|
| `admin/frequentadores` (Nome, Comarca/Órgão, Vínculo) | ✅ `Pessoa` + `Vinculo` + `Orgao`, via `SticapiClient::Pessoas` | Tabela de **espelho** local (`frequentadores_cache`), alimentada via `sticapi_client` — não é cadastro próprio |
| `admin/frequentadores` (Digital) | ❌ é dado biométrico específico do ponto | Já existe em `users.digitais_hash` (tabela já existente em Frequência, não é nova) |
| `admin/direitos_deveres` (afastamentos) | ✅ `Afastamento`/`TipoAfastamento`/`MotivoAfastamento`, via `SticapiClient::Pessoas` | Tabela de **espelho** local (`afastamentos_cache`), alimentada via `sticapi_client` |
| `admin/estacoes` | ❌ não existe no Pessoas (é ativo físico do ponto) | Tabela própria em Frequência (mantido — já implementada, Sprint 9) |
| `admin/regimes` (jornadas) | ❌ não existe no Pessoas (regra de ponto, não cadastro) | Tabela própria em Frequência (mantido) |
| `admin/frequencia` (registros brutos) | ❌ não existe no Pessoas | Tabela própria em Frequência (mantido) |
| `admin/gestores_individuais` | ⚠️ existe `gestor_contrato.rb` no Pessoas, mas é conceito de gestão de contrato, não de gestão de ponto — não reaproveitar | Tabela própria em Frequência (mantido) |
| `admin/versoes` | ❌ não existe no Pessoas | Tabela própria em Frequência (mantido) |

**Mecanismo de consumo definido (Sprints 10 e 12):** client `sticapi_client` (gem oficial, já instalada na Sprint 8), autenticado via token HTTP (`POST /auth/sign_in`), chamando o módulo `SticapiClient::Pessoas`. A leitura é feita por um **job agendado** via Solid Queue (`config/recurring.yml`), que grava/atualiza uma tabela de espelho local uma vez por dia — mais um **gatilho manual** (botão na tela) para quando o dado precisa ficar fresco antes do próximo horário agendado. As telas de Frequência sempre leem o espelho local, nunca a Sticapi diretamente em tempo de request — isso garante que uma queda da Sticapi não derruba as telas administrativas. Não há conexão de banco entre os dois sistemas nem credencial de banco compartilhada — toda comunicação é via API HTTP autenticada. Continua sendo uma solução simplificada (não é o padrão API+eventos formal do ADR-0001), reformalizada na Sprint 21 (Fase C).

### 0.2 Nova ordem de prioridade

1. **Fase A — Popular o front** (Sprints 9–15): para cada tela já existente, criar o model/migration mínimo e a query de listagem, sem regra de negócio de cálculo. Critério "pronto" = a tela mostra dados reais do banco, com cadastro básico (CRUD) onde fizer sentido.
2. **Fase B — Regras de negócio** (Sprints 16–20): cálculo diário, consolidação mensal, banco de horas, relatório final, retificador, gestão de exceções — tudo o que hoje ainda não existe por trás das telas populadas na Fase A.
3. **Fase C — Integração formal com Pessoas + cutover** (Sprints 21–22): promover a integração cadastral simplificada da Fase A para o contrato oficial (API+eventos, ADR-0001/ADR-0006) e desligar a Intranet.

- **Dependência técnica fixa (não reordenável dentro da Fase B):** cálculo diário (Sprint 16) → consolidar mês (Sprint 17) → relatório final (Sprint 18).
- **Estratégia:** Strangler Fig (ADR-0002) — a Intranet `presenca` continua viva até o desligamento formal (última sprint).
- **Fora de escopo em todas as sprints:** qualquer alteração na EstaçãoPonto desktop (ADR-0003) e qualquer mudança de regra de cálculo sem evidência (DUV-005 já resolvida: v2 é o motor oficial).
- **Débito técnico assumido conscientemente:** na Fase A, dado cadastral (`Frequentador`, `Direito`) é lido do Pessoas via `sticapi_client` (API HTTP autenticada, não é o contrato canônico do ADR-0006, que ainda não existe) e espelhado localmente por job diário + gatilho manual — não é o padrão API+eventos definitivo do ADR-0001. Isso é destrinchado e corrigido na Fase C (Sprint 21), quando se decide se o padrão final mantém espelho via polling ou migra para eventos assíncronos — não é para virar solução definitiva sem revisão.
- **ATUALIZAÇÃO (2026-09-02, task 8.12):** decisão do usuário reverte a direção acima — o padrão final deixa de ser API+eventos (ADR-0001) e passa a ser **conexão direta somente-leitura ao Postgres do Pessoas** (`SELECT` via usuário `app.frequencia`, sem `INSERT`/`UPDATE`/`DELETE`). Infraestrutura já pronta (`app/models/pessoas_record.rb`, conexão `pessoas:` em `config/database.yml`). Isso torna obsoleta a pergunta em aberto da task 21.2 ("espelho local vs. eventos assíncronos") — não é mais uma escolha entre essas duas opções, é migrar os 6 pontos que usam `SticapiClient` pra ler via `PessoasRecord`. Sprint 21 precisa ser revisada à luz disso antes de ser executada.

---

## Fase A — Popular o front (Sprints 9–15)

### Sprint 9 — Model de Estações + tela `admin/estacoes`

**Status:** ✅ CONCLUÍDA (tasks 9.1–9.6) — 171 testes na conclusão original (9.1–9.5); 384 testes após a 9.6 (2026-09-02), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, não relacionada a `EstacaoPonto`; débito de outra task)

**Objetivo:** sair de `@estacoes = []` para dados reais, já usando o mesmo agregado que o canal `presenca/*` precisa (`EstacaoPonto`).

| Task | Descrição |
|---|---|
| 9.1 | Migration + model `EstacaoPonto` — campos vistos na grid: descrição, versão, último contato, VNC, AnyDesk, TeamViewer, observação, `codAtivacao` ✅ CONCLUÍDA |
| 9.2 | `Admin::EstacoesController#index` passa a consultar o model real (substituir array vazio) ✅ CONCLUÍDA |
| 9.3 | Formulário de cadastro/edição básico (sem validação de regra de negócio ainda) ✅ CONCLUÍDA |
| 9.4 | Ligar ao canal já existente: os controllers `presenca/adicione_estacao_controller.rb` e afins passam a gravar/consultar `EstacaoPonto` real em vez de dado solto ✅ CONCLUÍDA |
| 9.5 | Testes: listagem exibe estações cadastradas, cadastro básico funciona ✅ CONCLUÍDA |
| 9.6 | Replicar estrutura de dados legado das estações (`RegistroEstacaoPonto`, `EstacaoPing`, campos ausentes em `EstacaoPonto`) + importar dado real das 473 estações e do último registro de sincronização de cada uma (sem histórico bruto) — pedido direto do usuário, 2026-09-02 ✅ CONCLUÍDA (ver nota detalhada abaixo) |

#### Detalhamento da task 9.6 — Estrutura de dados legado das Estações (pedido direto, 2026-09-02)

**Contexto:** pedido direto do usuário, fora da numeração de sprint, para investigar o legado Intranet (`/intranet/presenca/EstacaoPonto`, tabelas `presenca_estacaoponto`, `presenca_registroestacaoponto`, `presenca_estacaoponto_ping` — schema real confirmado via MySQL `10.254.3.6:3306/intranet`) e replicar a **estrutura de dados** das estações no Frequencia, sem migrar dado histórico e sem lógica de negócio.

**Feito:**
- Migration `AddLegacyFieldsToEstacoesPonto` — adiciona a `estacoes_ponto` os campos do legado ainda ausentes: `codigo_unico_maquina` (string), `momento_inicio`/`momento_fim` (date), `liberado_batida_manual` (boolean, default `false`), `ativo` (boolean, default `true`).
- Model `RegistroEstacaoPonto` (tabela `registro_estacao_pontos`, `belongs_to :estacao_ponto`) — equivalente a `presenca_registroestacaoponto` (log de sincronização): `arquivo_criptografado`, `momento_processamento`, `momento_sinc`, `processado`, `ip`. Nasce vazia — os ~4,5M registros históricos do legado não foram migrados (decisão do usuário via `AskUserQuestion`).
- Model `EstacaoPing` (tabela `estacao_pings`, `belongs_to :estacao_ponto`) — equivalente a `presenca_estacaoponto_ping` (histórico completo de heartbeats, diferente de `EstacaoPonto#ultimo_contato`, que só guarda o último): `ip`, `momento`, `versao`. Nasce vazia — os ~46,7M registros históricos do legado não foram migrados.
- `EstacaoPonto` ganhou `has_many :registro_estacao_pontos, dependent: :destroy` e `has_many :estacao_pings, dependent: :destroy`.
- Nomenclatura confirmada via `ActiveSupport::Inflector.pluralize` antes de assumir (histórico de bugs de inflexão em português no projeto — `estacao`, `frequentador`, `categoria`, `cache`, `gestor individual`, `versao`, todos em `config/initializers/inflections.rb`): `estacao_ping` → `estacao_pings` e `registro_estacao_ponto` → `registro_estacao_pontos` pluralizam corretamente sem precisar de regra irregular nova.
- **Gap de i18n corrigido en passant:** faltava a chave `errors.messages.required` em `config/locales/pt-BR.yml` (usada pelo Rails para presença de `belongs_to`, distinta de `blank`) — sem ela, qualquer `belongs_to` obrigatório (inclusive os pré-existentes, como `TimeRecord belongs_to :user`) exibia "Translation missing" em vez de mensagem traduzida. Adicionada como `"não pode ficar em branco"`, mesmo texto de `blank`.
- **NÃO replicado (decisão consciente, documentada):**
  - `responsavel_id` em `EstacaoPonto` — seria FK para um "Usuario" do Intranet, sem equivalente no Frequencia; não inventada.
  - `ativo_bkp` — campo de backup/migração do legado, sem valor fora dele.
  - `Predio`/relação many-to-many `presenca_estacao_predio` — não existe conceito de "Predio" no Frequencia hoje; escopo maior, fora desta task.
  - `VersaoEstacaoPonto` — já coberto pelo model `Versao` existente (Sprint 14.4); nada novo criado.
  - Qualquer lógica de negócio do legado (`isAlive`, `getStatus`/`EstadoEstacaoEnum` com thresholds de dias, singleton `Estacoes` em memória, hash MD5) — fora de escopo, só estrutura de dados.
  - Os ~4,5M registros de `presenca_registroestacaoponto` (histórico completo) e ~46,7M de `presenca_estacaoponto_ping` — nenhum dado histórico bruto foi migrado, só estrutura vazia (decisão do usuário via `AskUserQuestion`).

**Atualização (2026-09-02) — importação de dados reais autorizada pelo usuário:** confirmado (novo `AskUserQuestion`) o que alimenta de fato a tela `/presenca/EstacaoPonto` no legado (`EstacaoPonto-explore.jsp`): Descrição/AnyDesk/TeamViewer/Observação vêm direto de `presenca_estacaoponto`; **VNC não é campo próprio — é o IP do último `RegistroEstacaoPonto` de cada estação** (`ep.ultimoRegistroEstacaoPonto.ipEnxuto`); as colunas Versão/Último Contato existem no cabeçalho HTML mas o binding de dados está **comentado no JSP** (`<%--${ep.ultimoEstacaoPing.versao}--%>`) — não há dado real ali no legado hoje. Importado (script one-off via `bin/rails runner`, contra o banco de desenvolvimento local, MySQL lido com charset `latin1`→UTF-8 explícito por causa de acentuação): **472 `EstacaoPonto` novas** (+1 PoC pré-existente = 473, batendo com o legado) e **450 `RegistroEstacaoPonto`** (só o mais recente de cada estação, não os 4,5M históricos — 1 estação ficou sem registro por nunca ter sincronizado). Campo `ativo` calculado pela regra real de `EstacaoPonto.java#isAtivo()` (`codigoAtivacao` presente + `momentoFim` nulo ou futuro), não pela coluna crua do banco (que tinha 260/473 valores `NULL` e não é o que o legado usa pra decidir status) — resultado: 393 ativas / 80 inativas. `EstacaoPonto#vnc_ip`/`#ultimo_registro_estacao_ponto` novos (replicam o regex de extração de IP do legado), `Admin::EstacoesController#index` com eager load (`includes(:registro_estacao_pontos)`), coluna VNC do index ligada ao dado real. A tabela HTML de `/estacoes` permanece com as mesmas 8 colunas do legado (Descrição/Versão/Último Contato/VNC/AnyDesk/TeamViewer/Observação/Ações) — decisão explícita do usuário de não adicionar colunas novas na listagem, mesmo com os campos extras existindo no banco/formulário. Suíte completa após a importação: 384 testes, 1120 assertions, 1 falha pré-existente (timezone, não relacionada).
- **Testes:** 13 testes novos (`test/models/registro_estacao_ponto_test.rb`, `test/models/estacao_ping_test.rb`, mais casos adicionados a `test/models/estacao_ponto_test.rb` para os campos novos e as associações). `bin/rubocop` sem ofensas nos arquivos alterados. Suíte completa: 384 testes, 1120 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde a Sprint 14.1, não relacionada) — sem regressão.

### Sprint 10 — Sincronização de Frequentadores (espelho via Sticapi) + tela `admin/frequentadores`

**Objetivo:** popular a grid (Nome, Comarca/Órgão, Vínculo, Digital, Gerência) a partir de um **espelho local** dos dados de `Pessoa`/`Vinculo`/`Orgao` do Pessoas, obtidos via `sticapi_client` (`SticapiClient::Pessoas`), atualizado por job diário + gatilho manual. Reaproveita a fundação já estabelecida na Sprint 8 (gem instalada, `config/sticapi.yml`, campo `cpf` em `users`, job base de importação já rodando em produção com escopo mínimo: `nome_completo`).

| Task | Descrição | Status |
|---|---|---|
| 10.1 | Confirmar fundação da Sprint 8 já aplicada: gem `sticapi_client`, `config/sticapi.yml`, campo `users.cpf`, `ImportarDadosPessoaJob` básico | ✅ Confirmado — ver Sprint 8 |
| 10.2 | Migration + model `FrequentadorCache` (tabela de **espelho**, não cadastro próprio) — campos: `cpf` (chave, único), `pessoa_id_pessoas` (id no Pessoas), `nome`, `orgao`, `vinculo`, `sincronizado_em` | ✅ `db/migrate/20260828133924_create_frequentador_caches.rb` + `app/models/frequentador_cache.rb`; 6 testes em `test/models/frequentador_cache_test.rb`, todos passando |
| 10.3 | Estender `ImportarDadosPessoaJob` (ou criar `SincronizarFrequentadoresJob`) — para cada `User` com `cpf` preenchido, chama `SticapiClient::Pessoas.get_by_cpf`/`find` e faz upsert em `FrequentadorCache` (além do `nome_completo` já atualizado direto em `users` desde a Sprint 8) | ✅ Upsert completo: `pessoa_id_pessoas` (`id`), `nome`, `orgao` (`lotacao_principal.unidade.descricao`), `vinculo` (`vinculos_ativos[0].tipo_vinculo.nome`) e `sincronizado_em`. Formato do payload confirmado em 2026-08-28 com uma chamada real de teste (CPF do próprio usuário, autorizado por ele, nunca persistido em nenhum arquivo/log/commit). 6 testes, todos passando |
| 10.4 | Agendamento diário (00:00) — já coberto pela Sprint 8 (task 8.9); só validar que a extensão do job continua rodando no mesmo agendamento | ✅ Validado — `config/recurring.yml` continua apontando para `ImportarDadosPessoaJob` (mesma classe, não foi criado job novo); modo em lote (sem `user_id`, o mesmo usado pelo agendamento) testado manualmente e confirmado fazendo upsert completo em `FrequentadorCache` (`orgao`/`vinculo` inclusos) |
| 10.5 | Gatilho manual — já coberto pela Sprint 8 (task 8.8, botão "Reimportar do Pessoas"); validar que também atualiza o `FrequentadorCache` | ✅ Validado com teste de integração (`perform_enqueued_jobs`, executa o job de verdade a partir do POST no controller) — botão atualiza `nome`, `orgao` e `vinculo` no cache |
| 10.6 | Confirmar vínculo do login local da estação (`users.cpf`, já criado na Sprint 8) com o `FrequentadorCache` correspondente | ✅ Formalizado com associações `User belongs_to :frequentador_cache` / `FrequentadorCache has_one :user` (via `cpf`, `optional: true` — frequentador sem cpf continua válido). 4 testes novos cobrindo os dois sentidos e os casos sem cpf/sem cache |
| 10.7 | `Admin::FrequentadoresController#index` passa a combinar `User` (nome, digital) com `FrequentadorCache` (órgão, vínculo) — nunca a Sticapi diretamente na hora do request | ✅ `includes(:frequentador_cache)` no controller (evita N+1, testado explicitamente contando queries SQL); view exibe `orgao`/`vinculo` reais quando o cache existe, mantém "—" quando não. 3 testes novos (exibição com cache, exibição sem cache, ausência de N+1) |
| 10.8 | Testes: job faz upsert corretamente (mock do `SticapiClient::Pessoas`, sem chamada HTTP real), sincronização manual funciona, tela continua respondendo com o espelho antigo se a Sticapi estiver fora do ar no horário do job, frequentador sem CPF vinculado não quebra a tela | ✅ Cobertura fechada: upsert (10.3), sincronização manual (10.5), 2 testes novos para "Sticapi fora do ar" (job engole exceção e preserva cache antigo; tela renderiza com o espelho antigo sem nenhuma chamada HTTP), e "sem cache" já coberto na 10.7. Suíte completa: 207 testes, 1 falha pré-existente (timezone, não relacionada) |
| 10.9 | Habilitar o filtro de Órgão em `admin/frequentadores` (index) — estava `disabled: true` desde o mockup original, usuário reportou "não clicável" (2026-08-31, mesmo padrão do achado na Sprint 11.7) | ✅ Removido `disabled: true`, filtro real via `frequentador_caches.orgao ILIKE`. **Filtro de Categoria continua desabilitado, documentado como pendência real** (não é bug): `FrequentadorCache` nunca guardou uma "categoria" — só `orgao` e `vinculo`. O payload real da Sticapi tem um campo `categoria_trabalhador_ativa` (confirmado na investigação da Sprint 10, task 10.3) que provavelmente é isso, mas nunca foi mapeado pra cá; trazer esse campo é trabalho novo (migration + `AtualizarFrequentadorCacheService` + jobs), não feito agora por decisão do usuário. 2 testes novos. Suíte completa: 317 testes, 1 falha pré-existente (timezone, não relacionada) |
| 10.10 | **Troca de fonte de dados de `admin/frequentadores`** (decisão do usuário, 2026-09-02, no âmbito da reversão formal do ADR-0001 nas tasks 8.12/8.13): a tela deixa de listar `User` locais (espelhados via `FrequentadorCache`/Sticapi) e passa a listar **todo `Pessoas::Vinculo.ativos`** do pessoas2 (3.920 vínculos ativos reais, confirmado via SQL direto) — inclusive quem ainda não tem `User` cadastrado localmente no Frequencia | ✅ **Query:** `Pessoas::Vinculo.frequentadores_ativos(nome:, orgao:, incluir_cpfs:, excluir_cpfs:)`, novo método de classe em `app/models/pessoas/vinculo.rb` — concentra a query complexa (nome, órgão, filtros de User local) num único ponto de entrada, tanto pra manter o controller enxuto quanto pra servir de único lugar a stubar nos testes (mesmo motivo da 8.13: `pessoas_test` existe mas não tem schema carregado). Filtro de nome via `joins(:pessoa).where("pessoas.nome ILIKE ?", ...)`. **Filtro de órgão:** como `lotacao_principal` não é um `belongs_to` direto em `Vinculo` (é resolvido em Ruby, ordenando por `inicio: :desc`), filtrar via **subquery de IDs** (`Pessoas::Lotacao.principais.merge(vigentes).joins(:unidade).where(...).select(:vinculo_id)` + `where(id: ...)`) em vez de um LEFT JOIN direto — decisão deliberada pra não arriscar duplicar linhas de vínculo (uma pessoa pode, em tese, ter mais de uma lotação "principal" vigente simultânea), o que quebraria a contagem/paginação do Kaminari. **Exibição da lotação/unidade:** pré-carregada em lote só pros vínculos da página atual via `Pessoas::Vinculo.unidades_por_vinculo(vinculo_ids)` (outro método de classe isolado), evitando 1 query por linha — mesmo raciocínio de `#lotacao_principal`, mas em lote. `tipo_vinculo` (via `configuracao_cadastro`) eager-loaded normalmente com `includes(configuracao_cadastro: :tipo_vinculo)` por ser `belongs_to` direto, sem risco de duplicação. **Vínculo sem User local:** a linha aparece normalmente (é vínculo ativo real do pessoas2); colunas Status/Digital mostram badge "Sem cadastro local"/"—", e a ação de reimportar/editar/inativar vira um botão desabilitado com tooltip explicativo — cadastro automático de `User` a partir de um vínculo do Pessoas **não foi implementado** (fora do escopo pedido, sinalizado como possível feature futura). **Status/Digital continuam vindo do `User` local** (não existem no pessoas2), ligados por `cpf`: `User.where(cpf: cpfs_da_pagina).index_by(&:cpf)`, pré-carregado em lote pra página inteira (1 query `IN`, não 1 por linha — validado com teste de contagem de SQL). **Filtros "Status"/"Digital"/"Frequentadores inativos"** (só existem no User local) resolvidos no controller como listas de CPF (`incluir_cpfs`/`excluir_cpfs`) calculadas a partir da tabela `users` e passadas pro método de classe — não dá pra fazer JOIN cross-database entre `pessoas` (banco do pessoas2) e `users` (banco do Frequencia), são bancos Postgres diferentes; quando "Frequentadores inativos" está desmarcado (padrão), oculta só quem tem `User` local inativo — vínculo sem `User` local nenhum sempre aparece. Filtro de "Sem Digital" vira **exclusão** (não inclusão) dos cpfs com digital cadastrada, pra incluir automaticamente quem não tem cadastro local nenhum. **Filtro de Categoria não foi tocado nesta task** (continua `disabled: true`, pendência documentada desde a 10.9 — resolvida na task 10.11 logo abaixo). **Testes:** reescritos 11 dos 13 testes de `frequentadores_controller_test.rb` (2 removidos — "filtra por órgão" e "espelho antigo" não se aplicam mais, a lógica de filtro de órgão agora vive dentro do método de classe stubado, não testável no nível de controller sem schema real do banco `pessoas_test`), seguindo o mesmo padrão de `define_singleton_method` já usado desde a 8.13 — `Pessoas::Vinculo.frequentadores_ativos`/`.unidades_por_vinculo` stubados com `Struct`s e `Kaminari.paginate_array(...).page(1)` (evita depender de dados reais). Teste de N+1 reescrito pra contar especificamente a query `IN` de `users.cpf` (1 query esperada, não 1 por linha). `bin/rubocop` limpo nos arquivos `.rb` tocados (`.erb` não é lintado por rubocop neste projeto — sem gem `erb_lint`/config equivalente). Suíte completa: 383 testes, 1116 assertions, 1 falha pré-existente (timezone, `presenca_endpoints_test.rb`, não relacionada), sem regressão (queda de 385→383 testes é a remoção documentada acima, não perda de cobertura) |
| 10.11 | Habilitar o filtro de Categoria em `admin/frequentadores` (index) — pendência documentada desde a 10.9, resolvida agora que a leitura é direta no Postgres do pessoas2 (task 10.10) | ✅ Fonte real: `categorias_trabalhador` (pessoas2) — chegada via `Vinculo -> ConfiguracaoCadastro -> TipoVinculo -> CategoriaTrabalhador` (nenhuma dessas 4 pontes existe como coluna direta, só associação). Models novos: `Pessoas::CategoriaTrabalhador` (tabela `categorias_trabalhador`, scope `.em_uso`) e `belongs_to :categoria_trabalhador` adicionado em `Pessoas::TipoVinculo`. **Achado:** a tabela inteira tem 45 categorias (taxonomia genérica do eSocial — a maioria irrelevante aqui, ex: "Trabalhador rural", "Cooperado", "Transportador autônomo") — decisão deliberada de **não** listar todas no select; o scope `.em_uso` restringe às categorias que aparecem de fato entre vínculos ativos reais (join por nome de tabela, não por associação Rails, porque os models enxutos deste namespace não replicam todas as associações intermediárias do pessoas2 — ex: `TipoVinculo` não tem `has_many :configuracoes_cadastro` aqui). Validado contra o banco real: **5 categorias em uso hoje** (Agente público - Outros; Estagiário — 414 vínculos; Servidor público ocupante de cargo exclusivo em comissão; Servidor público titular de cargo efetivo/magistrado/etc.; Trabalhador cedido/exercício em outro órgão). `Pessoas::Vinculo.frequentadores_ativos` ganhou o parâmetro `categoria_trabalhador_id:`, filtrando via `joins(configuracao_cadastro: :tipo_vinculo).where(tipos_vinculo: { categoria_trabalhador_id: ... })`. View: `select_tag :categoria` deixou de ser lista fixa em português (`disabled: true`) e passou a `options_for_select(@categorias_trabalhador.map { |c| [c.descricao, c.id] }, ...)`, habilitado. **Achado operacional, não relacionado ao código:** o servidor Puma local estava rodando desde antes da task 8.12 (conexão `pessoas:` só existe em `config/database.yml` depois disso) — igual ao bug de `inflections.rb` já visto nesta sessão, `database.yml` só é lido no boot; precisou reiniciar o processo pra reconhecer a nova conexão, gerando o erro transitório "The `pessoas` database is not configured for the `development` environment" (não era erro de configuração real). 2 testes novos (`test/controllers/admin/frequentadores_controller_test.rb`): opções reais no select via stub de `Pessoas::CategoriaTrabalhador.em_uso`, e repasse do param `categoria` pro método de classe. `bin/rubocop` limpo. Suíte completa: 385 testes, 1122 assertions, 1 falha pré-existente (timezone, não relacionada), sem regressão |

### Sprint 10B — Importação em Massa de Frequentadores por Unidade (Sticapi)

**Objetivo:** inverter a direção da Sprint 10 — em vez de só *atualizar* um `User` que já tem `cpf` preenchido manualmente, a aplicação passa a **descobrir e criar** Frequentadores automaticamente a partir de uma unidade do Pessoas, puxando os cadastros de verdade da Sticapi.

**Contexto/decisão com o usuário:** a Sprint 10 resolvia "dado um CPF já vinculado, mantenha os dados atualizados". Isso não resolve o cenário real: hoje nenhum frequentador tem `cpf`, e ninguém vai digitar isso manualmente para dezenas de pessoas — a aplicação precisa puxar a lista inteira do Pessoas. Escopo inicial: **uma unidade piloto** (a mesma da lotação do usuário, já validada com 87 servidores), antes de expandir para todos os órgãos do TJPI. `User`s são criados automaticamente quando não existem.

**Investigação técnica (2026-08-28), com o CPF do próprio usuário, autorizado por ele — nada persistido em arquivo/log/commit:**
- `SticapiClient::Pessoas.unidade(id:)` retorna a unidade com `servidores` (array de `{matricula, nome, cargo, inicio_vinculo, inicio_lotacao, exercicios_funcoes}`) — **sem CPF**
- `/pessoas/find` (via `SticapiClient::Pessoas.find`) **não resolve por matrícula** — só aceita `cpf` ou `username` (campo distinto da matrícula; testado e confirmado que matrícula sozinha retorna vazio)
- `SticapiClient::Gestorh.competencia(mes:, ano:)` retorna a folha inteira do TJPI (~4700 pessoas testado com jul/2026) com `{matricula, nome, cpf, nascimento, tipo_vinculo, folha}` — **é o elo que resolve matrícula → CPF**
- **Achado importante:** nem toda matrícula aparece em toda competência (testado: a matrícula do próprio usuário não apareceu na competência de jul/2026, provavelmente por diferença de vínculo/formato) — o job precisa **tolerar CPF não resolvido** sem quebrar o restante do lote

| # | Task | Responsabilidade | Critério de Aceitação | Status |
|---|------|------------------|----------------------|--------|
| 10B.1 | Confirmar a unidade piloto (id já identificado nos testes) e a competência (mês/ano) a usar para resolução matrícula→CPF | Planejamento | Decisão registrada aqui, sem hardcode escondido no código | ✅ Unidade piloto: id `110001469` (lotação principal do usuário, 87 servidores confirmados via `SticapiClient::Pessoas.unidade`). Competência: a mais recente disponível — regra de cálculo ainda a implementar na 10B.3, não fixar mês/ano aqui |
| 10B.2 | Criar `app/services/pessoas/resolver_cpf_por_matricula_service.rb` (ou similar) — busca a competência mais recente via `SticapiClient::Gestorh.competencia`, monta um mapa `matricula => cpf` em memória (cache de request, não persiste os ~4700 registros no banco) | Service | Dado um array de matrículas, retorna o mapa correspondente; matrícula não encontrada não gera erro | ✅ `app/services/resolver_cpf_por_matricula_service.rb` (sem namespace `Pessoas::`, seguindo o padrão flat já usado em `app/services/` do projeto). Recebe `mes`/`ano` explícitos por enquanto — a lógica de "competência mais recente sem hardcode" fica pra 10B.3, que ainda não foi implementada. 6 testes (resolução, matrícula não encontrada, competência vazia, normalização integer/string, registros sem matrícula/cpf, memoização — só 1 chamada HTTP mesmo com múltiplas buscas na mesma instância) |
| 10B.3 | Decidir e implementar como obter "a competência mais recente" sem hardcode de mês/ano fixo no código (ex.: mês/ano atual, com fallback para o mês anterior se o atual ainda não tiver fechado folha) | Service | Não quebra em janeiro (ano anterior) nem no primeiro dia do mês | ✅ `ResolverCpfPorMatriculaService.mais_recente(matriculas)` — em vez de "cai pro mês anterior só se o atual estiver vazio" (podia dar falso negativo: mês atual existir mas não conter a matrícula específica, ex. servidor recém-lotado), busca **mês atual + mês anterior e mescla**, com o mês atual tendo prioridade quando a matrícula aparece nos dois. 3 testes novos: prioridade do mês atual, fallback pro anterior quando só ele tem a matrícula, e virada de ano (janeiro → dezembro do ano anterior, via `travel_to`) |
| 10B.4 | Criar `app/jobs/importar_servidores_unidade_job.rb` — dado um `unidade_id`, busca `SticapiClient::Pessoas.unidade`, resolve CPFs via 10B.2, e para cada servidor resolvido: `User.find_or_create_by!(cpf:)` (com senha temporária/aleatória, sem digital, status a definir) + upsert em `FrequentadorCache` (reaproveitando a lógica de `ImportarDadosPessoaJob#atualizar_cache`, extraída para um método/service comum) | Jobs | Roda contra a unidade piloto; cria os `User`s que não existiam; não duplica os que já existiam (por `cpf`) | ✅ `app/jobs/importar_servidores_unidade_job.rb` + lógica de cache extraída para `app/services/atualizar_frequentador_cache_service.rb` (reaproveitado por `ImportarDadosPessoaJob` também, sem regressão — 9 testes antigos continuam passando). Senha placeholder via `SecureRandom.hex(16)` (decisão registrada na Sprint 21 — login local fica inutilizável até a troca de autenticação). `User` só é criado se ainda não existir pelo `cpf` (não sobrescreve `nome_completo` de quem já existia — mesmo comportamento conservador do `ImportarDadosPessoaJob`). 6 testes: criação nova, não duplicação, matrícula não resolvida pulada, erro em 1 servidor não impede os demais, unidade vazia, lock de concorrência (chave distinta de `ImportarDadosPessoaJob::LOCK_KEY`) |
| 10B.5 | Definir e documentar o que preencher em campos que `User` exige mas a Sticapi não fornece diretamente: `password` (aleatória, forçar troca depois?), `username` (**decisão do usuário 2026-08-28: usar o `username` real vindo do payload da Sticapi — `dados["username"]` — em vez do `generate_username` local; mesmo campo que o próprio pessoas2 usa para vincular seu `User` a `Pessoa` via `has_one :pessoa, foreign_key: "username", primary_key: "username"`. `generate_username` vira só fallback para quando a Sticapi não retornar `username`**), `status` (ativo por padrão?) | Modelagem | Regra documentada aqui e no código; sem violar `has_secure_password`/validações existentes; usuários já existentes não têm o `username` sobrescrito nesta sprint (evita quebrar login/sessão ativa — só se aplica na criação) | ✅ `ImportarServidoresUnidadeJob#criar_user_se_necessario` agora usa `dados["username"]` quando presente, cai no `generate_username` (callback já existente no model) quando ausente. `status` confirmado como default do schema (`1`, ativo), sem set explícito. 3 testes novos (username real, fallback local, status default) |
| 10B.6 | Servidor sem CPF resolvido (matrícula não encontrada na competência) — logar e pular, sem quebrar o restante do lote | Jobs | Teste: unidade com 1 matrícula não resolvível entre várias resolvíveis → as outras são importadas normalmente | ✅ Já não quebrava o lote desde a 10B.4 (`next` no `each`); adicionado `Rails.logger.warn` explícito por matrícula pulada, testado capturando o log real (`assert_match`) |
| 10B.7 | Lock de execução (mesmo padrão advisory lock do Postgres da Sprint 8/`ImportarDadosPessoaJob`), chave distinta por não ser o mesmo job | Jobs | Duas execuções simultâneas para a mesma unidade → só uma roda | ✅ Já implementado desde a 10B.4 — `LOCK_KEY = 592_017_384` (distinta de `ImportarDadosPessoaJob::LOCK_KEY = 837_462_915`), mesmo padrão `pg_try_advisory_lock`/`pg_advisory_unlock`. Teste de concorrência (segunda conexão Postgres real) confirmado passando |
| 10B.8 | Action + botão único "Importar servidores desta unidade" em `admin/frequentadores` — **um clique dispara `ImportarServidoresUnidadeJob` para a unidade inteira de uma vez** (não é um botão por pessoa; decisão explícita do usuário: o polling manual não deve ser um-a-um) | Controller/Frontend | Um clique enfileira a importação de todos os servidores da unidade piloto; distinto do botão "Reimportar do Pessoas" por frequentador já existente (Sprint 8), que continua servindo para atualizar 1 pessoa específica já cadastrada | ✅ Botão "Importar servidores da unidade piloto" em `page_actions` (topo da tela, não por linha) → `POST /frequentadores/importar_unidade` → `Admin::FrequentadoresController#importar_unidade` → `ImportarServidoresUnidadeJob.perform_later(UNIDADE_PILOTO_ID)`. `UNIDADE_PILOTO_ID = 110_001_469` fixo no controller (constante documentada — seletor de unidade/expansão pra outros órgãos é escopo de sprint futura). 3 testes: enfileiramento correto, execução real criando múltiplos `User`s de uma vez (prova que não é um-a-um), exige autenticação |
| 10B.9 | Testes: mock de `SticapiClient::Pessoas.unidade` e `SticapiClient::Gestorh.competencia` (sem chamada HTTP real), criação de `User` novo, não duplicação de `User` existente, matrícula não resolvida não quebra o lote, `FrequentadorCache` populado igual ao `ImportarDadosPessoaJob` | Testes | `rails test` passa, nenhuma chamada HTTP real | ✅ Cobertura já fechada incrementalmente nas tasks 10B.2–10B.8 (29 testes entre `resolver_cpf_por_matricula_service_test.rb`, `importar_servidores_unidade_job_test.rb` e `frequentadores_controller_test.rb`) — auditado critério por critério, todos os cenários pedidos aqui já existiam. 100% via `define_singleton_method`, nenhuma chamada HTTP real. Suíte completa: 228 testes, 1 falha pré-existente (timezone, não relacionada) |
| 10B.10 | Rodar de verdade contra a unidade piloto (ambiente de dev, com `bin/jobs` ativo) e validar visualmente na tela `admin/frequentadores` — critério de "puxar tudo de lá" atendido para essa unidade | Validação | Frequentadores da unidade piloto aparecem na tela sem cadastro manual prévio | ✅ Rodado de verdade contra os 87 servidores da unidade piloto: **69 `User`s + `FrequentadorCache`s criados** (diferença = matrículas não resolvidas na competência ou sem nome retornado, comportamento tolerante já esperado). Página `admin/frequentadores` validada via HTTP real (200, 69 botões de reimportação, 14 travessões para quem ficou sem cpf/cache). **`bin/jobs` não é necessário em dev**: `config/environments/development.rb` não define `queue_adapter`, então o Rails usa `:async` (thread em processo) — rodei o job direto e de forma síncrona. **Bug real encontrado e corrigido durante a validação:** `vinculos_ativos` no payload da Sticapi vem como Array quando a pessoa tem múltiplos vínculos, mas como **Hash único** quando tem só 1 — `AtualizarFrequentadorCacheService` corrigido com `Array.wrap` (mesmo idioma já usado pelo próprio pessoas2 em `registro_importacao.rb`/`association_error_detail_concern.rb`), 4 testes novos em `test/services/atualizar_frequentador_cache_service_test.rb`. Reexecutado o job após a correção: 69/69 com `orgao` e `vinculo` preenchidos |

**Fora de escopo desta sprint:** expandir para todos os órgãos do TJPI (fica para uma sprint futura, após validar o piloto), reconciliar/desativar `User`s que saíram da unidade (alguém que se desliga não é automaticamente inativado — decisão de negócio a parte), biometria/digital (continua exigindo cadastro manual na estação, isso não vem do Pessoas).

---

### Sprint 11 — Model de Regimes + tela `admin/regimes`

| Task | Descrição | Status |
|---|---|---|
| 11.1 | Migration + model `Regime` (+ `RegimeFrequentador`, AG-2) — campos: categoria, nome, modalidade, resumo, meta semanal | ✅ `db/migrate/..._create_regimes.rb` (categoria, nome, modalidade, resumo, meta_semanal — todos string, campos de negócio do legado (`limiteCredito`, `permitidoCompensarFalta` etc.) deliberadamente fora, ficam pra Fase B) + `db/migrate/..._create_regime_frequentadores.rb` (user_id, regime_id, tipo, momento_inicial, momento_final — sem os campos de auditoria `*Original`/`dataAlteracao` do legado, fora de escopo). Models `Regime`/`RegimeFrequentador` com associações (`User has_many :regimes, through: :regime_frequentadores`). 8 testes |
| 11.2 | `Admin::RegimesController#index` consulta o model real | ✅ `Regime.order(:nome)` + filtro por nome (`ILIKE`); view trocada de acesso Hash (`regime[:nome]`) para atributo de model (`regime.nome`). Coluna "Gerência" ficou vazia por ora (ações de editar/excluir são escopo da 11.3). 4 testes novos |
| 11.3 | Cadastro/edição básico de regime (sem motor de cálculo associado ainda) | ✅ CRUD completo (`new`/`create`/`edit`/`update`/`destroy`), mesmo padrão de `EstacoesController` (partial `_form` compartilhada, `require_admin` nas ações de escrita). `destroy` trata `ActiveRecord::DeleteRestrictionError` (regime com frequentadores vinculados) com alerta em vez de erro 500. 11 testes novos (CRUD + validação + restrição de exclusão + acesso não-admin) |
| 11.4 | Testes: listagem, associação básica regime↔frequentador | ✅ Cobertura já fechada incrementalmente nas tasks 11.1–11.3 — listagem em `regimes_controller_test.rb`, associação `Regime has_many :users, through: :regime_frequentadores` em `regime_test.rb`/`regime_frequentador_test.rb`. 23 testes entre os 3 arquivos, todos passando |
| 11.5 | Trazer a estrutura **real** do `Regime` da Intranet legada (não só o mockup simplificado da 11.1) — decisão do usuário 2026-08-31: "a Intranet vai deixar de existir, precisamos de algo próprio com os mesmos regimes e regras de negócio". Investigação em `intranet/src/modules/presenca/beans/Regime.java` + `ConfiguracaoFrequencia.java` (código-fonte real disponível no workspace, sem dump de dados/linhas reais — só estrutura). Escopo combinado: **só schema, sem motor de cálculo** (Fase B continua sendo Sprints 16+) | ✅ Categorias viram muitos-pra-muitos (`regime_categorias`, como `presenca_regime_categoriavinculo` do legado — antes era 1 string por regime); `ConfiguracaoFrequencia` do legado (`@Embeddable`) virou colunas diretas em `regimes` (`pode_faltar`, `permitido_acumular_horas`, `permitido_compensar_falta`, `permitido_contabilizar_horas_mesmo_com_meta_zero`, `maximo_banco_horas_diario_em_segundos` default 7200, `limite_credito`, `limite_debito`, `percentual_carga_minima`, `limite_dias_carga_minima`, `liberado_limitacao_inicio_hora_extra`); `expediente` (era `List<Horario>` serializado em JSON/`@Lob` no legado) virou `jsonb` nativo do Postgres; `modalidade` validado contra `Regime::MODALIDADES_DISPONIVEIS` (`HORAS`/`HORAS_COM_INTERVALO`/`OCORRENCIAS`); `anterior`/`padrao` como auto-referência opcional (versionamento do legado, sem lógica de clone/corte de período — isso é Fase B). **Não replicado de propósito:** o bug conhecido do legado (`ConfiguracaoFrequencia#clone` copia `limiteCredito` no lugar de `limiteDebito` — doc `02-regime-jornada.md` Q4); a restrição incerta de `HORAS_COM_INTERVALO` não aparecer no cadastro (Q1, dúvida não resolvida nem pelo time original); toda a lógica de cálculo (`metaSemanal`, `periodosSemana`, `getResumo` dinâmico) — fica manual/string por enquanto, igual já era. Migration corrigiu bug de inflexão do Rails: "categoria" termina em "-ia" e o Rails recusa pluralizar (mesmo padrão de "bacteria"/"bacterium"), quebrando o nome da tabela do model `RegimeCategoria` — corrigido com `inflect.irregular` (mesmo padrão já usado pra "estacao"/"frequentador"). Formulário ganhou multi-select de categorias e select de modalidade (antes eram campos de texto livre). 10 testes novos (`regime_test.rb`, `regime_categoria_test.rb`, `regimes_controller_test.rb`). Suíte completa: 272 testes, 1 falha pré-existente (timezone, não relacionada) |
| 11.6 | Alinhar a modelagem de dados do `Regime` com os valores **reais** usados no banco de produção da Intranet (`10.254.3.6:3306/intranet`, acesso confirmado via `hibernate.properties` do checkout local em `intranet/`) — decisão do usuário 2026-08-31: "quero que copie a modelagem de dados dos regimes" (só estrutura/enums, não linhas/dados pessoais). Achados: `presenca_regime_categoriavinculo` usa 6 códigos reais (`SERVIDOR_CARREIRA`, `CARGO_COMISSIONADO`, `ESTAGIARIO`, `TERCEIRIZADO`, `AUXILIAR_DA_JUSTICA`, `CEDIDO`) — diferentes dos rótulos em português que a 11.5 tinha copiado do filtro da tela, não do enum real; `modalidade` só usa `HORAS` (285/309) e `OCORRENCIAS` (24/309) na prática — `HORAS_COM_INTERVALO` nunca aparece em nenhum registro real, o que **confirma sem resolver** a dúvida Q1 da doc de engenharia reversa | ✅ `Regime::CATEGORIAS_DISPONIVEIS` corrigido pros 6 códigos reais; `Regime::CATEGORIAS_LABELS` novo (mapa código→rótulo legível, só para exibição — o valor persistido continua sendo sempre o código real). View (`_form.html.erb`, `index.html.erb`) atualizada pra mostrar rótulo mas salvar/filtrar pelo código. Testes/fixtures atualizados pros códigos reais em `regime_test.rb`/`regime_categoria_test.rb`/`regimes_controller_test.rb`, incluindo teste explícito de que o rótulo antigo em português ("Servidor Efetivo") não é mais válido e que `CATEGORIAS_LABELS` cobre todos os códigos de `CATEGORIAS_DISPONIVEIS`. 3 testes novos. Suíte completa: 274 testes, 1 falha pré-existente (timezone, não relacionada) |
| 11.7 | Habilitar os filtros de Categoria/Modalidade em `admin/regimes` (index) — estavam `disabled: true` desde a Sprint 11.1/11.2 (mockup original), o usuário reportou "não são clicáveis e provavelmente não possuem dados" ao tentar usar (2026-08-31) | ✅ Não era bug — os selects já tinham os dados certos (herdados da 11.6), só estavam desabilitados de propósito (placeholder do mockup, nunca implementado). Removido `disabled: true`, `Admin::RegimesController#index` ganhou filtro por `categoria` (join em `regime_categorias`) e `modalidade` (where direto), combináveis entre si e com o filtro de nome já existente. 5 testes novos |
| 11.8 | Corrigir a exibição de `modalidade` para rótulo legível em vez do código cru — pedido do usuário (2026-08-31): "Horas", "Horas com intervalo", "Ocorrências" (em vez de `HORAS`/`HORAS_COM_INTERVALO`/`OCORRENCIAS`) | ✅ `Regime::MODALIDADES_LABELS` novo (mesmo padrão de `CATEGORIAS_LABELS`), aplicado no select do formulário (`_form.html.erb`), no filtro (`index.html.erb`) e na coluna da tabela — valor persistido continua sendo o código. 3 testes novos, cobrindo os textos exatos pedidos. Suíte completa: 315 testes, 1 falha pré-existente (timezone, não relacionada) |
| 11.9 | Replicar a base de dados real que alimenta `/presenca/Regime-explore` no legado, populando `admin/regimes` com os mesmos dados (pedido direto do usuário, 2026-09-02) | ✅ **Investigação**: `Regime-explore.jsp` mostra Categorias/Nome/Modalidade/Resumo/Meta Semanal/Gerência + contador de frequentadores vinculados (`RegimeFrequentadorDao.countByRegimeNaData`); `RegimeDao.paginateList` só lista regimes com `global=true AND excluido=false AND visivel=true AND id NOT IN (anterior_id de outro regime não-excluído)` — de 309 regimes reais na tabela, só **78** aparecem de fato na tela (o resto são versões antigas substituídas, invisíveis ou excluídas). `hashExpediente` (nome enganoso) já é o JSON de horários no mesmo formato do `expediente` jsonb que o Frequencia já tinha desde a 11.5. **Migration** `AddExcluidoEVisivelToRegimes` — `regimes` ganha `excluido`/`visivel` (não existiam, precisos pra replicar a mesma semântica de exibição do legado). **Importação real** (script one-off via `bin/rails runner`, banco de desenvolvimento local, MySQL lido com `--default-character-set=latin1` + `iconv` pra UTF-8, mesmo procedimento já usado na importação de `EstacaoPonto` na task 9.6): **309 `Regime` criados**, **75 `RegimeCategoria`** (só os 6 códigos já confirmados na 11.6), **97 regimes** com `anterior_id`/`padrao_id` resolvidos via mapa `legacy_id → novo_id` em duas passadas (os ids do legado não batem com os novos, gerados pelo Postgres). `resumo`/`meta_semanal` deixados `nil` — não são colunas do legado, são calculados dinamicamente em `Regime.java#getResumo()`/`#getMetaSemanalFormatada()` a partir do `expediente`, e o Frequencia ainda não tem esse motor (Fase B, Sprint 16, em andamento em paralelo) — não inventado aqui. **`Admin::RegimesController#index`** ganhou o mesmo filtro do legado por padrão (`excluido: false, visivel: true`, excluindo quem é `anterior_id` de outro regime não-excluído) — confirmado contra o banco real: exatamente 78 regimes aparecem, batendo com a contagem do legado. **Fora de escopo, decisão consciente**: `presenca_regimefrequentador` (31.320 vínculos regime↔frequentador do legado) não foi importada — liga por `frequentador_id` interno do legado, não por CPF, exigiria a mesma resolução matrícula/frequentador→CPF já adiada nas Sprints 10B/21; o contador de frequentadores vinculados (`${regime.count}` do legado) não foi implementado na tela por depender desse dado. 4 testes novos (`regimes_controller_test.rb`: exclui `excluido`, exclui `!visivel`, exclui superado por `anterior_id`, mas mostra o antigo se a versão nova estiver excluída). `bin/rubocop` limpo. Suíte completa: 389 testes, 1137 assertions, 1 falha pré-existente (timezone, não relacionada), sem regressão |

### Sprint 12 — Sincronização de Direitos/Deveres (espelho via Sticapi) + tela `admin/direitos_deveres`

**Objetivo:** popular a grid (Tipo, Frequentador, Cargo, Lotação, Momento Inicial/Final, Status) a partir de um **espelho local** de `Afastamento`/`TipoAfastamento`/`MotivoAfastamento` do Pessoas, obtidos via `sticapi_client`, reaproveitando a mesma infraestrutura de job/autenticação da Sprint 10.

| Task | Descrição |
|---|---|
| 12.1 | Migration + model `AfastamentoCache` (espelho, não cadastro próprio) — campos: `afastamento_id_pessoas` (chave), `cpf` (referência a `FrequentadorCache`), `tipo`, cargo, lotação, `momento_inicial`/`momento_final`, status | ✅ `db/migrate/..._create_afastamento_caches.rb` (`afastamento_id_pessoas` único, `cpf` indexado, `tipo`/`cargo`/`lotacao`/`momento_inicial`/`momento_final`/`status`). Model `AfastamentoCache` com `belongs_to :frequentador_cache` via `cpf` (mesmo padrão de `User`↔`FrequentadorCache`); `FrequentadorCache` ganhou `has_many :afastamento_caches`. **Bug de inflexão do Rails encontrado e corrigido:** `singularize("frequentador_caches")` retornava `"frequentador_cach"` (sem o "e" final — Rails trata "-ches" como plural de "-ch", ex. "churches"→"church"), quebrando a inferência de classe do `has_many`; corrigido com `inflect.irregular "cache", "caches"` (mesmo padrão já usado pra "estacao"/"frequentador"/"categoria"). 7 testes novos, suíte completa: 281 testes, 1 falha pré-existente (timezone, não relacionada) |
| 12.2 | Job `SincronizarAfastamentosJob` — chama o(s) endpoint(s) correspondente(s) do módulo `SticapiClient::Pessoas` (mesma autenticação da Sprint 10) e faz upsert em `AfastamentoCache` | ✅ **Correção sobre o plano original**: o endpoint real não é `SticapiClient::Pessoas`, é `SticapiClient::Intranet.afastamentos(cpf:)` — confirmado pela documentação inline da própria gem (`[{id pessoa_id pessoa_nome pessoa_cpf afastamento inicio fim fim_vinculo tipo_desvinculacao}]`). Mesma autenticação/token de app da Sprint 8 (é a mesma `Sticapi::SticapiClient`, só módulo diferente). **Achado importante:** esse payload não tem `cargo`/`lotacao`/`status` — os campos correspondentes de `AfastamentoCache` (task 12.1) ficam `nil` por enquanto, sem inventar de onde viriam. Job segue o mesmo padrão de lock (`LOCK_KEY` distinto: `194_773_608`) e tolerância a erro por usuário dos jobs anteriores. 7 testes (upsert simples, múltiplos afastamentos, não duplica, resposta vazia não quebra, usuário sem cpf ignorado, erro em 1 usuário não impede os demais, lock de concorrência). Suíte completa: 288 testes, 1 falha pré-existente (timezone, não relacionada) |
| 12.3 | Agendar em `config/recurring.yml` — mesmo horário da Sprint 10 (00:00), rodando logo em seguida para evitar concorrência com o job de Frequentadores | ✅ `sincronizar_afastamentos` agendado pra `00:05` (5 min depois de `importar_dados_pessoa`, meia-noite). Validado com `Fugit.parse` diretamente (biblioteca real usada pelo Solid Queue) — os dois schedules resolvem pra `Fugit::Cron` válidos, com exatamente 5 min de diferença entre as próximas execuções |
| 12.4 | Gatilho manual "Sincronizar agora" em `admin/direitos_deveres`, mesmo padrão da Sprint 10 | ✅ Botão único (não por linha — a tela lista afastamentos, não frequentadores, então não faria sentido um botão por linha; segue a mesma lógica "não um-a-um" da 10B.8) → `POST /direitos_deveres/sincronizar_agora` → `SincronizarAfastamentosJob.perform_later` (sem argumento, sincroniza todos os frequentadores com cpf de uma vez). 4 testes novos. Suíte completa: 292 testes, 1 falha pré-existente (timezone, não relacionada) |
| 12.5 | `Admin::DireitosDeveresController#index` lê `AfastamentoCache` | ✅ `AfastamentoCache.includes(:frequentador_cache).order(momento_inicial: :desc)` + filtro por tipo. View trocada de acesso Hash pra atributos reais; "Frequentador" via `registro.frequentador_cache&.nome`; "Cargo"/"Lotação"/"Status" renderizam vazio por ora (não vêm do endpoint real, achado da 12.2); coluna "Gerência" vazia (sem CRUD manual nesta tela, é só espelho). 4 testes novos |
| 12.6 | Se a tela precisar de um tipo de "direito" que só existe no domínio de Frequência (ex. autorização específica de ponto, não afastamento cadastral) — sinalizar caso a caso e só aí avaliar tabela própria (não espelho) | ✅ Avaliado — condição não se aplica agora. Os "direitos" específicos do domínio de Frequencia (autorizar horas extras, autorizar batida em prédio não permitido, batida manual/errata, desconsiderar/reconsiderar ponto) já existem e já estão corretamente escopados na Sprint 19 (UC-08 a UC-11, Fase B — Gestão de exceções), fora da tela `admin/direitos_deveres` (que só lista Licença/Férias/Afastamento/Viagem a Serviço, tudo vindo do Pessoas via `AfastamentoCache`). Nada a criar agora; revisitar se surgir necessidade nova antes da Sprint 19 |
| 12.7 | Testes: job faz upsert corretamente (mock do client, sem chamada HTTP real), sincronização manual funciona, cargo/lotação ausentes não quebram a tela | ✅ Cobertura já fechada incrementalmente nas tasks 12.1–12.5 (22 testes entre `sincronizar_afastamentos_job_test.rb`, `direitos_deveres_controller_test.rb`, `afastamento_cache_test.rb`) — upsert via mock, sincronização manual, e telas renderizando normalmente com `cargo`/`lotacao`/`status` ausentes (nunca populados, por não existirem no payload real). Suíte completa: 296 testes, 1 falha pré-existente (timezone, não relacionada) |

### Sprint 13 — Grid de Frequência (registros brutos) — tela `admin/frequencia`

**Objetivo:** exibir os dados que o canal EstaçãoPonto **já está recebendo** (Parte 1, Sprint 5) — aqui é só ligar a grid à leitura, sem calcular nada ainda.

| Task | Descrição |
|---|---|
| 13.1 | Confirmar/ajustar model que representa o registro bruto já processado (`RegistroFrequencia`, membro interno do futuro `Dia`/AG-3 — aqui ainda tratado como leitura simples, sem a modelagem completa de `Dia`) | ✅ `TimeRecord` (já existia desde a Sprint 1) confirmado como o equivalente ao `RegistroFrequencia` do legado. **Gap real encontrado e corrigido**: `SincronizarRegistrosPontoController` já validava a `EstacaoPonto` de origem mas descartava o vínculo — `TimeRecord` não guardava de qual estação veio a batida (necessário pra coluna "Estação" da grid). Adicionado `estacao_ponto_id` (nullable — registros antigos e o sentinela de OS não suportado não têm uma `EstacaoPonto` real) + controller agora passa o vínculo real no `create!`. `raw_data`/`punched_at`/`authentication_mode`/`punch_type` já cobrem Data/Operação/Modo; "Lotação Época" não tem fonte de dado ainda (seria a lotação da pessoa no momento da batida — `FrequentadorCache` só guarda a atual, sem histórico; não inventado, fica pendente). 4 testes novos (model + integração, incluindo o caso do sentinela sem estação real). Suíte completa: 300 testes, 1 falha pré-existente (timezone, não relacionada) |
| 13.2 | `Admin::FrequenciaController#index` consulta os registros reais (Frequentador, Data, Operação, Modo, Lotação Época, Estação) | ✅ `TimeRecord.includes(:user, :estacao_ponto).order(punched_at: :desc)` + filtros por data/frequentador/estação. View traduz `punch_type` (entry/exit → Entrada/Saída) e `authentication_mode` (biometric/manual → Biometria/Manual). "Lotação Época" continua "—" (gap já documentado na 13.1, sem fonte de dado). 8 testes novos, suíte completa: 308 testes, 1 falha pré-existente (timezone, não relacionada) |
| 13.3 | Filtros básicos de listagem (por frequentador/data/estação) | ✅ Já implementado junto na 13.2 (`Admin::FrequenciaController#index`); auditado — os 3 filtros pedidos (frequentador, data, estação) têm teste dedicado cada um em `frequencia_controller_test.rb` |
| 13.4 | Testes: grid mostra batidas reais recebidas pelo canal `presenca/*` | ✅ Teste de ponta a ponta de verdade: `POST /presenca/ajax/SincronizarRegistrosPonto` (ingestão real, criptografada com `CryptoDes`) → `GET admin/frequencia` confirma a batida na tela, incluindo nome do frequentador e estação — não só cada lado testado isoladamente. Suíte completa: 309 testes, 1 falha pré-existente (timezone, não relacionada) |

### Sprint 14 — Telas agregadas: `frequencia_por_orgao`, `gestores_individuais`, `relatorio_terceirizados`, `versoes`

**Objetivo:** telas de consulta/agregação simples em cima do que já foi populado nas sprints 9–13.

| Task | Descrição |
|---|---|
| 14.1 | `frequencia_por_orgao` — query agregada (Trabalhado/Presenças/Ausências) sobre os dados da Sprint 13 | ✅ Agrupamento por `FrequentadorCache.orgao`. **Presenças** = dias distintos com pelo menos 1 batida (`TimeRecord`) no período filtrado. **Ausências** = contagem de `AfastamentoCache` (Sprint 12) iniciados no período. **Trabalhado** deliberadamente **não computado** — exigiria o motor de cálculo diário (horas entre entrada/saída), que é Fase B (Sprint 16); aparece como "—" em vez de inventar um número. Filtros por mês/ano/órgão. 8 testes novos. Suíte completa: 325 testes, 1 falha pré-existente (timezone, não relacionada) |
| 14.2 | `gestores_individuais` — model `GestorIndividual` + listagem (Nome, Órgão, Gerenciados) | ✅ Verificado o legado (`presenca_gestorindividual`) antes de modelar: no legado é tabela de ligação (vínculo do gestor em Pessoas → frequentador local gerido), e a Sticapi expõe o equivalente real via `SticapiClient::Intranet.gestores_individuais` — importar isso exigiria resolução matrícula→CPF (mesmo problema da 10B), fora de escopo aqui. Implementado como cadastro local simples (Fase A, mesmo espírito de `Regime`/`EstacaoPonto`): `GestorIndividual` (nome, orgao) `has_many :gerenciados` (Users) via `gestor_individual_gerenciados`. **Bug de pluralização en português encontrado de novo** ("gestor individual" → "gestores individuais", as duas palavras concordam — Rails só pluraliza uma) — resolvido com `self.table_name` explícito (mesmo padrão do `EstacaoPonto`) em vez de inflexão, porque inflexão de palavra única não bastava aqui. 9 testes novos. Suíte completa: 334 testes, 1 falha pré-existente (timezone, não relacionada) |
| 14.3 | `relatorio_terceirizados` — definir/confirmar com o gestor o que essa tela deve exibir (não há coluna definida no layout ainda — levantamento pendente) | 🚧 **BLOQUEADA** — investigação completa, implementação pendente de decisão fora do escopo desta task. Legado: `/intranet/presenca/DynMostraRegistroFrequencia-relatorioTerceirizados` filtra Mês/Ano + Empresa → Contrato, e pra cada `Frequentador` do contrato mostra um bloco (nome, situação de vínculo, função/lotação, regime) + calendário diário completo carregado via AJAX (`DynMostraRegistroFrequencia-submit`, template não localizado) — não é grid simples. Rastreei a origem de Empresa/Contrato: `tjpi_terceirizacao_empresa`/`tjpi_terceirizacao_contrato` são cadastro manual dentro do próprio Intranet (sem sync externo); o vínculo pessoa→contrato (`tjpi_vinculo.contratoTerceirizacao_id`, 2127 registros reais) idem. **Achado decisivo**: o **pessoas2 já tem o substituto moderno** — models `Contrato`, `EmpresaTerceirizada`, `VinculoTerceirizado` (dados reais vindos do Odoo via `Odoo::ListarContratosTerceirizacaoService`, ativamente mantidos) — mas isso só existe como tela interna autenticada do pessoas2 (`/vinculos_terceirizados`); **não há endpoint no `sticapi_client`** (gem que o Frequencia consome) expondo essa informação. Sem uma ponte oficial Sticapi, não há como popular Empresa/Contrato/vínculo no Frequencia sem quebrar o padrão de integração do projeto (tudo via Sticapi) ou acessar o Postgres de outro sistema diretamente — decisão consciente do usuário: **pausar e documentar como pendência formal**, não inventar a integração. Próximo passo real: solicitar à equipe do pessoas2 um endpoint Sticapi para `VinculoTerceirizado`/`Contrato`/`EmpresaTerceirizada`. Nenhum código/model/migration criado. |
| 14.4 | `versoes` — model simples de versões da EstaçãoPonto/app (Versão, Novidades, Link) | ✅ Havia um placeholder da tela (`Admin::VersoesController#index` com `@versoes = []` e rota só de `:index`) — substituído por model real `Versao` (`numero` obrigatório, `novidades` text, `link` string) e CRUD completo (index/new/create/edit/update/destroy), seguindo o mesmo padrão de `Regime` (Sprint 11): `before_action -> { require_admin }` nas ações de escrita, form parcial compartilhado entre `new`/`edit`. **Bug de pluralização em português encontrado de novo** — confirmado via `ActiveSupport::Inflector.pluralize("versao")` retornando `"versaos"` (perde o "ão" nasal) em vez de `"versões"`; resolvido com `inflect.irregular "versao", "versoes"` em `config/initializers/inflections.rb` (mesmo padrão de `estacao`/`frequentador`/`categoria`/`cache` já acumulados no projeto). Cadastro 100% local, sem integração Sticapi/Pessoas (não se aplica aqui). 17 testes novos (4 model + 13 controller, incluindo guarda de admin nas ações de escrita). Suíte completa: 351 testes, 1 falha pré-existente (timezone, não relacionada) |
| 14.5 | Testes: cada tela lista dado real, sem cálculo de negócio embutido | ✅ Auditoria das 3 telas implementadas na Sprint 14 (`frequencia_por_orgao`, `gestores_individuais`, `versoes`) contra os testes já existentes (`test/controllers/admin/*_controller_test.rb` das tasks 14.1/14.2/14.4). **Gap real encontrado**: em `frequencia_por_orgao`, o teste existente que comprova "Trabalhado" = "—" (`trabalhado aparece como travessao`) só cobre o caso sem nenhum `TimeRecord` no período — não provava que a coluna continua "—" mesmo havendo dados suficientes para calcular horas trabalhadas (entrada + saída completas no mesmo dia), deixando aberta a possibilidade de um cálculo condicional escondido. Adicionado 1 teste (`trabalhado continua travessao mesmo com batidas de entrada e saida completas`) que cria `TimeRecord`s de entrada (08:00) e saída (17:00) no mesmo dia para o mesmo frequentador e assevera que a coluna "Trabalhado" permanece "—" em todas as linhas — fechando o gap sem duplicar a cobertura já existente. **`gestores_individuais`**: sem gap — não há campo de cálculo (contagem de `gerenciados` é associação direta, não motor de negócio) e os testes existentes já provam dado real via banco (cria gestor + gerenciados → aparece com contagem correta; base vazia → mensagem de "nenhum gestor"). **`versoes`**: sem gap — CRUD 100% local sem campo calculado, já com cobertura completa de create/update/destroy/validação/guarda de admin usando dados reais do banco. 1 teste novo adicionado. Suíte completa: 352 testes, 1038 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, não relacionada a esta task, mesma falha reportada desde 14.1) — sem regressão. `bin/rubocop` sem ofensas no arquivo alterado. |

### Sprint 15 — Dashboard consolidado

| Task | Descrição |
|---|---|
| 15.1 | `Admin::DashboardController` — KPIs simples a partir dos dados já populados (contagem de frequentadores, estações ativas, batidas do dia) | ✅ Já existia um `Admin::DashboardController#index` de sprint anterior (métricas focadas em `User`: total/ativos/inativos, com digitais, batidas hoje/semana/mês, últimas entradas/saídas) — estendido, sem remover nada, com os 2 KPIs que faltavam pro escopo desta task: **Frequentadores** = `FrequentadorCache.count` (espelho local do Pessoas, Sprint 8/10) e **Estações de Ponto** = `EstacaoPonto.count`. Investigado o schema (`db/schema.rb`) antes de nomear o segundo KPI: **não existe coluna `ativa`/status de ativação em `estacoes_ponto`** (nem em `EstacaoPonto`, nem em `Admin::EstacoesController`/view, que só exibem `ultimo_contato` cru, sem badge de status) — não havia como decidir uma janela de heartbeat para "estação ativa" sem inventar uma regra de negócio nova, o que violaria a restrição de Fase A (task 15.2). Decisão: contar toda estação cadastrada (`EstacaoPonto.count`), documentado em comentário no controller — mesmo espírito de "dado real, sem cálculo" das tasks 14.x. "Batidas do dia" reaproveita `@registros_hoje`, já existente. 2 cards novos (`small-box`) na view, mesmo padrão AdminLTE/Bootstrap 5 dos cards existentes, com ícones bootstrap-icons SVG inline e link para as telas de detalhe (`frequentadores_path`/`estacoes_path`) |
| 15.2 | Nenhum KPI depende de cálculo de banco de horas/fechamento (isso só existe na Fase B) | ✅ Restrição de design respeitada, não é tarefa separada implementada — confirmado que os 5 KPIs do dashboard (usuários total/ativos/inativos/com digitais, batidas hoje/semana/mês, frequentadores, estações) são todos contagens diretas (`.count`) ou queries de filtro por data/tipo, sem nenhuma agregação de horas trabalhadas, banco de horas ou fechamento — esse motor só existe a partir da Sprint 16 |
| 15.3 | Testes: dashboard carrega sem erro com dados reais e com base vazia | ✅ 2 testes novos em `test/controllers/dashboard_controller_test.rb` (que já tinha 2 testes prévios de sprint anterior — carregar dashboard autenticado e redirecionar se não autenticado, mantidos intactos): "deve exibir contagem real de frequentadores, estacoes e batidas do dia" (limpa fixtures de `TimeRecord`/`FrequentadorCache`/`EstacaoPonto` — respeitando ordem de FK, `TimeRecord` antes de `EstacaoPonto` — cria 2 frequentadores, 1 estação e 1 batida, e confirma via regex no HTML que cada KPI aparece com o número correto ao lado do rótulo certo, evitando falso positivo por múltiplos `<h3>` iguais no mesmo dashboard) e "deve carregar dashboard sem erro com base vazia" (mesma limpeza, sem criar nada, confirma resposta `:success` e todos os 3 KPIs novos/afetados em "0", sem exception). Nenhum teste valida número calculado de horas trabalhadas/banco de horas — só contagens diretas, conforme 15.2. Suíte completa: 354 testes, 1054 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. `bin/rubocop` sem ofensas nos arquivos alterados (`dashboard_controller.rb`, `dashboard_controller_test.rb`). **Critério de saída da Fase A atingido**: as 9 telas admin da Fase A (`frequencia_por_orgao`, `gestores_individuais`, `versoes`, `estacoes`, `regimes`, `direitos_deveres`, `frequentadores`, `time_records`, `dashboard`) exibem dados reais do banco; `relatorio_terceirizados` (14.3) segue formalmente bloqueada por pendência de integração, fora do controle desta sprint. Nenhuma regra de cálculo de frequência foi implementada |

**Critério de saída da Fase A:** todas as 9 telas admin exibem dados reais do banco, com cadastro básico onde aplicável. Nenhuma regra de cálculo de frequência foi implementada ainda — é aceitável que os números de frequência sejam apenas listagem/contagem simples nesta fase.

---

## Fase B — Regras de negócio (Sprints 16–20)

### Sprint 16 — Motor de cálculo diário (UC-05)

**Objetivo:** implementar o cálculo diário oficial (v2), agora sobre o modelo `Frequentador`/`Regime` já populados na Fase A. Aqui formaliza-se `Dia` (AG-3) como raiz, com `RegistroFrequencia` e `CalculoDiario` como membros internos — não mais leitura solta como na Sprint 13.

**Débito técnico herdado da Sprint 11 (task 11.5, 2026-08-31):** o schema do `Regime` já tem os campos reais da Intranet legada (`expediente` jsonb, `configuracao` — limites de crédito/débito, banco de horas etc.), mas **nenhuma lógica de cálculo foi portada ainda**. Ao implementar 16.2/16.3, portar de `intranet/src/modules/presenca/beans/Regime.java`:
- `metaSemanal()`/`metaSemanalInMilis()` — soma de `Horario.metaSemanal(modalidade)` por item do `expediente`
- `periodosSemana()`/`periodosDoDia()`/`getHorariosDaSemana()` — mapeia `expediente` (jsonb) para períodos por dia da semana, usados pelo cálculo diário
- `getResumo()`/`getMetaSemanalFormatada()` — hoje `resumo`/`meta_semanal` no Frequencia são strings preenchidas manualmente; motor de cálculo deveria computá-las a partir do `expediente`, como o legado faz
- Estratégia por modalidade (`HORAS`, `HORAS_COM_INTERVALO`, `OCORRENCIAS`) — `CalculoDiarioService.java:171-183` no legado
- **Não replicar:** o bug `ConfiguracaoFrequencia#clone` (copia `limiteCredito` em `limiteDebito`) nem a restrição incerta de `HORAS_COM_INTERVALO` fora do cadastro (Q1, sem consenso nem no time original) — decisões já tomadas na 11.5, mantidas aqui.
- Precedência `TEMPORARIO > DIFERENCIADO > OFICIAL` quando um frequentador tem mais de um `RegimeFrequentador` vigente (`Regimento.getRegimeFrequentador` do legado) — ainda não modelada no Frequencia (hoje `RegimeFrequentador.tipo` existe como campo livre, sem essa regra de precedência aplicada)

| Task | Descrição | UC | Status |
|---|---|---|---|
| 16.1 | Modelar agregado `Dia` (AG-3) corretamente — migrar a leitura simples da Sprint 13 para essa estrutura | Nota crítica do PRD §4.2 | ✅ **Nota recuperada (2026-09-02)** — esta task foi implementada numa sessão anterior mas a nota de status se perdeu do arquivo (drift real, corrigido agora a partir do histórico da sessão). `Dia` implementado como **PORO** (`app/models/dia.rb`, não persistido) — espelha o legado (`Dia.java` também não é entidade JPA, só agrega `RegistroFrequencia`/`CalculoDiario` em memória); persistir uma tabela `dias` duplicaria dado já existente em `time_records` sem necessidade, já que ainda não há motor de cálculo (16.2/16.3). `CalculoDiario` ganhou tabela real (`calculo_diarios`, `belongs_to :user`, campos de resultado nullable) — vai guardar o resultado do cálculo que 16.2/16.3 ainda vão preencher, sem nenhuma lógica de cálculo implementada ainda (escopo estritamente respeitado). `RegistroFrequencia`: confirmado que `TimeRecord` já cobre isso (decisão da 13.1) — nenhum model novo criado. 17 testes novos (`calculo_diario_test.rb`, `dia_test.rb`). Suíte na época: 371 testes, 1080 assertions, 2 falhas pré-existentes documentadas (timezone; flake de horário em `TimeRecordsControllerTest`, não relacionadas), sem regressão |
| 16.2 | Portar regras do motor v2 (oficial, DUV-005) — inclui a lógica de `Regime.java` listada acima (meta semanal, períodos por dia, resumo dinâmico) | UC-05 | ✅ Portados 5 métodos novos de instância em `app/models/regime.rb`, todos puros (só leem o próprio `expediente` jsonb, sem tocar `TimeRecord`/`Dia`/`CalculoDiario` — orquestração fica pra 16.3): `meta_semanal_em_minutos` (`Regime.java:390-400`+`Horario.java:91-98/99-103`, soma por período — HORAS: minutos/dia × qtd dias; OCORRENCIAS/HORAS_COM_INTERVALO: só conta dias), `meta_semanal_em_milissegundos` (`Regime.java:401-414`, réplica da conversão forçada pra HORAS mesmo em regime OCORRENCIAS — portado por paridade com o legado, mas **sem consumidor real identificado ainda**; se a 16.4 precisar de paridade de payload em milissegundos, já está pronto, senão pode ficar morto), `meta_semanal_formatada` (`Regime.java:375-388`, formata o total calculado: "40h" sem decimal, "7,5h" com decimal, só o número sem "h" em OCORRENCIAS), `periodos_por_dia_da_semana` (`Regime.java:153-172`+`176-208`, agrupa o `expediente` num Hash `wday (0=domingo..6=sábado, `Date#wday` nativo, mapeamento direto sem o +1 do `Calendar.DAY_OF_WEEK` do Java) => [períodos]`) e os helpers privados `dias_do_periodo`/`quantidade_de_dias`/`meta_diaria_em_minutos` (`DiaSemanaEnum.java:115-118`, split by vírgula descartando strings vazias — confirmado necessário pois o dado real do `expediente` termina em vírgula sobrando, ex: `"SEG,TER,QUA,QUI,SEX,"`). **Decisão sobre `getResumo()` vs. `resumo_exibicao`**: não duplicado — `resumo_exibicao` (implementada em sessão anterior, com teste próprio) é só formatação de exibição pra tela `admin/regimes` ("dias de X às Y", 1 item por período bruto do `expediente`); o "resumo dinâmico" desta task é conceitualmente diferente — é o **total calculado** da meta semanal (`meta_semanal_formatada`), não uma lista de períodos formatados. Os dois convivem sem conflito, nenhum foi alterado. **Validação contra dado real**: não foi possível rodar `bin/rails runner` contra os 309 regimes reais (task 11.9) nesta sessão — sem conectividade com o Postgres de desenvolvimento (`10.150.110.174`, exige VPN/túnel indisponível neste ambiente). Validação feita por equivalência: o teste `"meta_semanal_em_minutos soma minutos por dia x quantidade de dias, modalidade HORAS, periodo unico"` usa exatamente o cenário do enunciado (SEG-SEX, 07:00-13:00) e confirma 1800 minutos = 30h/semana, batendo com o esperado (6h/dia × 5 dias). Recomenda-se rodar `Regime.where(modalidade: "HORAS").where.not(expediente: []).first.meta_semanal_formatada` manualmente assim que houver acesso ao banco de dev, como checagem adicional (não bloqueante — a lógica é determinística e coberta por teste). 14 testes novos em `test/models/regime_test.rb` (meta semanal HORAS com período único e múltiplos períodos, OCORRENCIAS, HORAS_COM_INTERVALO, expediente vazio, milissegundos, formatação com/sem decimal/OCORRENCIAS/vazio, agrupamento por dia da semana em cenários simples/múltiplos horários no mesmo dia/dias diferentes por período/expediente vazio). `bin/rubocop` sem ofensas em `regime.rb`/`regime_test.rb`. Suíte completa: 410 testes, 1177 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |
| 16.3 | Orquestração por modalidade (estratégias por regime, já cadastrado na Sprint 11) — inclui precedência oficial/diferenciado/temporário | UC-05 | ✅ **Parte 1 — Precedência de `RegimeFrequentador`** (portado de `RegimentoServices.getRegimeFrequentador`, `RegimentoServices.java:75-88`, + `TipoRegimeFrequentadorEnum.java` + `RegimeFrequentador.java:138-146` `contem(Calendar)`): `RegimeFrequentador` ganhou as constantes `OFICIAL`/`DIFERENCIADO`/`TEMPORARIO` (`TIPOS_DISPONIVEIS`, mesmo padrão de `Regime::CATEGORIAS_DISPONIVEIS`) e `PRECEDENCIA = { "OFICIAL" => 0, "DIFERENCIADO" => 1, "TEMPORARIO" => 2 }`, o scope `vigentes_em(data)` (`momento_inicial <= data` e `momento_final` nil ou `>= data`) e o método de classe `RegimeFrequentador.vigente_para(user, data)` (`where(user:).vigentes_em(data).max_by { PRECEDENCIA }` — maior precedência vence). **Decisão de estrutura**: método de classe em `RegimeFrequentador`, não `User#regime_frequentador_vigente` — a regra pertence inteiramente aos dados do próprio model (tipo + período), no mesmo espírito de `RegimentoServices` ser um service dedicado a `RegimeFrequentador`/`Regime` no legado. **`tipo` continua campo livre** (sem `validates :tipo, inclusion:` — mudaria comportamento de cadastro existente sem pedido explícito, fora de escopo); um `tipo` ausente/desconhecido é tratado defensivamente como `PRECEDENCIA_DESCONHECIDA = -1` (menor que `OFICIAL`), não quebra os `RegimeFrequentador`s reais do banco de dev que hoje não têm `tipo` preenchido. **Parte 2 — Orquestração por modalidade**: Strategy pattern em `app/services/calculo_diario_service.rb` (orquestrador, `.calcular(user, data)`) + `app/services/calculo_diario_service/estrategia_base.rb` (`EstrategiaBase`, calcula a meta do dia em comum a partir de `Regime#periodos_por_dia_da_semana`, já portado na 16.2) + 3 subclasses, uma por modalidade — `PrimeiraEntradaUltimaSaida` (HORAS: trabalhado = diferença entre primeira e última marcação), `EntradasESaidas` (HORAS_COM_INTERVALO: trabalhado = soma dos pares completos de entrada/saída, mesmo pareamento de `Admin::TimeRecordsController#calcular_trabalhado`), `Ocorrencias` (OCORRENCIAS: presença binária — `getCalculo(presente)` de `OcorrenciasV2.java:56-64`, trabalhado = meta inteira se houve qualquer marcação, `0` caso contrário). Dispatch por `regime.modalidade` replicando `CalculoDiarioServiceV2.java:264-288` (só as classes `v2`, DUV-005). Orquestrador resolve `RegimeFrequentador.vigente_para`; sem regime vigente, grava a mesma mensagem do legado ("Nenhum regime ativo nesta data", `CalculoDiarioServiceV2.java:285`) com meta/trabalhado zerados; com regime, persiste em `CalculoDiario` via `find_or_initialize_by(user:, data:)`, preenchendo `meta_segundos`/`normal_segundos`/`excepcional_segundos` (sempre 0, não há expediente excepcional)/`total_segundos`/`informacao`/`falta` — todos campos já existentes no schema (`calculo_diarios`), nenhuma migration necessária. `falta` é uma versão simplificada de `CalculoStrategyV2#configuraFalta` (`meta > 0 && trabalhado == 0 && data < hoje`), sem a checagem de `aberto`. **Deliberadamente fora de escopo** (documentado em comentário no topo de `calculo_diario_service.rb`): regras históricas de data fixa (banco de horas GCET 2018-2022, carência de 15min desde maio/2017, datas liberadas de novembro/2022 — `CalculoStrategyV2.java`); banco de horas/acúmulo de saldo entre dias (Sprint 17); expediente excepcional/feriados/abonos (`PresencaExcepcional`, sem conceito equivalente ainda no Frequencia); flags `aberto` (turno em andamento) e `ausencia` (percentual de carga mínima via `Regime::configuracao`, que teria que ser portado à parte) — ficam no default `false`; `falta_a_descontar`/`falta_compensada`/`descontado_em_folha` — pertencem à consolidação mensal (Sprint 17). 31 testes novos (7 em `regime_frequentador_test.rb` — precedência TEMPORARIO > DIFERENCIADO > OFICIAL com sobreposição, fallback pro OFICIAL isolado, `nil` sem regime vigente, respeito a `momento_final`, tipo desconhecido/nil como menor precedência, isolamento por usuário; 18 em `calculo_diario_service_test.rb` — orquestração sem regime/persistência idempotente via `find_or_initialize_by`/dispatch pelas 3 modalidades, e cada estratégia com meta calculada certa, trabalhado certo a partir de `TimeRecord`s reais, dia sem marcação nenhuma e marcação ímpar sobrando). `bin/rubocop` sem ofensas nos arquivos novos/alterados. Suíte completa: 430 testes, 1207 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |
| 16.4 | Testes de paridade com dados reais do legado v2 | PRD §8 | ✅ Conectividade OK com o legado MySQL real (`10.254.3.6`, 12.190.754 linhas em `presenca_calculodiario`, 8.008.374 em `presenca_registrofrequencia`) — Postgres de dev (`10.150.110.174`) seguiu inacessível nesta sessão (mesma instabilidade da 16.2), mas não era necessário aqui: o legado MySQL já tem `presenca_regime`/`hashExpediente` com o mesmo formato de `expediente` jsonb do Frequencia. **Unidade confirmada por inspeção direta**: `meta`/`normal`/`total` em `presenca_calculodiario` estão em SEGUNDOS (regime "08:00-13:00" = 5h = 18000, bate com `meta` real), mesma unidade de `CalculoDiario#meta_segundos`/`#total_segundos` — comparação direta, sem conversão. **Metodologia**: consultas SQL exploratórias filtrando `excepcional = 0`, `informacao` vazia, `aberto = 0`, `ausencia = 0`, modalidade `HORAS`, data recente (2025-09-17, quarta-feira) e — crítico — exatamente 1 `RegimeFrequentador` vigente na data (evita ambiguidade de precedência; candidatos com 2+ regimes vigentes tied em precedência foram descartados). **4 casos reais extraídos** (só `frequentador_id` do legado, sem CPF/nome, em `test/services/calculo_diario_service_paridade_test.rb`): frequentador_id **8218** e **7303** — regime OFICIAL/HORAS 08:00-13:00, 2 marcações reais (ENTRADA/SAIDA) cada, trabalhado bruto abaixo da meta — **bateram exatamente** (17988s e 17694s respectivamente, motor simplificado == legado real). frequentador_id **8106** — mesmo regime, trabalhado bruto (18311s) ACIMA da meta (18000s) — **divergiu**: legado real capa `normal`/`total` na meta (grava 18000), motor simplificado usa a diferença bruta sem cap (mesmo padrão confirmado em outros 2 frequentadores do mesmo dia, 8279 e 6137, não incluídos como teste por redundância) — é exatamente o mecanismo de banco de horas/hora-extra que a nota de escopo da 16.3 já lista como não portado (Sprint 17), documentado no teste com `assert_equal` do valor real produzido (18311s), não do legado. frequentador_id **7698** — mesmo regime, 3 marcações no dia (ENTRADA, SAIDA, e uma 3a "INDEFINIDO"); legado real pareia especificamente ENTRADA↔SAIDA (15233s), enquanto `PrimeiraEntradaUltimaSaida` (16.3) foi deliberadamente especificado como "diferença entre a primeira e a última marcação do dia" sem filtrar por tipo de operação — usa a marcação INDEFINIDO como última, produzindo 19025s — divergência classificada como (a) simplificação já deliberada na 16.3, não bug novo, documentada com o valor real do motor simplificado. **Conclusão sobre fidelidade**: nos casos mais simples (trabalhado abaixo da meta, exatamente 2 marcações entrada/saida), o motor simplificado reproduz o legado real com exatidão; as duas classes de divergência encontradas (cap de banco de horas; pareamento por tipo de operação em vez de 1a/última marcação) são ambas rastreáveis a simplificações já documentadas na 16.3, nenhum bug genuíno encontrado que justificasse correção dentro do escopo desta task. 4 testes novos. `bin/rubocop` limpo em `calculo_diario_service_paridade_test.rb`. Suíte completa: 434 testes, 1215 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |

### Sprint 17 — Consolidação mensal e banco de horas (UC-06)

| Task | Descrição | UC |
|---|---|---|
| 17.1 | Consolidar mês → `RegistroMensalFrequencia` (AG-4), saldo/banco de horas | UC-06 | ✅ Portado de `RegistroMensalFrequencia.java` (bean) + `RegistroMensalFrequenciaServices.java` (`calcular`/`aplicarCalculosDiarios`/`calcularSaldoAcumulo`, Intranet legada). **Migration `CreateRegistroMensalFrequencias`**: `belongs_to :user`, `ano`/`mes`, `data_inicio`/`data_fim`, `meta_mensal`/`meta_mensal_dias`/`meta_atual`/`meta_atual_dias` (segundos/contagem), `trabalhado`/`trabalhado_normal`/`trabalhado_dias`, `dias_em_aberto`, `faltas`, `saldo_liquido`/`retido`/`acumulado`/`retificado` (segundos, podem ser negativos), `finalizado` (boolean, default `false`) — índice único em `(user_id, ano, mes)`. Campos do bean legado deliberadamente **não portados**: `trabalhadoExcepcional` (sem uso real — `CalculoDiario#excepcional_segundos` é sempre 0, não há expediente excepcional implementado), `tempoAusenteDeFalta`/`ausentes`/`faltasACompensar`/`faltasADescontar`/`creditoADevolver` (algoritmo de desconto em folha, fora de escopo), `momentoUltimoCalculo` (auditoria, sem consumidor). **Efeito colateral necessário**: `config/initializers/inflections.rb` ganhou `inflect.irregular "frequencia", "frequencias"` — mesmo problema já documentado para `"categoria"` (Sprint 11): palavras terminadas em "-ia" o Rails recusa pluralizar de novo, quebrando a inferência de tabela do model a partir do nome da classe. **Service `ConsolidacaoMensalService.consolidar(user, ano, mes, hoje: Date.current)`** (não um método de classe no model, mesmo espírito isolado de `CalculoDiarioService`): `find_or_initialize_by(user:, ano:, mes:)`, zera os campos calculados (paridade com `zerarRegistro`), agrega os `CalculoDiario` do mês (`aplicar_calculos_diarios`, paridade com `aplicarCalculosDiarios` linhas 106-152) e calcula saldo/banco de horas (`calcular_saldo_acumulo`, paridade com `calcularSaldoAcumulo` linhas 84-104). **`pode_calcular?`** (paridade com `isPodeCalcular`, linhas 123-132): portado como comparação direta de `Date` (`calculo.data < hoje || (calculo.data == hoje && !aberto && total != 0)`) — equivalente à comparação ano+dia-do-ano do legado, mais simples em Ruby por já ter `Date#<`/`#==` nativos. Confirmado por teste que **`meta_mensal_dias` só incrementa dentro do mesmo bloco `pode_calcular?`** que `meta_atual_dias` (fidelidade ao legado, apesar do nome sugerir "todos os dias do mês" — só `meta_mensal`, em segundos, soma sempre, mesmo dias futuros). **Saldo do mês anterior**: usa a versão do SERVICE (`getSaldoMesAnterior`, linhas 154-162 — `reg.getAcumulado()` direto), não a versão homônima do bean (linha 344, que soma retido/retificado de novo) — é a que `calcular()` realmente usa no legado; `0` quando não existe registro do mês anterior. **Banco de horas**: limite = `regime.limite_credito * 3600` se `saldo_liquido >= 0`, senão `-regime.limite_debito * 3600` (confirmado: os campos do Frequencia já estão em HORAS, mesmo padrão do fator `* 3600` do legado); excedente vira `retido`, `acumulado = saldo_liquido - retido` (nunca ultrapassa o limite, positivo ou negativo). Regime resolvido via `RegimeFrequentador.vigente_para(user, data_fim)` (mesmo padrão da 16.3); sem regime vigente no fim do mês, saldo/banco de horas ficam zerados (só a agregação de trabalhado/meta roda). **Deliberadamente fora de escopo** (documentado no topo do service): `RetificadorDeBancoHoras` não existe no Frequencia — `retificado` sempre `0` (task futura, Sprint 18 task 18.2, não criado aqui — **resolvido na task 18.2**, `retificado` agora soma os `RetificadorBancoHoras` ativos do mês); congelamento real de `finalizado` (task 17.2 — esta service sempre recalcula, mesmo com `finalizado = true`); algoritmo de desconto em folha. **Validação com dados reais**: não foi possível conectar ao Postgres de dev (`10.150.110.174`, timeout de conexão nesta sessão — mesma instabilidade documentada nas tasks 16.2/16.4) para validar contra os 309 regimes reais; validação feita só contra o banco de test local (fixtures/dados criados nos testes), determinística e coberta por teste, não bloqueante. 14 testes novos (`registro_mensal_frequencia_test.rb`: validações de model, unicidade `user+ano+mes`; `consolidacao_mensal_service_test.rb`: agregação simples de trabalhado/meta_mensal/faltas/trabalhado_dias em 3 dias, criação vs. reaproveitamento em recálculo, `meta_atual` vs. `meta_mensal` divergindo com dia futuro no mês usando `hoje:` injetado — mesmo padrão `travel_to`/data fixa já usado em outros testes do projeto, dia de hoje aberto vs. fechado com trabalho, saldo líquido sem exceder limite, saldo excedendo limite de crédito, saldo excedendo limite de débito, saldo herdado do mês anterior via `acumulado`, saldo `0` sem registro anterior, sem regime vigente não calcula saldo). `bin/rubocop` sem ofensas nos arquivos novos/alterados. Suíte completa: 448 testes, 1258 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |
| 17.2 | Mecanismo real de congelamento (`finalizado`) — corrigir bug do legado (nunca efetivado, DUV-010) | PRD §8 | ✅ **Causa raiz confirmada** (grep exaustivo em todo o código-fonte do legado, `intranet/`): `setFinalizado(true)` **nunca é chamado em lugar nenhum** — o campo nasce `false` no construtor do bean (`RegistroMensalFrequencia.java`) e nunca muda. A checagem em `CalculoDiarioServiceV2.java:69` (`if (rmf == null || !rmf.isFinalizado()) { ...recalcula... }`) em si está correta, mas como `finalizado` nunca vira `true`, a trava de "mês fechado, não recalcula mais" nunca funcionou na prática — todo mês era recalculável pra sempre, mesmo depois do fechamento pretendido (o comentário do próprio bean já descrevia a intenção: "apos rodar o algoritmo de desconto em folha definir todos os registros mensais como finalizado = true... modificacoes passadas devem ser feitas utilizando retificadores"). **Correção aplicada em `ConsolidacaoMensalService.consolidar`**: antes de zerar/reagregar, verifica `registro.persisted? && registro.finalizado?` — se verdadeiro, levanta `ConsolidacaoMensalService::MesFinalizadoError` (classe de erro específica, não `RuntimeError` genérico), sem tocar em nenhum campo calculado do registro existente. **Decisão erro vs. retorno silencioso**: optado por levantar erro explícito em vez de devolver o registro existente sem avisar — uma API que falha silenciosamente voltando um valor desatualizado sem sinalizar seria pior que um erro claro, já que o chamador pode estar assumindo que os campos foram recalculados (documentado no comentário de topo do service). **Ação de fechar um mês**: como o Frequencia não tem (e não deveria inventar aqui) o "algoritmo de desconto em folha" do legado como gatilho automático, implementado como ação explícita/manual — `RegistroMensalFrequencia#finalizar!` (`update!(finalizado: true)`) e ponto de entrada `ConsolidacaoMensalService.finalizar(user, ano, mes)` (exige registro já existente — `find_by!`, levanta `ActiveRecord::RecordNotFound` se o mês nunca foi consolidado), mesmo padrão de `.consolidar` como ponto de entrada do service. Gatilho de UI fica para Sprint 18 (relatório final), fora de escopo aqui — nenhuma tela admin nova criada. **Reabertura**: decisão consciente de implementar `RegistroMensalFrequencia#reabrir!` (`update!(finalizado: false)`) — útil como correção administrativa antes do retificador existir (task 18.2); depois que o retificador existir, reabrir deixa de ser o caminho recomendado (o comentário do bean legado já orienta usar retificadores para modificações passadas), mas documentado como via de escape válida até lá. **Retificador**: não implementado, confirmado fora de escopo (task 18.2, `retificado` continua sempre `0`). 9 testes novos (3 em `registro_mensal_frequencia_test.rb`: nasce `finalizado = false`, `finalizar!` persiste `true`, `reabrir!` persiste `false`; 6 em `consolidacao_mensal_service_test.rb`: recalcula normalmente quando não finalizado, recusa recalcular levantando `MesFinalizadoError` sem alterar campos calculados existentes quando finalizado, `.finalizar` marca `true` num registro existente, `.finalizar` levanta `ActiveRecord::RecordNotFound` para mês nunca consolidado, `reabrir!` permite recalcular de novo, mês nunca calculado continua criável/calculável normalmente com a trava presente). `bin/rubocop` sem ofensas nos 4 arquivos tocados/criados. Suíte completa: 457 testes, 1276 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |
| 17.3 | Atualizar telas da Fase A (`frequencia`, `frequencia_por_orgao`, `dashboard`) para refletir dados calculados, não mais contagem simples | — | ✅ **`admin/frequencia_por_orgao`** (`Admin::FrequenciaPorOrgaoController#linha_do_orgao`): coluna "Trabalhado" trocou de `nil` hardcoded pra soma real de `CalculoDiario#total_segundos` dos usuários do órgão no período filtrado (mês/ano, mesmo filtro já usado nas outras colunas) via `trabalhado_calculado` — formatado como `HH:MM:SS` reaproveitando `formatar_duracao` (extraído de `Admin::TimeRecordsController` pro novo concern `app/controllers/concerns/duracao_formatavel.rb`, `DuracaoFormatavel`, incluído nos dois controllers — evita duplicar o formato). Sem nenhum `CalculoDiario` no período (ninguém rodou `CalculoDiarioService.calcular` ainda pros usuários daquele órgão), continua "—" — mesma disciplina de não inventar dado ausente, confirmado por teste que bate exatamente nisso mesmo com batidas brutas completas (entrada+saída) presentes, provando que a tela não infere cálculo a partir de `TimeRecord`. **`admin/dashboard`**: adicionado 1 KPI novo — "Faltas no Mês" (`@faltas_mes`), soma de `RegistroMensalFrequencia#faltas` do mês corrente via `ConsolidacaoMensalService.consolidar`/`.finalizar` (Sprint 17). Deliberadamente só 1 KPI novo, não um redesenho — os 5 KPIs existentes (task 15.1/15.2) continuam sendo contagem direta porque nenhum de fato dependia de "Fase B"; "Faltas no Mês" é o primeiro que só existe graças ao motor de cálculo, prova que o dashboard *pode* refletir dado calculado agora sem forçar mudança nos demais. Sem nenhum `RegistroMensalFrequencia` consolidado no mês, soma fica `0` (comportamento natural de `.sum` em relação vazia, sem necessidade de tratamento de "—" como nas telas de tabela). **`admin/frequencia` — decisão de NÃO alterar**: avaliado e descartado. É um grid de auditoria de batidas brutas (1 linha por marcação: Frequentador/Data/Operação/Modo/Lotação Época/Estação/Gerência), fundamentalmente diferente de `frequencia_por_orgao` (agregada por órgão+período). Uma coluna "Trabalhado do dia" por linha de batida (i) duplicaria o mesmo valor de `CalculoDiario` em N linhas de um mesmo usuário+dia (uma por marcação), poluindo uma tela cujo propósito declarado é auditar marcações individuais, não agregados; (ii) misturaria conceito de "resultado calculado do dia" com "evento bruto pontual", o que essa tela nunca teve (nem meta, nem trabalhado, nem nada agregado hoje). Concluído que essa tela é, por natureza, de auditoria de marcações e não deveria ganhar dado calculado — nenhuma alteração feita nela, mantida como estava. **Job/gatilho automático de cálculo**: deliberadamente NÃO implementado (fora de escopo explícito desta task) — continua sendo uma pendência real que exige decisão própria de onde/quando disparar `CalculoDiarioService.calcular`/`ConsolidacaoMensalService.consolidar` (a cada sincronização de ponto? job diário? sob demanda no primeiro acesso à tela?), a ser tratada em task futura. Validação manual feita via `bin/rails runner` local (não documentado como automação de produção). 3 testes novos (2 em `frequencia_por_orgao_controller_test.rb`: soma correta de `total_segundos` por órgão/período com filtro de mês excluindo dado fora do período; 1 em `dashboard_controller_test.rb` reforçando o zero sem dado, mais 1 somando faltas de 2 usuários no mês corrente excluindo mês anterior). `bin/rubocop` sem ofensas nos arquivos tocados/criados (as 10 ofensas pré-existentes de `Layout/SpaceInsideArrayLiteralBrackets` em `time_records_controller.rb` são anteriores a esta task, em linhas não tocadas — confirmado via diff). Suíte completa: 460 testes, 1285 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |
| 17.4 | Testes: consolidação, saldo, trava de fechamento efetiva | UC-06 | ✅ **Auditoria de cobertura** (mesmo espírito da 14.5): releitura crítica de `registro_mensal_frequencia_test.rb` (6 testes, 17.1/17.2) e `consolidacao_mensal_service_test.rb` (14 testes, 17.1/17.2) antes de escrever qualquer teste novo. **Cobertura já sólida** para agregação isolada (soma trabalhado/meta/faltas), `meta_atual` vs `meta_mensal` com dia futuro/hoje aberto, saldo simples/excedendo crédito/excedendo débito, herança de `acumulado` do mês anterior (2 meses), e a trava `finalizado`/`MesFinalizadoError` (recusa recalcular, `.finalizar`/`.reabrir!`, `RecordNotFound` para mês nunca consolidado). **3 gaps reais identificados e cobertos** (2 testes novos em `consolidacao_mensal_service_test.rb`, sem duplicar nada existente): (1) **fluxo ponta a ponta ausente** — todos os testes de 17.1/17.2 criam `CalculoDiario` direto via `create!`, nunca passando pelo `CalculoDiarioService.calcular` real (Sprint 16); um teste novo bate `TimeRecord`s reais, roda `CalculoDiarioService.calcular` dia a dia (`travel_to` necessário porque `CalculoDiarioService#falta?` compara com `Date.current` real, não recebe `hoje:` injetado como `ConsolidacaoMensalService` — achado à parte da auditoria, documentado no teste, não é bug pois cada service tem seu próprio contrato de "hoje", só uma armadilha de comparar objetos com `hoje:` injetado só de um lado), consolida o mês e confirma que meta/trabalhado/faltas/saldo batem exatamente com o esperado — prova que os dois services (16.3 + 17.1) se encaixam de verdade, não só isoladamente. (2) **trava "efetiva" testada só por 1 campo** — o teste existente da 17.2 só reafirma `trabalhado` inalterado após tentativa de recálculo bloqueada; o teste ponta a ponta novo tira um snapshot completo de `registro.attributes` (exceto `updated_at`) antes da tentativa de recálculo bloqueada e confirma snapshot idêntico depois, cobrindo todos os campos calculados de uma vez, não só um. (3) **saldo em cadeia de 3+ meses cruzando o limite não coberto** — o teste existente de herança cobre só 2 meses, nenhum deles excedendo o limite; teste novo consolida 3 meses seguidos (mês 1 dentro do limite, mês 2 excede o limite de crédito e reté o excedente, mês 3 parte do `acumulado` já travado no limite e confirma que não há retenção adicional quando o saldo iguala exatamente o limite, provando a borda `> limite` vs `>= limite`) — prova que o encadeamento "acumulado vira saldo do mês anterior" sobrevive ao cruzamento do banco de horas no meio da sequência, não só em um salto isolado. Nenhum outro gap crítico encontrado — os campos deliberadamente fora de escopo (`retificado`, desconto em folha) continuam sem teste porque continuam sem implementação (Sprint 18). 2 testes novos (`consolidacao_mensal_service_test.rb`). `bin/rubocop` sem ofensas no arquivo tocado. Suíte completa: 462 testes, 1304 assertions, 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. **Sprint 17 concluída (4/4) — ressalva explícita**: assim como a Sprint 16, a conclusão da Sprint 17 NÃO significa fidelidade total ao motor de cálculo mensal do legado — seguem fora de escopo/não portados: banco de horas GCET histórico (2018-2022), carência de 15min (desde maio/2017), expediente excepcional/feriados/abonos, retificadores de banco de horas (`retificado` sempre `0`, Sprint 18 task 18.2), algoritmo de desconto em folha; e, reforçando a nota já registrada na 17.3, **não existe job/gatilho automático** populando `CalculoDiario`/`RegistroMensalFrequencia` — todo o pipeline (`CalculoDiarioService.calcular` → `ConsolidacaoMensalService.consolidar`/`.finalizar`) continua manual/sob demanda, uma pendência real a ser tratada em task futura |
| 17.5 | **Job automático pra rodar o pipeline de cálculo** (`CalculoDiarioService.calcular` → `ConsolidacaoMensalService.consolidar`) — débito técnico registrado ao fechar a 17.4, decisão explícita do usuário (2026-09-03): "tem que ter esse job automático". Mesmo espírito dos jobs diários já usados pra espelhar dados do Pessoas (Sprints 10/12: `config/recurring.yml`, Solid Queue) | UC-06 | ✅ **`app/jobs/calcular_frequencia_job.rb`** (`CalcularFrequenciaJob`), seguindo exatamente o mesmo padrão já estabelecido em `ImportarDadosPessoaJob`/`SincronizarAfastamentosJob`: advisory lock via `pg_try_advisory_lock`/`pg_advisory_unlock` com **`LOCK_KEY = 451_926_073`** (distinta das outras 3 já em uso: `837_462_915` de `ImportarDadosPessoaJob`, `592_017_384` de `ImportarServidoresUnidadeJob`, `194_773_608` de `SincronizarAfastamentosJob`), itera `User.where.not(cpf: nil).find_each`, `rescue StandardError` por usuário sem derrubar o job inteiro, log de erro por usuário. **As 4 decisões que estavam em aberto, implementadas conforme já definido**: (a) **frequência** — diário, `config/recurring.yml` (ambiente `production`) às **00:10**, depois de `importar_dados_pessoa` (meia-noite) e `sincronizar_afastamentos` (00:05), mesma lógica de sequência não-paralela (comentário no YAML explica o motivo: dados do Pessoas já sincronizados antes de calcular); (b) **escopo por execução** — pra cada `User` com `cpf`, recalcula `CalculoDiario` via `CalculoDiarioService.calcular(user, data)` pros **últimos 3 dias** (`DIAS_RECALCULADOS = 3`: hoje, ontem, anteontem — não o mês inteiro), idempotente via `find_or_initialize_by` já existente no service; (c) **consolidação mensal** — depois dos 3 dias, roda `ConsolidacaoMensalService.consolidar(user, ano, mes)` pro **mês corrente** (`Date.current.year`/`.month`) de cada usuário, também idempotente; (d) **tratamento de `MesFinalizadoError`** — `rescue ConsolidacaoMensalService::MesFinalizadoError` isolado *antes* do `rescue StandardError` genérico, corpo vazio (comentário explica: mês já finalizado pela trava da 17.2 é esperado, não é erro, não loga, só segue pro próximo usuário) — erros genuínos continuam sendo logados por usuário sem interromper o job. **5 testes novos** (`test/jobs/calcular_frequencia_job_test.rb`): roda sem erro pra usuário sem nenhum `TimeRecord` (ainda assim cria os 3 `CalculoDiario`, via `aplicar_sem_regime`/regime ausente); recalcula os 3 dias corretamente e consolida o mês corrente pra um usuário com marcações reais (`travel_to` + `TimeRecord`s reais, `RegimeFrequentador` vinculado); pula silenciosamente um usuário com mês já finalizado (via `ConsolidacaoMensalService.finalizar`) sem lançar exceção e continua processando o usuário seguinte normalmente; erro genérico em 1 usuário não impede o processamento dos demais; lock de concorrência (2ª execução simultânea não processa nada). **Armadilha de teste encontrada e documentada**: `define_singleton_method` seguido de `remove_method` num método real (`def self.calcular`) apaga o método em vez de restaurar a definição original — corrigido com um helper (`com_calcular_stub`) que salva o `Method` original via `.method(:calcular)` e o restaura explicitamente no `ensure`, em vez do padrão ingênuo usado inicialmente (que causava falha intermitente dependente da ordem dos testes). `bin/rubocop` sem ofensas nos arquivos criados/tocados (`calcular_frequencia_job.rb`, `calcular_frequencia_job_test.rb`; `recurring.yml` não é Ruby, rubocop não o analisa). Suíte completa: **467 testes, 1319 assertions** (462→467, 1304→1319), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. **Sprint 17 agora 5/5 concluída**, débito técnico da 17.4/17.5 quitado |

### Sprint 18 — Relatório final e retificador (UC-07, UC-12)

| Task | Descrição | UC |
|---|---|---|
| 18.1 | Gerar/atualizar `RelatorioFrequenciaFinal` (AG-5) | UC-07 | ✅ Portado de `RelatorioFrequenciaFinal.java` (bean pai) + `RelatorioFrequentador.java` (bean filho) + `RelatorioFrequenciaFinalServices.java` (`montarRelatorio`/`atualizarRelatorio`/`criarRelatorioFrequentador`, Intranet legada). **Migration `CreateRelatorioFrequenciaFinais`**: `ano`/`mes` (inteiros, mesma convenção de `RegistroMensalFrequencia`), `data_geracao` (not null), `data_alteracao` (nullable, só preenchida na 2ª geração em diante) — índice único em `(ano, mes)`. **Migration `CreateRelatorioFrequentadores`**: `belongs_to :user`, `belongs_to :relatorio_frequencia_final`, `saldo_bruto`/`valor_retroativo`/`resultado` (inteiros, default 0). **Sem campo `orgao`**: confirmado no bean legado que `private Orgao orgao` está comentado (`//private Orgao orgao`) — mesmo no legado real o relatório nunca foi filtrado por órgão, é único por mês/ano; documentado no comentário do model `RelatorioFrequenciaFinal` pra não reintroduzir esse filtro por engano numa task futura. **Efeito colateral de migration necessário**: o inflector do Rails pluraliza "final" como "finals" (regra do inglês) em vez de "finais" — a tabela foi criada como `relatorio_frequencia_finais` (português correto) e o model recebeu `self.table_name = "relatorio_frequencia_finais"` explícito pra não depender de inflection custom (mesma classe de problema já visto com `"categoria"`/`"frequencia"` nas Sprints 11/17, mas resolvido aqui via `table_name` direto no model em vez de mexer em `config/initializers/inflections.rb`, mais localizado). A `t.references :relatorio_frequencia_final` também precisou de `foreign_key: { to_table: :relatorio_frequencia_finais }` explícito pelo mesmo motivo (senão a migration tentava criar a FK contra a tabela errada, `relatorio_frequencia_finals`, inexistente — migration falhava com `PG::UndefinedTable`). **Model `RegistroMensalFrequencia` ganhou `#saldo_bruto`** (`trabalhado - meta_mensal`): extraído do cálculo inline que já existia dentro de `ConsolidacaoMensalService#calcular_saldo_acumulo` (mesma fórmula, Sprint 17) pra ser reutilizável por `RelatorioFrequenciaFinalService` sem duplicar a conta nem recalcular a consolidação inteira. **Decisão sobre `valor_retroativo`/`ValorRetroativo`**: `ValorRetroativo` não existe no Frequencia (conceito de correção retroativa de folha, fora do que foi construído nas Sprints 16/17) — **não criado nesta task**, conforme escopo explícito. `valor_retroativo` fica sempre `0`, mesmo padrão já usado em `retificado` (`RegistroMensalFrequencia`, Sprint 17). Com `valor_retroativo = 0`, a fórmula original do `resultado` (`saldo_bruto >= 0 ? valor_retroativo : saldo_bruto + valor_retroativo`) simplifica pra `saldo_bruto >= 0 ? 0 : saldo_bruto` — implementado exatamente assim em `RelatorioFrequenciaFinalService#criar_relatorio_frequentador`. **DUV-011 documentado, não replicado**: comentário no topo do service explica o bug `cont=+valor.getNumeroHora()` (linha 142 de `RelatorioFrequenciaFinalServices.java` — atribuição do valor positivo, não soma acumulada, só o último valor do loop sobrevive) como aviso pra quem for implementar `ValorRetroativo`/`calcularValorRetroativo` na task 18.3 futura; como esta task não implementa esse cálculo, o bug não se manifesta aqui. **Geração idempotente** (`RelatorioFrequenciaFinalService.gerar(ano, mes, users:)`): busca `RelatorioFrequenciaFinal.find_by(ano:, mes:)` — se existe, atualiza (`data_alteracao = agora`, destrói e recria os `RelatorioFrequentador` filhos do zero, dentro de uma transação); se não existe, cria (`data_geracao = agora`). Pra cada usuário, lê `RegistroMensalFrequencia.find_by(user:, ano:, mes:)` sem criar nada (`saldo_bruto = 0` se não existir, mesmo comportamento do `registroMensal != null ? ... : 0` do legado). 10 testes novos (`relatorio_frequencia_final_test.rb`: validações de model, unicidade `ano+mes`; `relatorio_frequencia_final_service_test.rb`: geração cria relatório + 1 `RelatorioFrequentador` por usuário passado, gerar de novo pro mesmo mês/ano atualiza em vez de duplicar, `resultado = 0` com saldo positivo, `resultado = 0` com saldo zero, `resultado` igual ao saldo negativo, usuário sem `RegistroMensalFrequencia` entra com `saldo_bruto`/`resultado = 0`, índice único `ano+mes` impede duplicata). `bin/rubocop` sem ofensas nos 8 arquivos tocados/criados (2 migrations, 3 models, 1 service, 2 testes). Suíte completa: **478 testes, 1344 assertions** (467→478, 1319→1344), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. **Pendência retroativa resolvida**: `valor_retroativo` ficava sempre `0` "não criado nesta task" — resolvido na task 18.3, `ValorRetroativo` agora existe e `valor_retroativo` reflete a soma real dos valores retroativos do mês |
| 18.2 | Retificador de banco de horas | UC-12 | ✅ Portado de `RetificadorDeBancoHoras.java` (bean) + `TipoRetificadorEnum.java` (Intranet legada). **Migration `CreateRetificadorBancoHoras`**: `belongs_to :user` (frequentador afetado), `belongs_to :responsavel, class_name: "User", optional: true` (quem registrou — opcional, decisão documentada no model: não há tela admin nesta task, nem conceito fácil de "usuário logado" pra amarrar aqui; a tela futura pode tornar obrigatório na camada de formulário sem quebrar o schema), `ano`/`mes` (inteiros), `tipo` (string), `segundos_a_retificar` (int), `observacao`/`informacao` (texto livre), `excluido` (boolean, default `false`, soft-delete) — índice em `(user_id, ano, mes)`, sem unicidade (um frequentador pode ter vários retificadores no mesmo mês, inclusive do mesmo tipo, conforme o próprio comentário do bean legado). Tabela `retificador_banco_horas` pluraliza corretamente sem precisar de inflection custom (confirmado via `ActiveSupport::Inflector.pluralize`/`tableize` antes de assumir, mesma cautela das Sprints 11/17/18.1). **`momento_registro` não portado como campo separado**: decisão documentada no model — o bean legado só tinha esse campo porque o JPA da época não expunha `created_at` automaticamente; no Rails, `created_at` (timestamps padrão) já cobre o mesmo propósito, duplicar seria redundância sem ganho. **Campo `calculoDiario` do bean legado não portado**: confirmado por grep que nenhum service/DAO do legado realmente usa esse vínculo (só existe `findByCalculoDiario`, sem chamador real) — sem uso a portar. **Model `RetificadorBancoHoras`**: os 7 tipos reais de `TipoRetificadorEnum.java` como constantes (`TIPOS_DISPONIVEIS`, mesmo padrão de `RegimeFrequentador::TIPOS_DISPONIVEIS`, Sprint 16) — `CREDITO_POR_DESCONTO_EM_FOLHA`, `CREDITO_POR_TRABALHO_EXCEPCIONAL`, `CREDITO_POR_DEBITO_INDEVIDO`, `CREDITO_POR_DEBITO_EM_OUTRO_MES` (fator +1, crédito) e `DEBITO_POR_CREDITO_INDEVIDO`, `DEBITO_POR_CREDITO_EM_OUTRO_MES`, `DEBITO_PARA_COMPENSACAO` (fator -1, débito), com `TIPOS_LABELS` (rótulos em português já traduzidos do enum legado) e `FATOR_MULTIPLICACAO` (hash tipo→+1/-1, expostos via `#fator_multiplicacao`). `#excluir!` (`update!(excluido: true)`) — soft-delete, mesmo espírito de `RegistroMensalFrequencia#finalizar!`/`#reabrir!`, nunca `destroy` real (preserva auditoria). **`ConsolidacaoMensalService`**: novo método privado `soma_retificadores` (`RetificadorBancoHoras.where(user:, ano:, mes:, excluido: false).sum { seg * fator }`) substitui o `retificado = 0` hardcoded da task 17.1 no fluxo de `.consolidar`; usa `Enumerable#sum` com bloco deliberadamente (não `+=` em laço) para evitar por construção a classe de bug do legado DUV-011 (task 18.3), reforçado por teste explícito com 3 retificadores no mesmo mês provando soma completa, não só o último valor. **Decisão de design real — retificar mês FINALIZADO**: o comentário do bean legado já descrevia a intenção original ("modificacoes passadas devem ser feitas utilizando retificadores"), ou seja, o retificador é o caminho pretendido pra corrigir um mês já fechado sem reabri-lo; mas `.consolidar` continua bloqueado por `MesFinalizadoError` (task 17.2) porque ele reagrega os `CalculoDiario` do zero, o que destruiria o congelamento de `trabalhado`/`meta_mensal`/etc. que `finalizar!` deveria garantir. Optado por criar um ponto de entrada SEPARADO, `ConsolidacaoMensalService.aplicar_retificador(user, ano, mes)`: reaplica só a soma de retificadores e recalcula `retificado`/`saldo_liquido`/`retido`/`acumulado` (via `calcular_saldo_acumulo`, reaproveitado sem alteração), preservando os totais de trabalhado/meta/faltas já persistidos — funciona mesmo com `finalizado? == true`, sem passar pela trava do `.consolidar` (não a burla: é exatamente o caminho que a trava deveria deixar passar). Exige registro já existente (`find_by!`, `RecordNotFound` se o mês nunca foi consolidado, mesmo padrão de `.finalizar`). 16 testes novos (8 em `retificador_banco_horas_test.rb`: validações de model, `responsavel` opcional, `tipo` fora de `TIPOS_DISPONIVEIS` inválido, `fator_multiplicacao` +1 nos 4 tipos de crédito e -1 nos 3 de débito, `excluir!` marca `excluido = true` sem apagar o registro; 8 em `consolidacao_mensal_service_test.rb`: retificador de crédito soma no `retificado`/saldo líquido, retificador de débito subtrai, múltiplos retificadores no mesmo mês somam corretamente — não só o último valor, prova contra DUV-011 —, retificador excluído não entra na soma, retificador de outro mês/ano não entra na soma, `aplicar_retificador` recalcula retificado/saldo de um mês finalizado sem reagregar `CalculoDiario` (trabalhado permanece congelado mesmo com novo `CalculoDiario` lançado depois do fechamento), `aplicar_retificador` levanta `RecordNotFound` pra mês nunca consolidado). `bin/rubocop` sem ofensas nos 5 arquivos tocados/criados (1 migration, 1 model, 1 service, 2 testes). Suíte completa: **492 testes, 1370 assertions** (478→492, 1344→1370), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. **Pendência retroativa resolvida**: a nota da task 17.1 dizia que `retificado` ficaria sempre `0` "task futura, Sprint 18 task 18.2, não criado aqui" — resolvido nesta task, `retificado` agora reflete a soma real dos retificadores ativos do mês |
| 18.3 | Corrigir bug do legado `cont=+valor` → `cont+=valor` (DUV-011, não replicar) | PRD §8 | ✅ Portado de `ValorRetroativo.java` (bean) + `ValorRetroativoDao.java` (Intranet legada). **O bug real** (`calcularValorRetroativo`, `RelatorioFrequenciaFinalServices.java`, linhas 128-149): `cont=+valor.getNumeroHora();` dentro do loop `for (ValorRetroativo valor : valores)` é interpretado pelo Java como `cont = (+valor.getNumeroHora())` — atribuição do valor unário positivo — e não `cont += valor.getNumeroHora()` (soma acumulada). Resultado real do legado: quando há 2+ `ValorRetroativo` pro mesmo frequentador/mês/ano, só o ÚLTIMO valor do loop que bate o filtro de mês sobrevive; os anteriores são descartados silenciosamente, sem erro nem log. **Migration `CreateValorRetroativos`**: `belongs_to :user`, `ano`/`mes` (inteiros), `data_geracao` (datetime, not null), `processo` (string, referência administrativa/judicial), `numero_hora` (int, not null) — índice em `(user_id, ano, mes)`, **sem unicidade**. **Decisão sobre `@OneToOne` do bean legado não replicado**: o bean legado usa `@OneToOne` entre `Frequentador` e `ValorRetroativo`, o que seria estranho pra um valor mensal (um usuário só poderia ter UM valor retroativo em toda a vida). Confirmado no próprio `ValorRetroativoDao#getByMesAno` que o legado já lida com `List<ValorRetroativo>` de tamanho > 1 pro mesmo mês/ano — exatamente o cenário do bug DUV-011 — ou seja, na prática o legado já não respeitava sua própria restrição `@OneToOne` (modelagem ruim do bean, não limitação de negócio real). Aqui, `belongs_to :user` simples permite múltiplos valores retroativos por usuário, inclusive vários no mesmo mês/ano. **Correção do bug** (`ValorRetroativo.soma_do_mes(user, ano, mes)`, método de classe): `where(user:, ano:, mes:).sum(:numero_hora)` — soma real via `.sum` do ActiveRecord, sem a armadilha de sintaxe do Java. **Filtro redundante não replicado**: o `mesDoValor == mes` dentro do loop Java é desnecessário (o `ValorRetroativoDao#getByMesAno` legado já filtra por `frequentador+mes+ano` na própria query, mesmo comportamento do `where(user:, ano:, mes:)` aqui). **Wire no `RelatorioFrequenciaFinalService`**: `valor_retroativo` trocado de fixo `0` pra `ValorRetroativo.soma_do_mes(user, @ano, @mes)`; fórmula do `resultado` (`RelatorioFrequenciaFinalService#criar_relatorio_frequentador`) restaurada pra versão completa do legado — `saldo_bruto >= 0 ? valor_retroativo : saldo_bruto + valor_retroativo` (não mais o `saldo_bruto >= 0 ? 0 : saldo_bruto` simplificado da task 18.1, que só valia enquanto `valor_retroativo` era sempre `0`). 11 testes novos (8 em `valor_retroativo_test.rb`: validações de model, múltiplos valores no mesmo mês/ano válidos — prova que não há restrição `OneToOne` —, **`soma_do_mes` soma todos os 3 valores de um cenário [3, 5, 8] = 16, não fica só com o último [8]** — teste principal da task, prova direta contra DUV-011 —, `soma_do_mes` retorna `0` sem valores no mês, `soma_do_mes` não inclui valores de outro mês/ano, `soma_do_mes` não inclui valores de outro usuário; 3 em `relatorio_frequencia_final_service_test.rb`: `resultado` igual à soma de 2 valores retroativos [5, 7] = 12 quando saldo bruto positivo — não o último valor isolado —, `resultado` soma saldo bruto negativo com a soma de 3 valores retroativos [3, 4, 5] = 12). `bin/rubocop` sem ofensas nos 5 arquivos tocados/criados (1 migration, 1 model, 1 service, 2 testes). Suíte completa: **503 testes, 1388 assertions** (492→503, 1370→1388), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão |
| 18.4 | Testes: geração idempotente, retificação pós-fechamento | UC-07/12 | ✅ **Auditoria de cobertura** (mesmo espírito da 14.5/17.4): releitura crítica de `relatorio_frequencia_final_test.rb` (4 testes, 18.1), `relatorio_frequencia_final_service_test.rb` (9 testes, 18.1/18.3), `retificador_banco_horas_test.rb` (8 testes, 18.2), `consolidacao_mensal_service_test.rb` — parte de retificadores (8 testes, 18.2) e `valor_retroativo_test.rb` (8 testes, 18.3) antes de escrever qualquer teste novo. **Cobertura já sólida** para validações de model, unicidade `ano+mes`, não-duplicação do relatório pai na regeração, fórmula de `resultado` (positivo/zero/negativo, com/sem `valor_retroativo`), os 7 tipos/fatores de `RetificadorBancoHoras`, soma de retificadores no `retificado` (inclusive prova contra DUV-011 com 3 retificadores), exclusão lógica, filtro por mês/ano/usuário, e `aplicar_retificador` funcionando em mês finalizado preservando `trabalhado`. **3 gaps reais identificados e cobertos** (3 testes novos, sem duplicar nada existente): (1) **"geração idempotente" testada só pela não-duplicação, nunca pela atualização de valor** — nenhum teste existente provava que uma 2ª geração para o mesmo mês/ano reflete dados que mudaram entre a 1ª e a 2ª rodada; teste novo em `relatorio_frequencia_final_service_test.rb` consolida um mês, gera o relatório (1ª rodada, `valor_retroativo = 0`), finaliza o mês, aplica um retificador via `aplicar_retificador` (prova que mexer no mês pós-fechamento não quebra nada) e lança um `ValorRetroativo` novo, depois gera de novo (2ª rodada) — confirma que o relatório pai não duplica (`assert_no_difference`), mas o `RelatorioFrequentador` filho é recriado do zero (`id` diferente) refletindo `valor_retroativo = 6`/`resultado = 6`, não os `0` "congelados" da 1ª rodada, provando que `destroy_all` + recriação (linha 37 do service) realmente reconsulta o estado atual, não reaproveita valores antigos. (2) **"retificação pós-fechamento" testada só por 1 campo (`trabalhado`)** — o teste da 18.2 só reafirmava `trabalhado` inalterado; teste novo em `consolidacao_mensal_service_test.rb` tira um snapshot de TODOS os campos "congelados" (`trabalhado`/`trabalhado_normal`/`trabalhado_dias`/`meta_mensal`/`meta_mensal_dias`/`meta_atual`/`meta_atual_dias`/`faltas`/`dias_em_aberto`) antes de `aplicar_retificador`, lança mais um `CalculoDiario` depois do fechamento e confirma snapshot idêntico depois — provando que só `retificado`/`saldo_liquido`/`retido`/`acumulado` mudam, nada mais, não um recálculo disfarçado. (3) **retificação pós-fechamento não reabre o mês (gap de segurança não coberto)** — nenhum teste confirmava que `ConsolidacaoMensalService.consolidar` continua recusando com `MesFinalizadoError` depois de uma chamada bem-sucedida a `aplicar_retificador`; teste novo finaliza um mês, aplica um retificador, confirma `finalizado?` continua `true`, e confirma que `.consolidar` (recálculo completo) continua sendo recusado — prova que a retificação é cirúrgica (via separada, task 18.2) e não reabre a trava da 17.2 por engano. Nenhum outro gap crítico encontrado — os testes de validação de model e filtros de escopo (mês/ano/usuário/exclusão) já cobrem as bordas relevantes. 3 testes novos (1 em `relatorio_frequencia_final_service_test.rb`, 2 em `consolidacao_mensal_service_test.rb`). `bin/rubocop` sem ofensas nos 2 arquivos tocados. Suíte completa: **506 testes, 1406 assertions** (503→506, 1388→1406), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. **Sprint 18 concluída (4/4) — ressalva explícita**: assim como as Sprints 16/17, a conclusão da Sprint 18 NÃO significa fidelidade total ao motor de cálculo do legado — seguem fora de escopo/não portados: banco de horas GCET histórico (2018-2022), carência de 15min (desde maio/2017), expediente excepcional/feriados/abonos, algoritmo de desconto em folha completo; e, reforçando a nota já registrada nas Sprints 16/17 (task 17.4), **não existe job/gatilho automático** para `RelatorioFrequenciaFinal`/`ValorRetroativo`/`RetificadorBancoHoras` — só o pipeline de cálculo diário/mensal (`CalculoDiarioService`/`ConsolidacaoMensalService`) tem job (task 17.5); geração de relatório final, lançamento de valores retroativos e retificadores continuam ações manuais/sob demanda, pendência real a ser tratada em task futura |

### Sprint 19 — Gestão de exceções (UC-08, UC-09, UC-10, UC-11)

| Task | Descrição | UC |
|---|---|---|
| 19.1 | Batida manual / errata | UC-08 — ✅ **Concluída (2026-09-03)** — portado de `RegistroFrequencia#manualFromForm` (intranet, `presenca/beans/RegistroFrequencia.java:214-246`), que distingue 2 conceitos: **batida manual** (`errata=false`) — admin insere um `TimeRecord` NOVO na hora (`Dao.save` roda ANTES da intervenção, que é só a justificativa documentada); **errata** (`errata=true`) — correção que fica PENDENTE, sem criar o registro (`Dao.save` só roda no branch `!errata`), até resolução futura. Model novo `IntervencaoFrequencia` (`belongs_to :user` frequentador afetado, `belongs_to :responsavel` admin, `tipo` "batida_manual"/"errata", `justificativa` obrigatória, `momento`, `punch_type`, `time_record` opcional, `status` "registrado"/"pendente") + `RegistroManualFrequenciaService.registrar` orquestrando a transação (cria `TimeRecord` com `authentication_mode: "manual"` só pra batida manual; errata nunca cria `TimeRecord`). **Decisão consciente de escopo**: o legado usa um framework genérico de aprovação (`PreVinculadoIntervencao`, com N tipos via `PreVinculadoAcaoEnum`) compartilhado por toda a Sprint 19 (UC-08 a UC-11) — generalizar esse mecanismo inteiro agora arriscaria superdimensionar a task; construído só o necessário pra UC-08 (2 valores de `tipo`, sem workflow de aprovação/rejeição multi-estado — isso é escopo de 19.2 em diante), com estrutura pensada pra ser reaproveitada, não descartada. Nenhuma tela admin nova criada (fora de escopo desta task). Validações: `justificativa`/`momento`/`tipo`/`status` obrigatórios, `time_record` presente exatamente quando `tipo == "batida_manual"` e ausente quando `"errata"` (validação cruzada no model). Corrigido também: `User#intervencoes_frequencia` precisou de `class_name:` explícito (Rails inferiu incorretamente "IntervencoesFrequencia" a partir do plural). 10 testes novos (6 model + 4 service) em `test/models/intervencao_frequencia_test.rb` e `test/services/registro_manual_frequencia_service_test.rb`. Suíte completa: 516 testes, 1438 assertions, 1 falha pré-existente (timezone, não relacionada) — baseline 506/1406 antes desta task |
| 19.2 | Desconsiderar / reconsiderar ponto | UC-09 — ✅ **Concluída (2026-09-03)** — portado de `RegistroFrequencia#desconsiderar`/`#reconsiderar` (intranet, `presenca/beans/RegistroFrequencia.java:300-318` e `340-358`), que distingue: **desconsiderar** — marca um `TimeRecord` já existente como excluído do cálculo (`ressalva = true`, `horario = DESCONSIDERADO` no legado), sem apagar/ocultar o registro (não é soft-delete); exige justificativa; **reconsiderar** — desfaz (`ressalva = false`, `horario = NORMAL`), sem justificativa obrigatória (o legado não recebe `intervencaoObs` neste método, diferente de `desconsiderar`). `desconsiderarPorPredioNaoPermitido` (linhas 320-338) explicitamente **fora de escopo** — motivo semanticamente diferente (prédio não autorizado, não correção manual) e é a task 19.4. **Reaproveitado `IntervencaoFrequencia`** (não criado model novo): `TIPOS` ganhou `"desconsideracao_ponto"`/`"reconsideracao_ponto"`; ambos entram no grupo `TIPOS_COM_TIME_RECORD` (junto com `batida_manual`) porque sempre agem sobre um `TimeRecord` real; `justificativa` passou a ser condicional (`TIPOS_SEM_JUSTIFICATIVA_OBRIGATORIA = %w[reconsideracao_ponto]`) — exigiu 2ª migration tornando a coluna `justificativa` nullable no banco (a 19.1 criou `null: false`, só existiam tipos que sempre exigiam). `TimeRecord` ganhou `desconsiderado`/`ressalva` (booleans, default `false`, migration nova) e os métodos `#desconsiderar!(justificativa:, responsavel:)`/`#reconsiderar!(responsavel:)` (transação: atualiza os campos + cria a `IntervencaoFrequencia` já `status: "registrado"`, efetiva na hora, sem workflow pendente — diferente da "errata" da 19.1). `TimeRecord#intervencao_frequencia` (has_one, 19.1) virou `#intervencoes_frequencia` (has_many) — um mesmo `TimeRecord` agora pode acumular mais de uma intervenção ao longo do tempo (ex.: desconsiderado e depois reconsiderado). **Integração real com o motor de cálculo** (o ponto mais importante da task): `CalculoDiarioService::EstrategiaBase#registros` passou a filtrar `.reject(&:desconsiderado?)` sobre `Dia.para(user, data).registros` — ponto único usado pelas 3 estratégias (`PrimeiraEntradaUltimaSaida`, `EntradasESaidas`, `Ocorrencias`), então a exclusão vale pra todas automaticamente. Decisão de não filtrar em `Dia#registros`: `Dia` continua representando TODAS as batidas reais do dia (registro desconsiderado continua existindo/visível); é o motor de cálculo, não o agregado, quem decide ignorá-lo. Testes provam efeito real (não decorativo): `PrimeiraEntradaUltimaSaida` e `EntradasESaidas` recalculando antes/depois de `desconsiderar!` com resultado diferente; `Ocorrencias` com a única marcação do dia desconsiderada calculando como sem presença; `reconsiderar!` devolvendo o registro ao cálculo. 13 testes novos (6 model `IntervencaoFrequencia`, 4 model `TimeRecord`, 4 `CalculoDiarioService`/estratégias — cobrindo também idempotência de `reconsiderar!` em registro nunca desconsiderado). `bin/rubocop` sem ofensas nos 8 arquivos tocados/criados (2 migrations, 2 models, 1 service/estratégia, 3 testes). Suíte completa: **529 testes, 1473 assertions** (516→529, 1438→1473), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. |
| 19.3 | Autorizar horas extras (deferir/indeferir) | UC-10 — ✅ **Concluída (2026-09-03)** — portado de `RegistroFrequencia#preencheIntervencaoLimitado` (intranet, `presenca/beans/RegistroFrequencia.java:248-266`) + `podeCalcularLimitado`/`isLimitadoValidado`/`isLimitadoIndeferido` (linhas 289-298) + `PreVinculadoAcaoEnum` (`tjpi/enums/PreVinculadoAcaoEnum.java:74-76`). Fluxo real do legado: quando um servidor bate a saída e o cálculo do dia indicaria acúmulo de horas extras além do permitido, o sistema abre um pedido pendente (`ACAO_ACUMULO_DE_HORAS_EXTRAS`) pra um gestor decidir; a resolução vira `ACAO_AUTORIZACAO_ACUMULO_DE_HORAS_EXTRAS` (deferido — horas extras passam a contar) ou `ACAO_INDEFERIMENTO_ACUMULO_DE_HORAS_EXTRAS` (indeferido — ficam limitadas na meta). **Reaproveitado `IntervencaoFrequencia`** (não criado model novo): `TIPOS` ganhou `"acumulo_horas_extras"`, sempre com `time_record` (a marcação de saída que gerou o acúmulo, entra em `TIPOS_COM_TIME_RECORD`), sem `justificativa` obrigatória (entra em `TIPOS_SEM_JUSTIFICATIVA_OBRIGATORIA` — é o sistema que abre o pedido, não uma pessoa se justificando, igual `preencheIntervencaoLimitado` não recebe esse parâmetro) e, inédito nesta task, sem `responsavel` obrigatório (`TIPOS_SEM_RESPONSAVEL_OBRIGATORIO` novo) — o pedido nasce sem responsável humano porque quem o cria é o próprio sistema. Exigiu 2 mudanças de schema (migration `AddResolucaoToIntervencaoFrequencias`): `responsavel_id` passou a aceitar NULL (era `null: false` desde a 19.1, quando só existiam tipos com responsável humano na criação), e novo campo `resolvido_por_id` (nullable, FK pra `users`) — decisão deliberada de NÃO reaproveitar `responsavel_id` pra guardar quem decidiu o pedido, porque são papéis semanticamente diferentes: `responsavel` é "quem é responsável pela ação registrada na criação" (pode ser nil quando é o sistema), `resolvido_por` é "quem decidiu um pedido pendente" (sempre uma pessoa, só preenchido na resolução). `User` ganhou `#intervencoes_frequencia_como_resolvedor` (has_many, mesmo padrão de `#intervencoes_frequencia_como_responsavel`). **Taxonomia final de `status` (4 valores, documentada no topo do model)**: `"registrado"` — ação humana já efetiva na criação, sem workflow (batida_manual, desconsideracao_ponto, reconsideracao_ponto); `"pendente"` — pedido em aberto aguardando decisão (errata — resolução ainda não implementada, task futura; acumulo_horas_extras — resolvido nesta task); `"aprovado"` — pedido pendente deferido; `"indeferido"` — pedido pendente negado. `"aprovado"` não é redundante com `"registrado"`: um só existe depois de passar por `"pendente"`, o outro nunca passa por esse estado. **API**: `IntervencaoFrequencia.solicitar_autorizacao_horas_extras(time_record)` (método de classe, sem `responsavel:`/`justificativa:` — quem cria é o sistema) cria o pedido `status: "pendente"`; `#deferir!(responsavel:)`/`#indeferir!(responsavel:)` (instância) resolvem, validando que a intervenção esteja `status: "pendente"` — caso contrário levantam `IntervencaoFrequencia::ResolucaoInvalidaError` (erro nomeado, não silencioso; cobre tanto "já resolvida" quanto "nunca foi um pedido pendente", ex.: tentar deferir uma "batida_manual"). **Decisão consciente de NÃO integrar com o motor de cálculo**: `CalculoDiarioService` (Sprint 16) nunca implementou o cap diário de banco de horas (`maximoBancoHorasDiarioEmSegundos`/`ajustarSegundosTrabalhados` do legado) — decisão de escopo já tomada na 16.3 e reconfirmada na 16.4 (uma das 2 divergências reais encontradas contra dado do legado foi exatamente trabalhado bruto acima da meta não sendo capado). Sem esse cap, o motor de cálculo não tem o conceito de "acúmulo além do permitido" que dispararia a criação automática de um pedido — construir essa detecção agora reabriria uma decisão de escopo já documentada em 2 tasks anteriores, sem pedido explícito. Esta task entrega só o mecanismo de aprovação em si (dados + `solicitar_autorizacao_horas_extras`/`deferir!`/`indeferir!`), pronto pra ser consumido quando o cap diário de banco de horas for implementado (task futura, ainda não numerada) — nenhuma lógica de detecção automática foi criada em `CalculoDiarioService`. 7 testes novos em `test/models/intervencao_frequencia_test.rb` (solicitação nasce pendente sem responsavel/justificativa; solicitação exige time_record; deferir! marca aprovado + resolvido_por; indeferir! marca indeferido + resolvido_por; deferir! após já resolvido levanta erro; deferir! numa intervenção que nunca foi pendente levanta erro; validação de time_record obrigatório pro tipo). `bin/rubocop` sem ofensas nos 4 arquivos tocados/criados (1 migration, 2 models, 1 teste). Suíte completa: **536 testes, 1489 assertions** (529→536, 1473→1489), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. |
| 19.4 | Autorizar batida em prédio não permitido | UC-11 — ✅ **Concluída (2026-09-03)** — portado de `RegistroFrequencia#desconsiderarPorPredioNaoPermitido` (intranet, `presenca/beans/RegistroFrequencia.java:320-338`) e `#preencheIntervencaoBaterPontoEstacaoNaoPermitida` (linhas 268-286). **Achado importante ao portar**: o código real do legado usa por engano o enum `ACAO_ACUMULO_DE_HORAS_EXTRAS` dentro de `preencheIntervencaoBaterPontoEstacaoNaoPermitida` — bug de copy-paste (a mensagem fala de "bater ponto em prédio não autorizado", mas o enum usado é o de horas extras); os nomes corretos pro conceito, `ACAO_AUTORIZACAO_PERMITIR_BATER_PONTO_OUTRO_PREDIO`/`ACAO_INDEFERIMENTOPERMITIR_BATER_PONTO_OUTRO_PREDIO`, existem em `PreVinculadoAcaoEnum` (`tjpi/enums/PreVinculadoAcaoEnum.java:79-80`) mas nunca são usados pelo código real — **não replicado**: o tipo novo usa o conceito correto, não o bug. **Decisão consciente de escopo — não construído `Predio`**: o conceito de prédio/edifício (quais estações pertencem a quais prédios, quais frequentadores têm permissão em quais prédios) não existe no Frequencia — foi investigado e explicitamente descartado na task 9.6 (`EstacaoPonto` no legado tem `ManyToMany` com `Predio`, não replicado, decisão documentada como "escopo maior, fora daquela task"). Sem `Predio`, não há como o sistema detectar automaticamente "estação não é de um prédio autorizado" — essa detecção fica de fora; os 2 mecanismos abaixo são acionados manualmente (um admin/gestor aciona), não pelo sistema sozinho, até `Predio` existir de verdade numa task futura. **Reaproveitado `IntervencaoFrequencia`** (não criado model novo): `TIPOS` ganhou `"desconsideracao_predio"` (mesmo grupo de `"desconsideracao_ponto"` — `TIPOS_COM_TIME_RECORD`, sempre `"registrado"`, efetivo na hora, sem justificativa obrigatória no model porque é gerada automaticamente) e `"autorizacao_predio"` (mesmo padrão de `"acumulo_horas_extras"` — `TIPOS_SEM_RESPONSAVEL_OBRIGATORIO` e `TIPOS_SEM_JUSTIFICATIVA_OBRIGATORIA`, pedido `"pendente"` resolvido pelos `#deferir!`/`#indeferir!` já existentes, **confirmado sem nenhuma alteração** — a lógica de resolução (`#resolver!`) só depende de `status == "pendente"`, não do `tipo`). **Decisão**: `estacao_ponto` NÃO ganhou `belongs_to` próprio em `IntervencaoFrequencia` — como `time_record` já referencia `estacao_ponto` (Sprint 13.1), basta `intervencao.time_record.estacao_ponto`, evitando duplicar dado; o kwarg `estacao_ponto:` nos métodos novos existe pra deixar a intenção explícita na chamada. **`TimeRecord#desconsiderar_por_predio!(estacao_ponto:, responsavel:, justificativa: nil)`**: reaproveita o mesmo efeito de `#desconsiderar!` (`desconsiderado: true`, `ressalva: true`, exclusão real do cálculo via `CalculoDiarioService::EstrategiaBase#registros`, sem duplicar essa lógica), com `tipo: "desconsideracao_predio"` e justificativa padrão gerada ("Ponto desconsiderado: servidor bateu na estação [descrição], prédio não autorizado") — aceita override opcional pra não travar um caso em que o responsável queira detalhar mais. **`IntervencaoFrequencia.solicitar_autorizacao_predio(time_record, estacao_ponto:)`**: mesma assinatura minimalista de `.solicitar_autorizacao_horas_extras` (método de classe, sem `responsavel:`/`justificativa:` na criação); embora a ativação seja sempre manual (diferente de horas extras, que é o sistema quem abre), o desenho escolhido foi "mecanismo abre o pedido pendente, pessoa resolve" — o responsável fica registrado na resolução (`#deferir!(responsavel:)`/`#indeferir!(responsavel:)`), preservando rastreabilidade sem reabrir a assinatura do método de criação. 8 testes novos: 5 em `test/models/intervencao_frequencia_test.rb` (solicitação nasce pendente sem responsavel/justificativa; exige time_record; deferir!/indeferir! funcionam sem alteração; validação de time_record obrigatório pros 2 tipos novos), 1 em `test/models/time_record_test.rb` (`desconsiderar_por_predio!` marca desconsiderado/ressalva e cria intervenção com justificativa padrão; aceita justificativa customizada — 2 testes), 1 em `test/services/calculo_diario_service_test.rb` (prova de efeito real no motor de cálculo, mesmo padrão da 19.2, adaptado pro novo método). `bin/rubocop` sem ofensas nos 5 arquivos tocados (2 models, 3 testes). Suíte completa: **544 testes, 1515 assertions** (536→544, 1489→1515), 1 falha pré-existente (timezone em `presenca_endpoints_test.rb`, mesma falha reportada desde 14.1, não relacionada) — sem regressão. |
| 19.5 | Testes: fluxos de aprovação/rejeição, auditoria | UC-08–11 — ✅ **Concluída (2026-09-04)** — auditoria de cobertura das tasks 19.1-19.4. Fluxos de aprovação/rejeição (`deferir!`/`indeferir!`, erro em intervenção já resolvida ou nunca pendente, distinção `responsavel`/`resolvido_por`) e o efeito real de `desconsiderado`/`reconsiderar!` no motor de cálculo (`CalculoDiarioService::EstrategiaBase#registros`) já estavam bem cobertos pelas tasks anteriores — nenhum teste duplicado. 2 gaps reais encontrados e fechados em `test/models/time_record_test.rb`: (1) a associação `TimeRecord#intervencoes_frequencia` é `has_many` por decisão deliberada (um registro pode acumular mais de uma intervenção ao longo do tempo, ex. desconsiderado → reconsiderado), mas nenhum teste exercitava a associação em si (só `IntervencaoFrequencia.find_by` isolado) — testado o acúmulo de 2 intervenções pro mesmo `TimeRecord`; (2) `dependent: :nullify` na mesma associação nunca tinha sido exercitado — testado que destruir um `TimeRecord` anula `time_record_id` nas intervenções associadas sem apagá-las, preservando o histórico de auditoria. `bin/rubocop` limpo nos arquivos tocados. Suíte completa: 546 testes, 1519 assertions, 0 falhas novas (1 falha pré-existente e não relacionada, timezone em `PresencaEndpointsTest`) |

### Sprint 20 — Recálculo em lote e valores retroativos (UC-16, UC-17)

| Task | Descrição | UC |
|---|---|---|
| 20.1 | Registrar valores retroativos (já sem o bug DUV-011) | UC-16 |
| 20.2 | Recalcular mês / recalcular todos (job) | UC-17 |
| 20.3 | Testes de idempotência do recálculo em lote | UC-17 |

**Critério de saída da Fase B:** o sistema calcula frequência de ponta a ponta (dia → mês → relatório final) com as mesmas regras do legado v2, e as telas populadas na Fase A mostram dados calculados de verdade.

---

## Fase C — Integração formal com Pessoas e cutover (Sprints 21–22)

### Sprint 21 — Formalizar integração cadastral (UC-14)

**Objetivo:** substituir a leitura simplificada criada na Sprint 10 pelo contrato oficial do ADR-0001, fechando a ACL pendente (PRD §3).

**REVISÃO (2026-09-04):** o objetivo acima e as tasks 21.1-21.4 abaixo ficaram obsoletos com a reversão do ADR-0001 já feita na task 8.12 (2026-09-02) — não existe mais uma "API canônica do Pessoas" a ser contratada via ADR-0006; o padrão definitivo já é a conexão direta somente-leitura ao Postgres do Pessoas (`PessoasRecord`), já em uso desde a Sprint 8. 21.1 (ADR-0006) e 21.2/21.3 (espelho vs. eventos, migração pro padrão escolhido) não fazem mais sentido como estavam escritas — não há decisão pendente aí, a decisão já foi tomada e implementada. 21.4 (ACL por ID, nunca FK/Hibernate direto) também já está coberta na prática pelos models `Pessoas::*` (nenhum código do Frequencia referencia FK do pessoas2 diretamente fora desses models). Essas 4 tasks ficam formalmente fechadas/supersedidas por essa nota, sem ADR novo — não vale burocratizar uma decisão que já está implementada e testada.

**Decisão registrada com o usuário (2026-08-28), para executar aqui — não antes:** a autenticação de administradores/usuários do Frequencia deve deixar de usar senha local (`has_secure_password`) e passar a usar o **login/senha reais do Pessoas**, via a estratégia `sticapi_authenticatable`/Devise que o gem `sticapi_client` já traz pronta (**correção 2026-09-02**: apesar do nome, essa estratégia não é usada pelo pessoas2 — seu `User` autentica com `:database_authenticatable` local; é um mecanismo do gem, sem referência viva de uso em produção neste workspace, ver detalhe na task 21.5). Como consequência, o CRUD administrativo local de `User` (criar/editar/excluir frequentador manualmente pela tela) deixa de fazer sentido — **a intenção é remover as rotas de `POST`/`DELETE` de `Admin::UsersController`** (mantendo GET/listagem), já que o cadastro passa a vir inteiramente do Pessoas. Isso só deve ser feito **depois** que a importação em massa (Sprint 10B) e a autenticação via Sticapi estiverem validadas de ponta a ponta — remover escrita local prematuramente quebraria o cadastro manual que ainda é a única via disponível hoje.

**REVISÃO da 21.5 (2026-09-04):** decisão do usuário muda a abordagem de "login/senha via Sticapi" para **login/senha via leitura direta do hash bcrypt no banco do Pessoas** — mais simples e consistente com o padrão já adotado (SELECT-only, sem depender do gateway Sticapi estar de pé). Achado que viabiliza isso: a tabela `users` do pessoas2 usa Devise `:database_authenticatable` (bcrypt puro, gem `bcrypt ~> 3.0`, mesma família usada pelo `has_secure_password` do Frequencia) — `encrypted_password` é um hash bcrypt autocontido, verificável com `BCrypt::Password.new(hash) == senha_em_texto`, sem precisar da app do pessoas2 nem da Sticapi rodando. `Pessoas::Pessoa` (Frequencia) já expõe `username`/`cpf`; falta só um `Pessoas::User` (leitura de `users.username`/`encrypted_password`, join por `username` com `pessoas`) e ajuste no fluxo de autenticação local. Ver task 21.5 revisada abaixo — **substitui** o plano de `sticapi_authenticatable`/Devise (mantido como registro histórico da investigação, mas não é mais o caminho escolhido).

**Mecanismo confirmado tecnicamente em 2026-08-28** (testado com o login real do usuário, senha nunca persistida em arquivo/log/commit — só via `ENV` em teste ad-hoc, descartado depois):
1. `Sticapi::SticapiClient.instance.get_token` — pega o token de aplicação (mesmo usado pra tudo, `config/sticapi.yml`)
2. Monta um JWT: `JWT.encode({ user: login, password: senha }, token_de_app, "HS256")` — o **token de app funciona como segredo de assinatura**, a senha da pessoa nunca trafega em texto puro fora desse JWT
3. `POST /users/log_in` com esse JWT (`Sticapi::SticapiClient.instance.sticapi_request("/users/log_in", data: token)`)
4. Sucesso retorna dados vindos do **Active Directory** do TJPI (`dn`, `samaccountname`, `username`, `cpf`, `email`, `name`, `unities`, `unities_short`) — confirma que a Sticapi delega a validação de senha ao AD, não guarda/compara nada por conta própria
5. A gem `sticapi_client` já implementa isso pronto como estratégia Devise (`Devise::Strategies::SticapiAuthenticatable`, registrada como `:sticapi_authenticatable`) — **não precisa reimplementar**, só configurar o model `User` pra usar essa estratégia. **Não** é "mesmo padrão do pessoas2" (correção 2026-09-02) — o pessoas2 usa `:database_authenticatable` local; essa estratégia do gem não tem uso confirmado em nenhum app real deste workspace até agora

**Entrega parcial antecipada (2026-08-28), fora da numeração das tasks:** `admin/users/:id/edit` já bloqueia edição manual para frequentadores com `cpf` preenchido (vindos do Pessoas) — campos desabilitados na view **e** bloqueio real no `Admin::UsersController#update` (defesa em profundidade, já que `disabled` no HTML não impede um POST direto). Usuários sem `cpf` (admins, cadastros manuais) continuam editáveis normalmente. Isso adianta parte do espírito da task 21.6 (que fala em remover `POST`/`DELETE` do controller inteiro) de forma seletiva — só para quem já está vinculado ao Pessoas — sem esperar a troca de autenticação da 21.5. 4 testes em `test/controllers/users_controller_test.rb`.

| Task | Descrição | UC |
|---|---|---|
| 21.1 | ~~Especificar/aceitar ADR-0006 (contrato da API canônica do Pessoas)~~ — **SUPERSEDIDA (2026-09-04)**: não existe mais API a contratar; o padrão definitivo (conexão direta somente-leitura, ADR-0001 revertido) já foi decidido e implementado na task 8.12. Ver nota de revisão da Sprint 21 acima. | PRD §10 |
| 21.2 | ~~Decidir e implementar o padrão definitivo: espelho por eventos vs. `sticapi_client`~~ — **SUPERSEDIDA (2026-09-04)**: decisão já tomada (conexão direta), não é mais uma escolha entre essas duas opções | ADR-0001 |
| 21.3 | ~~Migrar `Frequentador`/`Direito` do espelho via job para o padrão definitivo escolhido em 21.2~~ — **SUPERSEDIDA (2026-09-04)**: já migrado nas tasks 8.13-8.16 (leituras via `Pessoas::Vinculo` ao vivo) | UC-14 |
| 21.4 | ~~Anti-corruption layer: referências por ID, nunca FK/Hibernate direto~~ — **SUPERSEDIDA (2026-09-04)**: já coberto na prática pelos models `Pessoas::*` (`app/models/pessoas/`), único ponto de acesso ao pessoas2 | PRD §3 |
| 21.5 | Substituir autenticação local (`has_secure_password`) por login/senha reais do Pessoas (pedido do usuário, 2026-09-02) — **REVISADA (2026-09-04)**: abandonada a via `sticapi_authenticatable`/Devise (investigação registrada no histórico abaixo, tecnicamente viável mas nunca usada em produção neste workspace) em favor de **leitura direta do hash bcrypt** via `PessoasRecord`, mesmo padrão SELECT-only já em uso. Confirmado: pessoas2 usa Devise `:database_authenticatable` (bcrypt puro, `bcrypt ~> 3.0`, mesma família do `has_secure_password` do Frequencia) — `users.encrypted_password` é autocontido e verificável com `BCrypt::Password.new(hash) == senha`, sem depender da app do pessoas2 nem de rede/Sticapi. Escopo: `Pessoas::User` (novo model, lê `username`/`encrypted_password` de `pessoas2.users`, join por `username` com `Pessoas::Pessoa` pra achar `cpf`); `User#authenticate` do Frequencia passa a verificar contra esse hash quando o usuário tem `cpf` (vindo do Pessoas), mantendo `has_secure_password` local só como fallback pra contas sem `cpf` (admins/cadastros manuais) — não remove `password_digest` nem quebra o login de admin local. *Histórico da investigação anterior (Sticapi/Devise, não mais o caminho escolhido):* `Devise::Strategies::SticapiAuthenticatable` já existe pronta dentro do gem `sticapi_client` — monta um JWT assinado com o token de aplicação, faz `POST /users/log_in` no gateway Sticapi; tecnicamente viável mas dependeria do gateway estar de pé, sem referência viva de uso em produção neste workspace — **✅ IMPLEMENTADA (2026-09-04)**: `app/models/pessoas/user.rb` criado (`Pessoas::User < PessoasRecord`, `table_name = "users"`, método de classe `buscar_por_cpf`). Achado real que simplificou o escopo original: `pessoas2.users` já tem a coluna `cpf` direto na própria tabela (`db/schema.rb` linha ~4887) — **não precisou** do join por `username` com `Pessoas::Pessoa` cogitado na primeira redação desta nota; a busca é só `Pessoas::User.buscar_por_cpf(cpf)`. `User#authenticate` (`app/models/user.rb`) foi sobrescrito: quando `cpf.present?`, valida a senha contra `BCrypt::Password.new(pessoas_user.encrypted_password) == senha`; quando `cpf.blank?`, cai em `super` (`has_secure_password` local, intocado — login de admin sem cpf continua idêntico). Decisão para usuário com `cpf` mas sem `Pessoas::User` correspondente (conta removida/renomeada no pessoas2): login **falha** (`false`), sem fallback pra senha local — evita uma porta de autenticação órfã com `password_digest` potencialmente desatualizado ou nunca definido para essas contas. Efeito colateral encontrado e corrigido: `test/controllers/time_records_controller_test.rb` criava um `@admin`/`@user` de teste com `cpf` preenchido (só pra simular o filtro por nome via `Pessoas::Vinculo.cpfs_por_nome`) — precisou stubar `Pessoas::User.buscar_por_cpf` no `setup`/`teardown` do arquivo pra não quebrar o login desses usuários de teste; os demais arquivos com `admin: true` não tinham `cpf` e não precisaram de ajuste. 7 testes novos (4 em `test/models/user_test.rb`, 1 em `test/controllers/sessions_controller_test.rb`, mais os ajustes de stub); suite completa: 551 testes, 1527 assertions, 0 falhas novas (1 falha pré-existente conhecida e não relacionada, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone). `bin/rubocop` sem ofensas nos arquivos tocados. | ADR-0001, decisão 2026-08-28, revisão 2026-09-04, implementação 2026-09-04 |
| 21.6 | Remover `POST`/`DELETE` de `Admin::UsersController` (criação/exclusão manual de frequentador) — só depois de 21.5 validado; manter somente leitura/listagem | Decisão 2026-08-28 — **✅ IMPLEMENTADA (2026-09-04)**: rotas `new`/`create` (POST) e `destroy`/`purge` (DELETE, incluindo o member route `purge`) removidas de `config/routes.rb` e as respectivas actions removidas de `app/controllers/admin/users_controller.rb`; `before_action`s (`set_user`, `require_admin`) restritos a `[:index, :edit, :update]`. Ficam ativas apenas `index` (listagem) e `edit`/`update` (PATCH) — `update` foi mantida porque não é "criação/exclusão", é edição, e o controller já bloqueia edição de quem tem `cpf` desde a entrega antecipada de 2026-08-28 (só resta útil para contas locais sem `cpf`, ver achado abaixo). View `app/views/admin/users/new.html.erb` removida (órfã, sem rota). Botões "Novo Usuário", "Inativar" e "Excluir definitivamente" removidos de `app/views/admin/users/index.html.erb` — e também de `app/views/admin/frequentadores/index.html.erb`, que reaproveitava as mesmas rotas `user_path`/`purge_user_path` (achado durante a investigação, não estava listado no escopo original da task; sem essa remoção a view quebraria com `NoMethodError: undefined method 'purge_user_path'`). **Achado sobre criação de admin local (investigação pedida antes da remoção):** a tela `new`/`create` era, na prática, a única forma de criar uma conta local *sem* `cpf` (usada hoje só para administradores e cadastros manuais/demo, já que `user_params` nunca permitiu `cpf` — ou seja, nunca criava "frequentador" de fato, pois o cadastro cadastral de frequentador já vinha do Pessoas desde a task 8.14). Removida a tela, a única via que sobra para criar/editar-senha/inativar/excluir um admin local é `rails console`/`db:seed` (mesmo padrão já usado em `db/seeds.rb` para o usuário `admin.admin`) — não há mais nenhuma via self-service pela UI. Isso é uma regressão operacional real (deixa de ser tarefa de admin-via-tela e passa a exigir acesso a console/deploy), mas não é um bloqueio: (1) a decisão de negócio já era explícita desde 2026-08-28 em remover completamente escrita manual do controller; (2) contas admin são criadas raramente (evento de setup/onboarding, não operação do dia a dia); (3) o mecanismo alternativo (console/seeds) já existe e é usado hoje para o admin inicial. Registrado aqui para visibilidade — se a operação sentir falta de uma tela de gestão de admins locais, é matéria para uma nova task futura (ex: um `rails runner`/`rake task` dedicado, ou reabrir uma tela restrita só para contas sem `cpf`), não para reverter esta remoção. Testes: removidos os 2 testes de `new`/`create` e os 3 de `destroy`/`purge` em `test/controllers/users_controller_test.rb` (ficam 6 testes: listagem, seção "sem cpf", edit/update para com e sem cpf). Suite completa: 545 testes, 1502 assertions, 0 falhas novas (1 falha pré-existente conhecida e não relacionada, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone). `bin/rubocop` nos arquivos tocados: 0 ofensas novas (as únicas ofensas remanescentes, `Layout/SpaceInsideArrayLiteralBrackets`, são pré-existentes no estilo do `config/routes.rb`/`users_controller.rb`, confirmado via `git stash` antes desta task — não introduzidas por esta mudança). |
| 21.7 | Testes: consistência eventual, reprocessamento de evento perdido, login via Sticapi (sucesso/falha), ausência de rotas de escrita manual | UC-14 | **✅ IMPLEMENTADA (2026-09-04)** — task de auditoria de cobertura (mesmo padrão de 14.5/17.4/18.4/19.5), não de implementação nova. Descrição original reinterpretada: "consistência eventual"/"reprocessamento de evento perdido" **não se aplica** — não existe mais fila de eventos nem espelho por eventos a reprocessar desde a reversão do ADR-0001 (task 8.12); no lugar, adicionado 1 teste cobrindo o comportamento real equivalente (`test/models/user_test.rb`: `authenticate` propaga a exceção quando a leitura ao pessoas2 falha, ex. `ActiveRecord::ConnectionNotEstablished` — sem rescue em nenhuma camada hoje, sem fallback silencioso pra senha local; comportamento documentado, não um mecanismo novo). "login via Sticapi" virou "login via pessoas2" (task 21.5) — 3 gaps reais fechados em `test/controllers/sessions_controller_test.rb`: falha de login do usuário vindo do Pessoas com senha errada (caminho de código do hash bcrypt, distinto do `has_secure_password` local já coberto), e falha de login quando o `cpf` existe no Frequencia mas não há mais `Pessoas::User` correspondente (conta órfã). "Ausência de rotas de escrita manual" (21.6) não tinha prova negativa — 3 testes adicionados em `test/controllers/users_controller_test.rb`: helpers `new_user_path`/`purge_user_path` levantam `NameError` (rota não gerada), e `POST /users`/`DELETE /users/:id` diretos retornam 404 (`show_exceptions = :rescuable` no ambiente de teste converte `ActionController::RoutingError` em resposta, não exceção). Nenhum teste duplicado dos já existentes na 21.5/21.6 foi adicionado. 6 testes novos; suite completa: 551 testes, 1513 assertions, 0 falhas novas (1 falha pré-existente conhecida e não relacionada, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone). `bin/rubocop` nos 3 arquivos de teste tocados: 0 ofensas. |

### Sprint 22 — Desligamento da Intranet (`presenca`)

| Task | Descrição |
|---|---|
| 22.1 | Checklist de paridade final Intranet × Frequência (todas as UCs P0–P2 validadas em produção) |
| 22.2 | Confirmar que `presenca_debitoremanscentenegociavel` (código morto) não foi migrada |
| 22.3 | Cutover: redirecionar EstaçãoPonto (config, não código) para o novo sistema |
| 22.4 | Período de coexistência monitorada (definir janela com o gestor) |
| 22.5 | Desligar módulo `presenca` da Intranet |
| 22.6 | Arquivar `docs2/` (decisão pendente do PRD §9) |

---

# Resumo do Cronograma Consolidado

| Sprint | Tema | Tasks | Status |
|--------|------|-------|--------|
| 1 | Setup e Modelagem | 10 | ✅ |
| 2 | Criptografia DES + UrlBase64 | 5 | ✅ |
| 3 | Endpoints de Autenticação e Relógio | 7 | ✅ |
| 4 | Endpoints de Dados Biométricos | 6 | ✅ |
| 5 | Registro de Batidas e Finalização | 10 | ✅ |
| 6 | Integração com Estação de Ponto (JavaFX) | 10 | ✅ Parcial |
| A | Layout + Views Iniciais + Auto-Alternação | 11 | ✅ |
| R | Reconciliação Estação/Frequência | 7 | ✅ |
| 7 | Login Manual na Interface de Ponto | 9 | ✅ |
| 8 | Integração com Sistema Pessoas via Sticapi (Fundação) | 16 | ✅ Concluída (16/16) — inclui reversão formal do ADR-0001 (8.12, 2026-09-02): conexão direta somente-leitura ao Postgres do Pessoas substitui Sticapi; a reescrita (8.13) de 4 dos 6 pontos que usavam `SticapiClient` pra ler via `PessoasRecord`; a substituição das leituras restantes de `FrequentadorCache` por SELECT ao vivo em `frequencia_por_orgao`/`dashboard` (8.14); filtro de Usuário de `admin/time_records` (8.15); e `admin/users` passando a listar vínculo ativo do pessoas2, com seção separada pra contas locais sem cpf (8.16) |
| 9 | Model de Estações + tela `admin/estacoes` | 6 | ✅ Concluída (6/6) — inclui replicação da estrutura legado e importação de dados reais das estações (9.6, 2026-09-02) |
| 10 | Sincronização de Frequentadores (espelho Sticapi) | 11 | ✅ Concluída (11/11) — inclui filtro de Órgão habilitado (10.9); fonte de dados trocada de `User`/`FrequentadorCache` para `Pessoas::Vinculo.ativos` direto do pessoas2 (10.10); filtro de Categoria habilitado via `categorias_trabalhador` do pessoas2 (10.11, 2026-09-02) |
| 10B | Importação em Massa de Frequentadores por Unidade (Sticapi) | 10 | ✅ Concluída (10/10) — validada contra dados reais (69 frequentadores importados) |
| 11 | Model de Regimes + tela `admin/regimes` | 9 | ✅ Concluída (9/9) — inclui estrutura real portada da Intranet legada (11.5), alinhamento aos valores reais de produção (11.6), filtros de categoria/modalidade habilitados (11.7), rótulos legíveis de modalidade (11.8) e importação dos 309 regimes reais do legado + filtro de exibição real (11.9, 2026-09-02) |
| 12 | Sincronização de Direitos/Deveres (espelho Sticapi) | 7 | ✅ Concluída (7/7) |
| 13 | Grid de Frequência (registros brutos) | 4 | ✅ Concluída (4/4) |
| 14 | Telas agregadas | 5 | ✅ Concluída (4/5) — `frequencia_por_orgao` (14.1), `gestores_individuais` (14.2), `versoes` (14.4), auditoria de testes (14.5); `relatorio_terceirizados` (14.3) formalmente bloqueada por falta de endpoint no pessoas2 |
| 15 | Dashboard consolidado | 3 | ✅ Concluída (3/3) — critério de saída da Fase A atingido |
| 16 | Motor de cálculo diário (UC-05) | 4 | ✅ 4/4 concluídas — 16.1 (modelagem do agregado `Dia`), 16.2 (métodos de cálculo do `Regime`: meta semanal, períodos por dia da semana), 16.3 (precedência de `RegimeFrequentador` + orquestração por modalidade via Strategy pattern em `CalculoDiarioService`) e 16.4 (4 testes de paridade com dados reais do legado v2 — 2 bateram exatamente, 2 divergências documentadas e rastreadas a simplificações já deliberadas na 16.3, nenhum bug genuíno encontrado). **Atenção**: "4/4 concluídas" significa que o escopo formal de cada task foi entregue, não que o **critério de saída da Fase B** (linha 564: "calcula frequência de ponta a ponta... com as mesmas regras do legado v2") foi atingido — o motor segue **intencionalmente simplificado**: banco de horas/hora-extra (GCET 2018-2022 e o cap geral confirmado na 16.4), carência de 15min desde maio/2017, expediente excepcional/feriados/abonos, `aberto`/`ausencia` completos, e pareamento de marcações por tipo de operação (ENTRADA/SAIDA, não 1a/última) seguem fora do motor, todos documentados como débito técnico explícito a portar em tasks futuras (ainda não numeradas, fora de Sprints 17-20 que cobrem consolidação mensal/banco de horas/relatório/exceções, não o refinamento do cálculo diário em si) |
| 17 | Consolidação mensal e banco de horas (UC-06) | 5 | ✅ **Concluída (5/5)** — 17.1 (`RegistroMensalFrequencia` + `ConsolidacaoMensalService`: agregação mensal e saldo/banco de horas portados de `RegistroMensalFrequenciaServices.java`), 17.2 (congelamento real de `finalizado`, corrigindo o bug do legado — `setFinalizado(true)` nunca era chamado — `consolidar` agora recusa recalcular um mês finalizado via `MesFinalizadoError`, `finalizar!`/`reabrir!` como ações explícitas), 17.3 (`frequencia_por_orgao` mostra "Trabalhado" real somando `CalculoDiario`; dashboard ganhou KPI "Faltas no Mês"; `frequencia` avaliada e mantida sem alteração), 17.4 (auditoria de testes, 3 gaps reais fechados: fluxo ponta a ponta, trava testada por snapshot completo, cadeia de saldo cruzando limite em 3 meses) e 17.5 (`CalcularFrequenciaJob`, `LOCK_KEY = 451_926_073`, `config/recurring.yml` às 00:10 — quita o débito técnico registrado ao fechar a 17.4: pipeline de cálculo agora roda automaticamente todo dia, recalculando os últimos 3 dias e consolidando o mês corrente de cada usuário, pulando silenciosamente meses já finalizados) |
| 18 | Relatório final e retificador (UC-07, UC-12) | 4 | ✅ Concluída (4/4) — 18.1 (`RelatorioFrequenciaFinal`/`RelatorioFrequentador` portados de `RelatorioFrequenciaFinalServices.java`, geração idempotente por `ano`/`mes`, `valor_retroativo` sempre 0 até `ValorRetroativo` existir, DUV-011 documentado sem replicar); 18.2 (`RetificadorBancoHoras` — 7 tipos/fatores portados de `TipoRetificadorEnum.java`, `retificado` agora somado de verdade em `ConsolidacaoMensalService`, `aplicar_retificador` permite corrigir mês finalizado sem reabri-lo); 18.3 (`ValorRetroativo` criado, bug DUV-011 do legado `cont=+valor` corrigido via `ValorRetroativo.soma_do_mes`/`.sum`, `valor_retroativo` agora somado de verdade e fórmula completa do `resultado` restaurada em `RelatorioFrequenciaFinalService`); 18.4 (auditoria de cobertura — 3 gaps reais cobertos: geração idempotente refletindo `valor_retroativo` atualizado entre rodadas, retificação pós-fechamento provada por snapshot completo dos campos congelados, e confirmação de que retificar não reabre a trava de `MesFinalizadoError`). **Ressalva de fidelidade ao legado**: banco de horas GCET histórico, carência 15min, expediente excepcional, algoritmo de desconto em folha completo, e ausência de job automático pro relatório final/retroativos/retificadores (só o cálculo diário/mensal tem job, task 17.5) seguem fora de escopo, mesma ressalva já registrada ao fechar as Sprints 16/17 |
| 19 | Gestão de exceções (UC-08 a UC-11) | 5 | ✅ Concluída (5/5) |
| 20 | Recálculo em lote e retroativos (UC-16, UC-17) | 3 | 📋 Planejada |
| 21 | Formalizar integração cadastral (UC-14) | 7 | ✅ Concluída (7/7 — 4 supersedidas/fechadas por nota: 21.1-21.4, decisão já tomada e implementada na task 8.12 antes mesmo desta sprint existir; 3 implementadas de fato nesta sprint: 21.5 autenticação via hash bcrypt do pessoas2, 21.6 remoção de rotas de escrita manual, 21.7 auditoria de cobertura fechando os gaps reais das duas anteriores) |
| 22 | Desligamento da Intranet (`presenca`) | 6 | 📋 Planejada |
| 23 | Devise + CanCanCan + Rolify (Autenticação + Autorização) | 10 | ✅ Concluída (10/10 — login/logout Devise, autenticação via cpf/Pessoas2 preservada, Rolify + Ability configurados, CanCanCan integrado em todos os controllers admin, seeds/roles migrados, views Devise criadas, suíte de testes fechando os gaps reais de cobertura na 23.10) |
| 24 | CSS/Estilo + Dark Mode + Font Awesome | 9 | ✅ Concluída (9/9 — infraestrutura de build Node/Sass replicada do basic8 (24.1), CSS custom e dark mode portados (24.2/24.3), Propshaft servindo o build local no lugar do CDN (24.4), todos os ícones migrados de Bootstrap Icons para Font Awesome (24.5), helper `menu_activated?` (24.6), Stimulus controllers de tema/sidebar criados e conectados ao layout (24.7/24.8), auditoria de testes fechando o critério de saída na medida do testável sem Capybara/Selenium (24.9)) |

**Total: 162 tasks**

---

## Observações

- **Ordem recomendada:** Parte 1 (Sprints 1→8) primeiro — canal EstaçãoPonto e fundação Sticapi — depois Parte 2 (Sprints 9→22) em fases: A (9–15) → B (16–20) → C (21–22).
- **Sprint 2** pode ser paralelizada com a Sprint 1 se houver mais de um desenvolvedor.
- **Sprint 6** depende da conclusão das Sprints 1 a 5 e do acesso à Estação de Ponto real (cliente JavaFX).
- **Sprint 7** depende das Sprints A (view PontoDePresenca) e R (CRUD admin de usuários).
- **Sprint 8** é independente do fluxo de marcação de ponto (Sprints 1-7) — pode ser feita em paralelo. Entrega apenas a fundação da integração Sticapi.
- **Sprint 10** depende da Sprint 8 (fundação Sticapi) e é pré-requisito técnico para a Sprint 12 (mesma infraestrutura de job/autenticação).
- **Fase B (16-20)** tem dependência técnica fixa entre si: 16 → 17 → 18; as demais (19, 20) podem ser paralelizadas entre si após a Fase B iniciar.
- **Fase C (21-22)** só deve começar depois que a Fase B estiver com critério de saída atendido e o ADR-0006 aceito.
- **Integração com Pessoas (Sprints 10 e 12) via `sticapi_client`** — não há acesso direto ao banco de Pessoas2 nem credencial de banco compartilhada; toda comunicação é via API HTTP autenticada da Sticapi, com credenciais em `config/sticapi.yml` (Sprint 8). Horário do job diário já definido (00:00).
- **Débito técnico assumido:** o espelho local atualizado por job (Sprints 10 e 12) é uma solução simplificada — não deixar essas sprints "fecharem" sem registrar o débito formalmente (issue), para reavaliar na Sprint 21 se o padrão final continua sendo espelho via job ou migra para eventos assíncronos.
- **Débito técnico assumido (2026-08-31):** o `Regime` (Sprint 11, task 11.5) já tem o schema real portado da Intranet legada (`expediente` jsonb, `configuracao` de limites/banco de horas, categorias muitos-pra-muitos), mas **nenhuma lógica de cálculo foi portada** — `metaSemanal()`, `periodosSemana()`, `getResumo()` dinâmico e a orquestração por modalidade continuam pendentes, fica formalmente registrado como escopo da Sprint 16 (ver nota detalhada nessa sprint, com os métodos exatos de `Regime.java` a portar).
- **Débito técnico assumido (2026-09-03), quitado em 2026-09-03 na task 17.5:** `CalculoDiarioService.calcular` (Sprint 16) e `ConsolidacaoMensalService.consolidar` (Sprint 17) existem desde as sprints 16/17, mas até então nada os disparava automaticamente — eram só services chamáveis manualmente (console/futuro botão). Resolvido com `CalcularFrequenciaJob` (`LOCK_KEY = 451_926_073`), agendado em `config/recurring.yml` às 00:10 (produção), mesmo espírito dos jobs diários já usados pra espelhar Pessoas (Sprints 10/12).
- **Testes:** Cada sprint deve incluir testes unitários e/ou de integração. Não postergar testes para o final.
- **Validação:** Ao final de cada sprint, rodar `rails test` completo e garantir que não há regressão.
- **Tela `relatorio_terceirizados`** não tem colunas definidas no layout atual — levantamento de requisito pendente antes da Sprint 14.
- **View `presenca_frequentadorestacao`** precisa ser validada em produção antes da Sprint 21 (cache/eventos).
- **ADR-0006** (contrato API do Pessoas) precisa ser criado e aceito antes da Sprint 21.
- **Destino de `docs2/`** — decisão do gestor, não bloqueia sprints técnicas.
- Endpoints periféricos (`PrediosPermitidos`, fotos, auto-update de estação) tratados caso a caso, sob demanda.
- **Status geral:** Sprints 1-5 concluídas (25 testes, 0 falhas). Sprint 6 parcialmente concluída — API validada via curl, mas testes com estação JavaFX real pendentes (Maven não disponível). Sprint A e R concluídas (127 testes, 0 falhas). Sprint 7 concluída. Sprint 8 concluída (11/11). Sprint 9 concluída. Sprint 10 concluída (8/8). Sprint 10B concluída (10/10, validada com dados reais — 69 frequentadores importados). Sprint 11 concluída (6/6, estrutura real do Regime portada e alinhada à Intranet legada). Sprint 12 concluída (7/7, `AfastamentoCache` via `SticapiClient::Intranet`). Suíte completa: 296 testes, 1 falha pré-existente (timezone, não relacionada). Sprints 13–22 planejadas/pendentes.

---

# Parte 3 — Migração de Infraestrutura (basic8 → Frequencia)

> **Objetivo:** Incorporar padrões de infraestrutura do template basic8 ao Frequencia, respeitando as regras de negócio existentes e o layout AdminLTE 4. O Frequencia é a aplicação-alvo; o basic8 é a referência. Estilo/CSS podem ser alterados conforme decisão do usuário.
>
> **Pré-requisitos concluídos:** Análise completa de ambos os projetos, mapeamento de compatibilidade, priorização do Orchestrator.

---

## Sprint 23 — Devise + CanCanCan + Rolify (Autenticação + Autorização)

**Objetivo:** Implementar framework completo de autenticação (Devise) e autorização (CanCanCan+Rolify), substituindo `has_secure_password` e o check binário `admin?`.

> ⚠️ **Risco alto:** Esta sprint altera o mecanismo de login. A autenticação DEVE continuar funcionando via users/senhas importados do BD do Pessoas2 (não local). Usuários com `cpf` autenticam contra o hash bcrypt do Pessoas2 (`Pessoas::User.buscar_por_cpf(cpf).encrypted_password`). Apenas usuários sem `cpf` (admins/cadastros manuais) usarão Devise local. O método `authenticate` custom (User model, linha 56-63) NÃO pode ser removido — é o ponto de integração com o Pessoas2.

| # | Task | Descrição | UC |
|---|------|-----------|-----|
| 23.1 | Gerar migration `add_devise_to_users` | Adicionar colunas Devise: `email`, `encrypted_password`, `reset_password_token`, `reset_password_sent_at`, `remember_created_at`, `sign_in_count`, `current_sign_in_at`, `last_sign_in_at`, `current_sign_in_ip`, `last_sign_in_ip`. **NÃO remover** `password_digest` ainda (manter compatibilidade durante transição). | Auth |
| 23.2 | Gerar migration `create_roles` + `create_users_roles` | Rolify requer duas tabelas: `roles` (nome, resource_type/id) e `users_roles` (user_id, role_id). Seguir o schema do basic8. | Roles |
| 23.3 | Configurar Devise no model `User` | Adicionar `devise :database_authenticatable, :registerable, :recoverable, :rememberable, :validatable, :trackable`. **CRÍTICO:** Preservar o `authenticate` custom (User model, linha 56-63) que lê `Pessoas::User.buscar_por_cpf(cpf)` e verifica o hash bcrypt do Pessoas2. Usuários com `cpf` autenticam APENAS contra Pessoas2 (não contra `encrypted_password` local). Usuários sem `cpf` (admins) autenticam via Devise local. **NÃO remover** `password_digest` (manter compatibilidade). | Auth |
| 23.4 | Configurar `Rolify` no model `User` | Adicionar `rolify` ao Gemfile, `User.has_role?`, `User.add_role`, `User.with_role`. Criar seeds com roles padrão: `admin`, `gestor`, `operador`. | Roles |
| 23.5 | Criar `app/models/ability.rb` | CanCanCan Ability: mapear permissões por role (`:admin` → manage all, `:gestor` → read/manage limited, `:operador` → read only). Referenciar models existentes do Frequencia. | Auth |
| 23.6 | Configurar Devise routes + controllers | `devise_for :users` em `config/routes.rb`. Criar `app/controllers/users/registrations_controller.rb` e `app/controllers/users/sessions_controller.rb` customizados se necessário. | Auth |
| 23.7 | Integrar CanCanCan em controllers | `Admin::ApplicationController`: `check_authorization` + `rescue_from CanCan::AccessDenied`. Controllers existentes: adicionar `load_and_authorize_resource` onde aplicável. | Auth |
| 23.8 | Criar views Devise (`sessions/new`, `registrations/new`) | Layout de login com AdminLTE 4. Formulário de cadastro (se permitido). | Auth |
| 23.9 | Migrar seeds existentes | Atualizar `db/seeds.rb`: criar roles, atribuir roles ao admin existente, migrar status de `status: 1` para Devise `confirmed_at: Time.current` (ou similar). | Roles |
| 23.10 | Testes | Testar login/logout Devise,Ability (permissoes por role), Controller (acesso negado), Rolify (assign/remove role), autenticação via CPF (fallback). | Auth |

**Critério de saída:** Login/logout funciona via Devise. Usuários com `cpf` autenticam contra Pessoas2 (hash bcrypt remoto, não local). Usuários sem `cpf` autenticam via Devise local. CanCanCan+Rolify configurados. Roles criadas e atribuídas. Suíte de testes passando sem regressão.

**Status das tasks (atualizado em 2026-09-10):**
- ✅ **23.1** — Migration `20260910000000_add_devise_to_users.rb` presente no working tree; colunas Devise adicionadas, `password_digest` preservado.
- ✅ **23.2** — Migration `20260910000001_rolify_create_roles.rb` presente (`roles` + `users_roles`).
- ✅ **23.3** — `app/models/user.rb`: `devise :database_authenticatable, :registerable, :recoverable, :rememberable, :validatable, :trackable` configurado. `authenticate` custom (Pessoas2/bcrypt remoto) intocado — verificado nesta sessão, não foi tocado.
- ✅ **23.4** — `rolify` no model `User` confirmado.
- ✅ **23.5** — `app/models/ability.rb` presente, com matriz de permissões documentada em comentários de topo (baseline `can :read, :all` para autenticados, `:admin` → manage all, `:gestor` → manage TimeRecord/IntervencaoFrequencia + baseline).
- ✅ **23.6** — `devise_for :users` em `config/routes.rb`; `app/controllers/users/sessions_controller.rb` customizado presente.
- ✅ **23.7** — Integração CanCanCan em `Admin::ApplicationController` (`check_authorization` + `rescue_from CanCan::AccessDenied do |exception| redirect_to dashboard_path, alert: exception.message end`) já implementada. Nesta sessão: corrigidas as 13 falhas da suíte causadas pela mudança de comportamento (12 testes de controller — `estacoes`, `regimes`, `versoes` — esperavam redirect para o próprio recurso e passaram a esperar `dashboard_path`, que é o comportamento uniforme e intencional do handler; 1 teste de `AbilityTest#test_permission_matrix_matches_expected_rows` esperava `can?(:read, TimeRecord) == false` para usuário autenticado sem role, desatualizado frente ao baseline documentado `can :read, :all`, corrigido para `true`, preservando a checagem de que escrita continua negada). Verificado que nenhum outro controller do projeto ainda usa o padrão legado `require_admin(fallback_path)` — o método permanece apenas como fallback documentado em `Admin::ApplicationController`, sem chamadores. Suíte final: 644 testes, 1835 assertions, 1 falha (pré-existente, timezone em `PresencaEndpointsTest`, não relacionada a esta task). `bin/rubocop` sem ofensas nos arquivos tocados.
- ✅ **23.9** — `db/seeds.rb`: criação das roles padrão (`admin`, `gestor`, `operador` via `Role.find_or_create_by!`) e atribuição da role `:admin` ao usuário admin inicial (`Admin Admin`, guardado por `!admin_user.has_role?(:admin)` para idempotência) já estavam implementadas em sessão anterior — confirmado nesta sessão que continuam corretas e não foram tocadas na coluna `admin: true` legada (convivência documentada no `Ability`). **Decisão sobre `status: 1` × Devise `confirmed_at`** (parte central desta task): NÃO migrado. Investigado antes de decidir — `app/models/user.rb` habilita `devise :database_authenticatable, :registerable, :recoverable, :rememberable, :validatable, :trackable`; o módulo `:confirmable` NÃO está na lista, e não existe coluna `confirmed_at` no schema nem na migration `20260910000000_add_devise_to_users.rb` (task 23.1, explicitamente aditiva, que documenta todas as colunas criadas — `confirmed_at` não é uma delas). `status` continua sendo a fonte de verdade real de "ativo/inativo": usado em `scope :ativos` (`where(status: 1)`) e em `active_for_authentication?` (`super && status == 1`, task 23.6), o guard efetivo de login via Devise. Sem `:confirmable` habilitado, popular `confirmed_at` seria uma coluna inerte (Devise só a consulta com o módulo incluso) e arriscaria, se `:confirmable` fosse ativado no futuro sem backfill correspondente, bloquear login de usuários existentes com `status: 1` mas `confirmed_at: nil` — risco desnecessário para uma sprint já marcada "Risco alto". Decisão documentada como comentário extenso no próprio `db/seeds.rb`, acima da criação das roles. Se o produto decidir adotar confirmação de e-mail via Devise futuramente, isso exige uma migration nova (`add_confirmable_to_users`), habilitar `:confirmable` no model e um plano de backfill — fora do escopo desta task. Verificação: `bin/rubocop db/seeds.rb` reporta as mesmas 12 ofensas (`Layout/SpaceInsideArrayLiteralBrackets`) já pré-existentes antes desta sessão (confirmado via `git stash`), nenhuma nova, nenhuma nas linhas adicionadas. Suíte final: 645 testes, 1842 assertions, 1 falha pré-existente (mesma, timezone em `PresencaEndpointsTest`). `bin/rails db:seed` (development) executado do início ao fim sem erro (exit 0).
- ✅ **23.10** — Auditoria de cobertura de teste (critério de saída da Sprint 23). Lidos primeiro TODOS os testes já existentes (`user_test.rb`, `ability_test.rb`, `role_test.rb`, `sessions_controller_test.rb`, `users/sessions_controller_test.rb`, `devise_login_view_smoke_test.rb`, `estacoes/regimes/versoes_controller_test.rb`) para não duplicar — a cobertura das tasks 23.1-23.9 já era extensa (login/logout Devise sucesso+falha+cpf+inativo, matriz completa de Ability por role incluindo baseline de transição e dupla fonte admin, Rolify add/has/remove/with_role/idempotência, fallback sem-cpf). 4 gaps reais identificados e fechados (item 3 do critério de saída, "Controller acesso negado"): `Admin::UsersController` (`load_and_authorize_resource` em edit/update) não tinha NENHUM teste — criado `test/controllers/admin/users_controller_test.rb` (index, edit, update, bloqueio de edição para usuário vinculado ao Pessoas, e acesso negado para não-admin/gestor); `Admin::DireitosDeveresController#sincronizar_agora` e `Admin::FrequentadoresController#reimportar_dados_pessoa`/`#importar_unidade` usam `authorize! :manage, ...` mas só tinham teste do caminho de autenticação, não do caminho de autorização — acrescentados testes de acesso negado (não-admin e gestor) nos arquivos de teste existentes. Nenhum bug real encontrado nos arquivos protegidos (`user.rb`, `ability.rb`, `admin/application_controller.rb`) — apenas gaps de teste. Suíte final: 659 testes (14 novos), 1893 assertions, 1 falha pré-existente (mesma, timezone em `PresencaEndpointsTest`, não relacionada). `bin/rubocop` sem ofensas nos 3 arquivos tocados.
- ✅ **23.8** — Criada a view real `app/views/users/sessions/new.html.erb` (Devise, layout `"login"`, mesma identidade visual AdminLTE 4 da tela legada), substituindo o reuso temporário de `admin/sessions/new` que existia em `Users::SessionsController#new` desde a task 23.6 (o próprio comentário no controller já sinalizava "NÃO criar views Devise ainda (task 23.8)"). O form usa `form_for(resource, as: resource_name, url: session_path(resource_name))` — submete pela rota Devise nativa (`/u/sign_in`), passando pelo Warden e pelos overrides já existentes no model (`find_for_database_authentication`, `valid_password?`, `active_for_authentication?`), que continuam roteando usuários com `cpf` para o hash bcrypt do Pessoas2 sem nenhuma alteração — `authenticate` custom e a tela/rota legada `/login` (`Admin::SessionsController` + `admin/sessions/new`) foram mantidos intocados e continuam funcionando em paralelo (não há decisão nesta task de desativar o fluxo legado — troca de rota default seria uma mudança de comportamento maior, fora do escopo "criar views"). **Registrations**: `registrations/new` NÃO foi criada. A rota já vem `skip: %i[registrations]` desde a task 23.6, e a Sprint 21 (tasks 21.5/21.6) já decidiu que não há auto-cadastro — contas locais (sem `cpf`) são criadas via console/seeds, e usuários com `cpf` vêm do Pessoas2. Criar uma view para uma rota inexistente/desabilitada seria código morto e contradiria a decisão documentada; se o produto decidir habilitar auto-cadastro no futuro, isso exige uma nova decisão explícita (não assumida aqui). Teste novo: `test/integration/devise_login_view_smoke_test.rb` (GET `/u/sign_in` renderiza form com campos `username`/`password`). Suíte final: 645 testes, 1842 assertions, 1 falha (mesma pré-existente, timezone em `PresencaEndpointsTest`, não relacionada). `bin/rubocop` sem ofensas nos arquivos Ruby tocados (rubocop não processa `.erb`, mesmo comportamento já observado no arquivo legado `admin/sessions/new.html.erb`).

---

## Sprint 24 — CSS/Estilo + Dark Mode + Font Awesome

**Objetivo:** Portar variáveis SCSS, dark mode, CSS custom do basic8. Migrar de Bootstrap Icons para Font Awesome. Criar helper `menu_activated?`.

> **Nota:** Frequencia já carrega Font Awesome 6.7.2 via CDN (`application.css:11`). A migração é só trocar classes `bi-*` por `fa-*`/`fas-*`/`far-*` nas views.

**REVISÃO (2026-09-14) — pipeline de assets:** investigação prévia à 24.1 encontrou uma diferença de arquitetura não endereçada pelo texto original das tasks 24.1-24.4: o Frequencia usa só Propshaft, sem NENHUM pipeline de compilação Sass (sem `package.json`, sem gem `dartsass-rails`/`sassc-rails`) — carrega Bootstrap/AdminLTE/Font Awesome via `@import url(...)` de CDN em `application.css` puro. O `basic8` (fonte de referência das tasks 24.1-24.3) usa uma stack Node completa (`package.json` com `sass`, `esbuild`, `postcss`, `yarn watch:css`) que compila SCSS local e empacota Bootstrap/AdminLTE/FontAwesome/Tom Select/etc. a partir de pacotes npm — nada disso existe no Frequencia hoje. Sem resolver essa lacuna, criar `_variables.scss`/`_custom.scss`/`_dark_mode.scss` como a 24.1-24.3 pedem resultaria em arquivos mortos (Propshaft não compila Sass).

**Decisão do usuário (2026-09-14):** replicar a stack do `basic8` por completo — Node + Yarn + `sass`/`esbuild`/`postcss` via `package.json`, migrando Bootstrap/AdminLTE/Font Awesome de CDN pra pacotes npm locais compilados, igual ao `basic8`. Isso **substitui** a orientação original da task 24.4 ("Manter CDN imports para Bootstrap/AdminLTE/Font Awesome") — 24.4 fica revisada abaixo. Escopo real da 24.1 passa a incluir a infraestrutura de build necessária pra qualquer arquivo `.scss` ter efeito (não só criar o arquivo de variáveis).

| # | Task | Descrição | UC |
|---|------|-----------|-----|
| 24.1 | Criar `app/assets/stylesheets/_variables.scss` | Portar variáveis do basic8 (`_variables.scss`): paleta (`$primary: #2563eb`), corpo/texto, links, botões, formulários, navbar, sidebar (claro + escuro), paginação, alertas, tipografia (`Inter`), tabelas, cards, sombras. Adaptar para AdminLTE 4 (variáveis `$lte-sidebar-*`). | CSS |
| 24.2 | Criar `app/assets/stylesheets/_custom.scss` | Portar CSS custom do basic8: reset de links, transições sidebar/content/footer, `.header-icon`, `.no-validate`, `.form-control` fixes, `.text-pointer`, `.d-none-unless-busy`, `.list-item:hover`, `.active-filters`, pagination fix, table hover fix, card overflow fix. | CSS |
| 24.3 | Criar `app/assets/stylesheets/_dark_mode.scss` | Portar bloco `@if $enable-dark-mode { @include color-mode(dark) { ... } }` do basic8: cards, tabelas, form controls, paginação, `.bg-light`, `.btn-light`, `.app-content-header`, tom-select dark, toastr dark, CKEditor dark. | CSS |
| 24.4 | Atualizar `app/assets/stylesheets/application.css` | Adicionar imports: `_variables.scss`, `_custom.scss`, `_dark_mode.scss`. Manter CDN imports para Bootstrap/AdminLTE/Font Awesome. | CSS |
| 24.5 | Migrar todas as views de `bi-*` para `fa-*`/`fas-*`/`far-*` | Substituir Bootstrap Icons por Font Awesome em todas as views admin e presenca. Ex: `bi bi-people` → `fas fa-users`, `bi bi-pencil` → `fas fa-pencil`, `bi bi-search` → `fas fa-search`. | Icons |
| 24.6 | Criar helper `menu_activated?` em `ApplicationHelper` | Helper que compara `request.path` com o `url` do item de menu. Necessário para o menu dinâmico da Sprint 25. Referência: basic8 `app/helpers/application_helper.rb`. | Menu |
| 24.7 | Criar Stimulus controller `theme_controller.js` | Toggle de dark mode com persistência em `localStorage`. Referência: basic8 `app/javascript/controllers/theme_controller.js`. | JS |
| 24.8 | Criar Stimulus controller `sidebar_controller.js` | Persistência de estado sidebar (colapsado/expandido) em `localStorage`. Referência: basic8 `app/javascript/controllers/sidebar_controller.js`. | JS |
| 24.9 | Testes | Testar que CSS carrega sem erros, que dark mode toggle funciona, que Font Awesome icons renderizam. | CSS |

**Critério de saída:** Tema customizado aplicado (cores, tipografia, sombras). Dark mode funcional. Todos os ícones migrados para Font Awesome. Helpers e Stimulus controllers criados.

- ✅ **24.1** — Escopo revisado (ver "REVISÃO (2026-09-14)" acima): esta task passou a cobrir a infraestrutura de build Node/Sass inteira, não só o arquivo de variáveis. `node`/`yarn` confirmados disponíveis no ambiente (`node v22.23.0`, `yarn 1.22.22`) antes de qualquer instalação.
  - **`package.json`** criado na raiz do `api-ponto/` espelhando o `basic8`, mas com **escopo de dependencies reduzido de propósito**: só os 3 pacotes que hoje vêm de CDN — `bootstrap` (`^5.3.3`, resolvido em `5.3.8`), `admin-lte` (`^4.0.0`, versão estável real, igual à CDN atual — NÃO a `4.0.0-rc7` que o basic8 usa), `@fortawesome/fontawesome-free` (`^6.7.2`, mantendo a mesma versão major da CDN atual, não a `7.x` do basic8, para não arriscar quebrar ícones já em uso). DevDependencies: `sass`, `postcss`, `postcss-cli`, `autoprefixer`, `nodemon` (para `watch:css`). Deliberadamente **não** replicados: `esbuild` (Frequencia usa `importmap-rails`/`turbo-rails`/`stimulus-rails` via gem, não bundling de JS — confirmado no `Gemfile`), `jquery`, `@hotwired/*`, `tom-select`, `ckeditor5`, `sweetalert2`, `toastr`, `bootstrap-table`, `bootstrap-fileinput`, `@nathanvda/cocoon`, `bootstrap-icons` — nenhum desses é usado hoje no Frequencia; adicionar seria dependência morta.
  - **`app/assets/stylesheets/_variables.scss`** criado com o conteúdo portado do `basic8` (paleta `$primary: #2563eb`, corpo/texto, links, botões, formulários, navbar, sidebar claro+escuro, paginação, alertas, tipografia `Inter`, tabelas, cards, sombras). As variáveis `$lte-sidebar-*` (claro) e `$lte-sidebar-*-dark` foram conferidas contra o pacote `admin-lte@4.0.0` real instalado (`node_modules/admin-lte/src/scss/_variables.scss` e `_variables-dark.scss`) — os nomes batem exatamente com o basic8, nenhuma adaptação de nome foi necessária.
  - **`app/assets/stylesheets/application.scss`** criado como entry point do Sass (`@import "variables"` + `bootstrap/scss/bootstrap` + `admin-lte/src/scss/adminlte` + módulos do Font Awesome). Compila para `app/assets/builds/app.css` (nome escolhido deliberadamente diferente de `application.css`, para não colidir no Propshaft com o manifesto existente).
  - **Decisão de convivência (`application.css` vs `application.scss`):** o `app/assets/stylesheets/application.css` atual (CDN) **não foi tocado** e continua sendo o que o layout referencia. O novo `application.scss`/`app.css` compilado existe em paralelo, provando que a infra funciona, mas a troca do layout para consumi-lo (removendo os imports CDN) fica para a task **24.4**, como o texto revisado da sprint já previa ("24.4 fica revisada").
  - **`config/initializers/assets.rb`** (novo) registra `app/assets/builds` e `node_modules/@fortawesome/fontawesome-free/webfonts` em `config.assets.paths`, para o Propshaft enxergar o build e os webfonts locais.
  - **`Procfile.dev`** criado (`web: bin/rails server` + `css: yarn watch:css`). **`bin/dev`** atualizado para usar `foreman start -f Procfile.dev` (mesmo padrão do `basic8`, instala foreman sob demanda via `gem install foreman`, sem entrar no `Gemfile`) — antes só rodava `rails server` direto; documentado aqui porque é uma mudança de fluxo de dev.
  - **`.gitignore`** (raiz `Frequencia/`) atualizado para ignorar `api-ponto/app/assets/builds/*` (mantendo `.keep`), padrão idêntico ao `node_modules` já ignorado.
  - **Build rodado com sucesso**: `yarn build:css` gerou `app/assets/builds/app.css` (2.3MB, 40599 linhas) sem erro — só warnings de depreciação internos do próprio Bootstrap 5.3.8 (`@import`/funções de cor legadas do Dart Sass, não relacionados a este trabalho).
  - **Suíte de testes**: `bin/rails test` → 659 testes, 1893 assertions, 1 falha (mesma pré-existente e conhecida, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada). `bin/rubocop config/initializers/assets.rb` (único `.rb` novo/tocado) → sem ofensas.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.2** — `app/assets/stylesheets/_custom.scss` criado portando o bloco de CSS custom que no `basic8` vive inline no fim do `application.scss` (basic8 não tem um `_custom.scss` separado como o texto original da task supunha — o conteúdo foi extraído de lá). Portado literalmente: reset de links (`a { text-decoration: none !important }`), transições `.main-sidebar`/`.content-wrapper`/`.main-footer`, `.header-icon`, `.no-validate` (todas as variações `:valid`/`.is-valid`/`:checked`), `.form-control`/`.form-select`/`.input-group-text` (borda) + `.is-invalid` (cor `$danger`), `.text-pointer`, `.d-none-unless-busy` (+ `turbo-frame[busy]`), `.list-item:hover`, `.active-filters`, fix de espaço da paginação (`ul.pagination`), fix de hover de tabela (`.table-hover tbody tr:hover`), fix de overflow de card com dropdown (`.card.overflow-hidden .dropdown-menu`).
  - **Import adicionado** em `app/assets/stylesheets/application.scss`: `@import "custom";` logo após os módulos do Font Awesome (mesma posição relativa que o bloco ocupa no `application.scss` do basic8 — depois de Bootstrap/AdminLTE/FontAwesome).
  - **Deliberadamente NÃO portado** (documentado em comentário no topo do próprio `_custom.scss`): fix de sort do `.bootstrap-table` e regra `.ts-wrapper`/`.ts-control`/`.ts-dropdown` do tom-select (pacote não presente no `package.json` da 24.1); estilos de `.ck-content`/CKEditor5; bloco inteiro `@if $enable-dark-mode { @include color-mode(dark) {...} }` (o `_variables.scss` do Frequencia não define `$enable-dark-mode` — fica para quando/se a 24.3 de dark mode for revisada); import e cores de toastr (`#toast-container`, pacote não presente); `.login-page-theme` (botão de tema da login page do basic8, não listado na task); `fieldset { border: none !important }` (reset genérico não listado na task); `.list-container:has(.list-item) .list-empty-state` (depende de partial de listagem específico do basic8 não presente no Frequencia). Nenhum seletor novo foi inventado — tudo que entrou existe literalmente no `basic8` e bate 1:1 com os itens enumerados na descrição da task.
  - **Build**: `yarn build:css` compilou sem erro, `app/assets/builds/app.css` regenerado (mesmos warnings de depreciação pré-existentes do Bootstrap/Dart Sass, não relacionados a este trabalho).
  - **Suíte de testes**: `bin/rails test` → 659 testes, 1893 assertions, 1 falha (mesma pré-existente, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada a CSS). Nenhum arquivo `.rb` tocado — `rubocop` não executado por não ser necessário.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.3** — `app/assets/stylesheets/_dark_mode.scss` criado portando o bloco `@if $enable-dark-mode { @include color-mode(dark) { ... } }` do `basic8/app/assets/stylesheets/application.scss` (linhas ~192-300 lá): `.card`, `.table`, `.form-control`/`.form-select`, `.pagination`, `.bg-light`, `.btn-light`, `.app-content-header`.
  - **Achado que revisa a nota da 24.2** ("`_variables.scss` do Frequencia não define `$enable-dark-mode`"): investigado a fundo, `$enable-dark-mode` **não é uma flag definida pelo basic8** — é uma variável nativa do Bootstrap 5.3 (`node_modules/bootstrap/scss/_variables.scss:387`, `$enable-dark-mode: true !default;`). O `basic8` também não a redefine em seu próprio `_variables.scss` (confirmado via grep) — ele só usa a variável depois de `@import "bootstrap/scss/bootstrap"` no `application.scss`, ponto em que o default do Bootstrap já está carregado. Decisão: **não** adicionar `$enable-dark-mode` a `_variables.scss` do Frequencia (evitar redefinição redundante/confusa de uma variável que o Bootstrap já provê). Em vez disso, o import de `_dark_mode.scss` em `application.scss` foi posicionado depois de `bootstrap/scss/bootstrap` (que já é o caso, já que fica depois de `_custom`), garantindo que o default `true` esteja disponível — mesma lógica do basic8. Documentado em comentário no topo do próprio `_dark_mode.scss`.
  - **Import adicionado** em `app/assets/stylesheets/application.scss`: `@import "dark_mode";` logo após `@import "custom";` (última linha do arquivo).
  - **Deliberadamente NÃO portado** (mesmo critério da 24.1/24.2 — dependências ausentes do `package.json`, documentado em comentário no topo do `_dark_mode.scss`): tom-select dark mode (`.ts-wrapper`, `.ts-control`, `.ts-dropdown`, `.ts-item`), toastr dark mode (`#toast-container` e variações `.toast-success/error/info/warning`), CKEditor 5 dark mode (`.ck-editor__*`, `.ck-toolbar`, `.ck-content`, `.ck-bottom`). Nenhum seletor novo foi inventado — tudo que entrou existe literalmente no `basic8` e bate 1:1 com os itens da descrição da task (exceto os 3 acima, explicitamente fora de escopo por falta de dependência).
  - **Verificação de compilação**: `yarn build:css` compilou sem erro (mesmos warnings de depreciação pré-existentes do Bootstrap/Dart Sass, não relacionados a este trabalho). Confirmado no `app/assets/builds/app.css` gerado que os seletores `[data-bs-theme=dark] .card`, `[data-bs-theme=dark] .btn-light`, `[data-bs-theme=dark] .app-content-header` foram de fato emitidos.
  - **Suíte de testes**: `bin/rails test` → 659 testes, 1893 assertions, 1 falha (mesma pré-existente e conhecida, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada a CSS). Nenhum arquivo `.rb` tocado — `rubocop` não executado por não ser necessário.
  - Nota: esta task cobre só o CSS condicional de dark mode, sem lógica de troca de tema — o toggle funcional (Stimulus `theme_controller.js`) é escopo da task 24.7, ainda não implementada.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.4** — Escopo revisado (ver "REVISÃO (2026-09-14)" acima, que substitui o texto original "Manter CDN imports"). Objetivo real: fazer o Propshaft servir o CSS compilado localmente (`yarn build:css`) no lugar do CSS com imports de CDN, sem quebrar `stylesheet_link_tag "application"` já usado em `app/views/layouts/application.html.erb:8` e `app/views/layouts/admin.html.erb:27`.
  - **Decisão tomada (caminho (a) do passo 1):** renomear a **saída do build Sass** para `application.css` dentro de `app/assets/builds/`, em vez de trocar o nome nas duas views. Ajustado `package.json`: `build:css:compile` agora compila para `./app/assets/builds/application.css` (antes `app.css`) e `build:css:prefix` (autoprefixer) aponta pro mesmo novo caminho. Motivo da escolha: `stylesheet_link_tag "application"` já é referenciado em 2 views — trocar o nome do arquivo de build é uma mudança em 1 lugar (`package.json`), contra 2 lugares (as views) se a opção (b) fosse escolhida; além disso, mantém o nome semântico `application` como "o" manifesto de CSS da aplicação, igual ao padrão Rails/Propshaft convencional.
  - **`app/assets/stylesheets/application.css` (antigo, com `@import url(...)` de CDN) removido.** Não coexiste mais com o build local — eliminada a ambiguidade de qual arquivo o Propshaft serve. Antes da remoção, `app/assets/builds/app.css` (nome antigo do build) também foi apagado e regenerado já com o novo nome via `yarn build:css`.
  - **Confirmação de resolução do Propshaft:** com `bin/rails runner` (ambiente `test`), `Rails.application.assets.load_path.find("application.css")` resolve para `app/assets/builds/application.css` (o build local, 2.3MB) — não há mais nenhum `application.css` em `app/assets/stylesheets/` para colidir. O load path do Propshaft lista `app/assets/builds` antes de `app/assets/stylesheets`, então mesmo sem a remoção do antigo haveria prioridade correta, mas a remoção elimina a ambiguidade por completo, como pedido. `RAILS_ENV=test bin/rails assets:precompile` rodou sem erro ("asset not found" ausente).
  - **Build final**: `yarn build:css` rodado uma última vez, gerando `app/assets/builds/application.css` (Bootstrap + AdminLTE + FontAwesome + `_variables` + `_custom` + `_dark_mode` consolidados), mesmos warnings de depreciação pré-existentes do Dart Sass/Bootstrap, sem erro.
  - **Suíte de testes**: `bin/rails test` → 659 testes, 1893 assertions, 1 falha (mesma pré-existente e conhecida, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada a CSS). Nenhum `.rb` novo tocado nesta task (só `package.json` e a remoção do `.css` antigo) — `rubocop` não executado por não ser necessário.
  - **Views não alteradas**: `app/views/layouts/application.html.erb` e `app/views/layouts/admin.html.erb` continuam com `stylesheet_link_tag "application"` inalterado — a resolução para o novo asset é transparente graças à opção (a).
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.5** — Migração completa de Bootstrap Icons (`bi bi-*`) para Font Awesome 6 Free (`fas fa-*`) em todas as views. Levantamento via `grep -rln "bi-\|bi bi-" app/views/` confirmou **23 arquivos afetados** (22 views admin/presenca + `layouts/admin.html.erb`); `layouts/login.html.erb` não usa classes `bi-*` em si, só carregava o CDN de Bootstrap Icons. `grep -rohn 'bi bi-[a-z0-9-]*' app/views/ | sort -u` levantou **35 ícones únicos**, todos migrados via script Python (`re.sub` com mapa nome→nome, não sed simples, para evitar colisão de prefixo entre nomes como `bi-person` e `bi-person-plus`).
  - **Mapeamento aplicado** (todos confirmados como existentes no estilo `solid` do Font Awesome 6.7.2 Free via `node_modules/@fortawesome/fontawesome-free/metadata/icon-families.json`, não Pro-only): `arrow-repeat→rotate`, `arrow-right→arrow-right`, `box-arrow-in-right→right-to-bracket`, `calendar-check→calendar-check`, `calendar-month→calendar-days`, `calendar-week→calendar-week`, `calendar-x→calendar-xmark`, `chevron-right→chevron-right`, `circle→circle`, `clock-history→clock-rotate-left`, `exclamation-triangle→triangle-exclamation`, `file-earmark-text→file-lines`, `funnel→filter`, `grid-1x2→table-cells`, `hdd-network→network-wired`, `info-circle→circle-info`, `journal-text→book`, `list→bars`, `lock→lock`, `pencil→pencil`, `people→users`, `person→user`, `person-badge→id-badge`, `person-check→user-check`, `person-circle→circle-user`, `person-plus→user-plus`, `person-slash→user-slash`, `plus-lg→plus`, `search→magnifying-glass` (nome canônico FA6; `fa-search` é apenas alias legado), `shield-lock→shield-halved` (FA6 Free não tem ícone combinado "shield+lock"; escolhido o mais próximo semanticamente e documentado aqui), `speedometer→gauge`, `tag→tag`, `tags→tags`, `trash→trash`, `x-lg→xmark`. Todas as classes extras (`nav-icon`, `nav-arrow`, `me-1`, `fs-1`, etc.) foram preservadas — só o nome do ícone em si foi trocado, e o prefixo de estilo mudou de `bi bi-` para `fas fa-`.
  - **Verificação pós-migração**: `grep -rn "bi bi-\|bootstrap-icons" app/views/` retornou vazio — nenhuma classe `bi-*` nem referência a Bootstrap Icons restante em nenhuma view.
  - **CDN de Bootstrap Icons removido dos dois layouts**: `app/views/layouts/admin.html.erb:19` e `app/views/layouts/login.html.erb:15` (link `cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3`).
  - **Confirmação de que Font Awesome continua carregado nos dois layouts** (investigado antes de remover, não assumido): `admin.html.erb` usa `<%= stylesheet_link_tag "application" %>`, que resolve para o bundle local `app/assets/builds/application.css` compilado na task 24.4 — esse bundle já importa `@fortawesome/fontawesome-free/scss/{fontawesome,brands,regular,solid,v4-shims}` (confirmado em `app/assets/stylesheets/application.scss:17-21`), então FA já estava disponível ali antes mesmo da remoção do CDN. `login.html.erb`, por outro lado, é 100% CDN (usado pelos layouts de `admin/sessions#new` e `users/sessions#new` via `layout "login"`) e **não** carregava Font Awesome nenhum antes desta task — só Bootstrap Icons. Como as views `admin/sessions/new.html.erb` e `users/sessions/new.html.erb` (layout `login`) usam ícones (`fa-shield-halved`, `fa-triangle-exclamation`, `fa-user`, `fa-lock`), foi necessário **adicionar** um `<link>` de Font Awesome 6.7.2 via CDN (`cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.7.2/css/all.min.css`) em `login.html.erb`, no lugar do link de Bootstrap Icons removido — sem isso a tela de login ficaria sem ícones.
  - **Build**: `yarn build:css` rodado após a remoção do CDN de `admin.html.erb` (não afeta o build em si, só a view) — compilou sem erro, mesmos warnings de depreciação pré-existentes do Bootstrap/Dart Sass.
  - **Suíte de testes**: `bin/rails test` → 659 testes, 1893 assertions, 1 falha (mesma pré-existente e conhecida, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada a ícones/CSS). Nenhum arquivo `.rb` tocado — `rubocop` não executado por não ser necessário.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.6** — `app/helpers/application_helper.rb` criado (não existia ainda no Frequencia). Portado **fielmente** `eval_with_rescue`/`menu_activated?` de `Zutils::Helpers` (gem `zutils` 4.0.0, usada pelo `basic8`), fonte confirmada em `basic8/vendor/bundle/ruby/3.4.0/gems/zutils-4.0.0/lib/zutils/helpers.rb` — não a descrição literal original da task ("compara `request.path` com o `url` do item de menu"), que não reflete o código real.
  - **Uso de `eval()`: decisão consciente confirmada com o usuário em 2026-09-14**, documentada em comentário de topo no próprio arquivo. A string avaliada (`menu_item[:active_test]`) é definida internamente pelo desenvolvedor ao montar o Hash `@static_menu` (Sprint 25, ainda não implementada) — nunca vem de `params`/`request`/input externo — então não é uma superfície de RCE explorável por um atacante, mas segue sendo `eval()` de fato, por isso documentado explicitamente em vez de escondido.
  - **Rubocop**: `Security/Eval` e `Lint/RescueException` desabilitados pontualmente só na linha do `eval`/`rescue Exception`, com `# rubocop:enable` logo depois — não desabilitado globalmente. `bin/rubocop app/helpers/application_helper.rb test/helpers/application_helper_test.rb` → sem ofensas.
  - **Achado durante os testes (comportamento real, não assumido):** a porta fiel do `zutils` tem uma peculiaridade — quando `active_test` é `nil` (item sem `active_test` definido) ou contém código Ruby inválido, `eval` levanta exceção, `eval_with_rescue` cai no `rescue Exception` e retorna a **string `"error"`**, que em Ruby é um valor **truthy**. Ou seja, `menu_activated?` retorna truthy (a string `"error"`) tanto para itens sem `active_test` quanto para `active_test` com sintaxe inválida — não `false`. Verificado empiricamente com um script Ruby isolado antes de escrever os testes. Essa peculiaridade é do código original do `basic8`/`zutils`, preservada de propósito por ser uma porta fiel (não uma reimplementação), e documentada em comentários nos próprios testes.
  - **`test/helpers/application_helper_test.rb`** criado (pasta `test/helpers/` também não existia). 6 testes cobrindo: `active_test` do próprio item avaliando para `true`; avaliando para `false`; `active_test` com código inválido (cai no rescue, retorna `"error"` truthy — comportamento real documentado acima); item pai que não ativa mas um filho ativa (`true`); neto de 2 níveis que ativa (`true`); item sem nenhum `active_test` em lugar nenhum (também retorna `"error"` truthy pela mesma peculiaridade, não `false`).
  - **Suíte de testes**: `bin/rails test` → 665 testes (659 + 6 novos), 1899 assertions, 1 falha (mesma pré-existente e conhecida, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada). 0 falhas novas.
  - Escopo mantido isolado: nenhuma implementação de `@static_menu`/menu dinâmico (Sprint 25) foi feita — só o helper isolado e testável por conta própria, como pedido.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.7** — `app/javascript/controllers/theme_controller.js` criado como porta **literal** de `basic8/app/javascript/controllers/theme_controller.js`: lógica de toggle claro/escuro/automático (`pick`/`apply`), persistência em `localStorage` (`color-mode` key), detecção de preferência do SO via `matchMedia("(prefers-color-scheme: dark)")`, `data-bs-theme` no `<html>` (alinhado com o CSS de dark mode portado na 24.3, que usa seletor `[data-bs-theme=dark]`), listener de `turbo:load` para reaplicar o tema em navegação Turbo, watcher de mudança de preferência do SO quando `mode === "auto"`, atualização do ícone (`fa-sun`/`fa-moon`/`fa-circle-half-stroke`) e sincronização de `data-bs-theme` em dropdowns abertos. Nenhuma lógica nova foi inventada — 100% do conteúdo do controller vem do basic8.
  - **Adaptação de mecanismo de registro (esbuild → importmap):** investigado `app/javascript/controllers/index.js` (usa `eagerLoadControllersFrom("controllers", application)`, do pacote `@hotwired/stimulus-loading`) e os dois controllers já existentes (`hello_controller.js`, `username_preview_controller.js`) — nenhum deles é registrado manualmente via `application.register(...)`; o carregamento é automático por convenção de nome de arquivo (`*_controller.js` dentro de `app/javascript/controllers/`), com os pins de `@hotwired/stimulus`/`@hotwired/stimulus-loading` fornecidos automaticamente pela gem `stimulus-rails` (não aparecem em `config/importmap.rb`, que só lista pins manuais de libs externas como jquery/bootstrap/admin-lte). Conclusão: **nenhuma mudança de API foi necessária no controller** — bastou colocar o arquivo na pasta com o nome certo para o importmap-rails/stimulus-loading descobri-lo e registrá-lo como `theme`. Diferente do basic8 (esbuild com import estático compilado), aqui o registro é 100% automático por eager-load de diretório via importmap, sem nenhum passo de build.
  - **UI de toggle criada em `app/views/layouts/admin.html.erb`:** adicionado `data-controller="theme"` na tag `<html>` (mesmo padrão do basic8, que declara o controller no elemento raiz) e um dropdown de tema (ícone `fa-sun`/`fa-moon`/`fa-circle-half-stroke`, `data-theme-target="icon"`, 3 botões `data-action="theme#pick"` com `data-theme-mode-param="light"/"dark"/"auto"`) inserido no `<ul class="navbar-nav ms-auto">` do navbar, antes do dropdown de usuário — mínimo necessário para tornar o controller funcional e testável, sem redesenhar o layout. Estrutura copiada de `basic8/app/views/shared/_header.html.erb` (mesmas classes `dropdown-item d-flex align-items-center gap-2`, mesmo texto em pt-BR "Claro"/"Escuro"/"Automático", já compatível com a regra de idioma do projeto). Não tocado `layouts/login.html.erb` — fora do escopo da task (login não foi mencionado nem tem o dropdown de usuário equivalente).
  - **Importmap:** confirmado que não é necessário nenhum `pin` novo — o controller é JS puro, sem dependência externa (só usa `Controller` de `@hotwired/stimulus`, já pinado pela gem `stimulus-rails`).
  - **Limitação de verificação documentada:** o projeto não tem infraestrutura de teste de sistema (`test/system/` inexistente, sem Capybara/Selenium no `Gemfile` — confirmado via `find`/`grep` antes de decidir não criar nenhuma). Não foi criada infraestrutura de teste de sistema só para esta task (fora de escopo, mudança maior que o pedido). Verificação ficou limitada a: leitura de código porta-a-porta com a fonte (`basic8`), inspeção do padrão de registro do importmap/stimulus-loading no próprio projeto, e confirmação de que `bin/rails test` não quebra (665 testes, 1899 assertions, 1 falha pré-existente e não relacionada, `PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone — 0 falhas novas). Nenhum arquivo `.rb` foi tocado (só `.js` e `.erb`), então `rubocop` não se aplica a esta task.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.8** — **Achado que revisa a premissa da task:** `basic8/app/javascript/controllers/sidebar_controller.js` **não existe** (confirmado lendo o diretório completo — só `application_controller.js`, `hello_controller.js`, `index.js`, `theme_controller.js` — e via `git log --all -- '*sidebar*'`, sem resultado). O basic8 delega o toggle+persistência da sidebar inteiramente ao JS nativo do AdminLTE 4 (`data-lte-toggle="sidebar"`, classe `sidebar-collapse` no `<body>`, `localStorage` próprio — confirmado em `node_modules/admin-lte/dist/js/adminlte.js`, classe `SidebarToggle`, `STORAGE_KEY_SIDEBAR_STATE`). A fonte de verdade real usada para portar a lógica foi o `<script>` inline **já existente** em `app/views/layouts/admin.html.erb` (persistência em `localStorage` na chave `admin-sidebar-collapsed`, escutando clique em `[data-lte-toggle="sidebar"]`) — código real do Frequencia, não invenção.
  - **`app/javascript/controllers/sidebar_controller.js`** criado seguindo o padrão arquitetural do `theme_controller.js` (24.7): `connect()`/`turbo:load` reaplicam o estado salvo (`applyStored`, toggla a classe `sidebar-collapse` no `<body>`), `toggle()` (chamado via `data-action="click->sidebar#toggle"`) persiste o estado resultante em `localStorage` com um delay de 300ms (mesma técnica do script original, para rodar depois do listener nativo do AdminLTE já ter alternado a classe), mesma chave `admin-sidebar-collapsed` do script original (para não perder o estado já salvo dos usuários).
  - **Conexão em `app/views/layouts/admin.html.erb`:** `data-controller="sidebar"` e `data-action="click->sidebar#toggle"` adicionados ao `<a data-lte-toggle="sidebar">` já existente no navbar (nenhum botão novo criado — reaproveitado o elemento migrado pela 24.5 de `bi-list` para `fas fa-bars`).
  - **JS inline duplicado — decisão documentada (passo 4 da task):** o `<script>` inline de persistência de sidebar em `admin.html.erb` **não foi removido** — a remoção é escopo explícito da task 25.5 (conforme nota da Sprint 24). Não há conflito funcional real entre os dois: ambos usam a mesma chave de `localStorage` e a mesma semântica (espelhar a classe `sidebar-collapse` do `<body>` e reaplicá-la no load), então a duplicação é redundante mas inofensiva. Um comentário foi adicionado no topo do bloco `<script>` inline apontando para o novo controller Stimulus e para a task 25.5. O bloco de "active menu highlight" do mesmo `<script>` inline (não relacionado a 24.8) também não foi tocado — já é redundante com o `current_page?` que os links da sidebar usam via ERB, mas está fora do escopo desta task.
  - **Verificação:** sem infraestrutura de teste de sistema no projeto (confirmado na 24.7) — verificação limitada a leitura de código (basic8, AdminLTE real instalado, script inline do Frequencia) e à suíte de testes. `bin/rails test` → 665 testes, 1899 assertions, 1 falha pré-existente e conhecida (`PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada). 0 falhas novas. Nenhum arquivo `.rb` foi tocado (só `.js` e `.erb`) — `rubocop` não se aplica a esta task.
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

- ✅ **24.9** — Auditoria de cobertura de teste (mesmo espírito da 14.5/17.4/18.4/19.5/23.10) fechando o critério de saída da Sprint 24 inteira. Lidos primeiro os testes já existentes (`test/controllers/admin/*_controller_test.rb`, `test/helpers/application_helper_test.rb`, `test/integration/devise_login_view_smoke_test.rb`) para não duplicar cobertura de renderização básica — nenhum deles verificava CSS/tema/ícones especificamente.
  - **Reinterpretação do critério de saída** ("CSS carrega sem erros, dark mode toggle funciona, Font Awesome icons renderizam"): confirmado nas tasks 24.7/24.8 que o projeto não tem infraestrutura de teste de sistema (`test/system/` inexistente, sem Capybara/Selenium no `Gemfile`) — não foi criada agora, por ser escopo muito maior que o pedido. "Testar clique real do toggle" não é viável sem essa infraestrutura; o critério foi reinterpretado para o que é testável via `bin/rails test` (requisição HTTP real via `ActionDispatch::IntegrationTest` + inspeção do HTML renderizado e do CSS compilado), documentado no cabeçalho do próprio arquivo de teste novo.
  - **3 gaps reais identificados e fechados**, todos em `test/integration/admin_layout_assets_test.rb` (arquivo novo, 4 testes): (1) **CSS carrega sem erros** — nenhum teste existente confirmava que `stylesheet_link_tag "application"` resolve pro build compilado (task 24.4) e responde 200 de verdade; teste novo faz `GET` numa página admin autenticada, extrai o `href` do `<link rel=stylesheet>` via `assert_select`, e faz um segundo `GET` nesse href confirmando `:success` e `media_type` `text/css` — prova ponta a ponta que o Propshaft serve o asset sem 404, não só que o arquivo existe em disco. (2) **Dark mode "funciona"** — sem Capybara, dividido em duas asserções factíveis: (a) o HTML da página admin contém `data-controller="theme"` e os 3 botões de toggle (`light`/`dark`/`auto`) adicionados na task 24.7 (`assert_select` nos `data-action`/`data-theme-mode-param`); (b) o CSS compilado (`app/assets/builds/application.css`, lido diretamente do disco, sem servidor) contém as regras `[data-bs-theme=dark] .card` e `[data-bs-theme=dark] .btn-light` portadas na task 24.3 — confirma que a infraestrutura de dark mode (CSS + toggle no DOM) está de fato presente e conectada; o clique real em browser (JS mudando o atributo e persistindo em `localStorage`) permanece não testável automaticamente, mesma limitação documentada nas tasks 24.7/24.8. (3) **Font Awesome icons renderizam** — nenhum teste existente confirmava classes `fas fa-*` no HTML nem ausência de resíduo `bi bi-*`; teste novo reaproveita `regimes_path` (já usado por `regimes_controller_test.rb`) e adiciona `assert_select "i.fas.fa-plus"`/`.fa-pencil`/`.fa-trash` mais `assert_select "i[class*='bi-']", 0` e `assert_no_match(/class="bi bi-/, ...)` — prova tanto a migração positiva (24.5) quanto a ausência de resíduo, sem duplicar o grep estático já documentado na nota da 24.5.
  - **Nenhum bug real encontrado** nos artefatos das tasks 24.1-24.8 durante esta auditoria — só gaps de teste, fechados acima.
  - **Verificação de pipeline**: `yarn build:css` rodado uma última vez, compilou sem erro (mesmos warnings de depreciação pré-existentes do Bootstrap/Dart Sass, não relacionados).
  - **Suíte de testes**: `bin/rails test` → **669 testes (665+4), 1928 assertions**, 1 falha pré-existente e conhecida (`PresencaEndpointsTest#test_POST_SincronizarRegistrosPonto...`, timezone, não relacionada). 0 falhas novas. `bin/rubocop test/integration/admin_layout_assets_test.rb` → sem ofensas.
  - **Sprint 24 concluída (9/9)** — critério de saída atingido na interpretação acima (sem Capybara/Selenium): tema customizado aplicado e compilado (24.1-24.4), todos os ícones migrados para Font Awesome (24.5), helper `menu_activated?` criado (24.6), Stimulus controllers de tema e sidebar criados e conectados ao layout (24.7/24.8), cobertura de teste fechando os 3 pontos do critério de saída na medida do testável sem infraestrutura de sistema (24.9).
  - Nada commitado — trabalho permanece não commitado até pedido explícito, conforme instrução.

---

## Sprint 25 — Menu Dinâmico + Layout Refactor

**Objetivo:** Extrair menu/header/sidebar para partials compartilhados com padrão `@static_menu`, refatorar `admin.html.erb` para usar shared partials. Integrar CanCanCan no menu (`can?`).

> **Pré-requisito:** Sprint 24 (CSS, helpers, Stimulus controllers).

| # | Task | Descrição | UC |
|---|------|-----------|-----|
| 25.1 | Criar `app/views/shared/_sidebar.html.erb` | Partial de menu dinâmico: iterar `@static_menu` (array de hashes `{name:, url:, icon:, permission:, children:}`). Usar `can?` do CanCanCan para filtrar itens por permissão. Usar `menu_activated?` para active state. Referência: basic8 `app/views/shared/_sidebar.html.erb` (94 linhas, suporta 3 níveis de profundidade). | Menu |
| 25.2 | Extrair header para `app/views/shared/_header.html.erb` | Navbar com: toggle sidebar, link Dashboard, dropdown do usuário logado (nome, logout via `button_to`). Referência: basic8 `app/views/shared/_header.html.erb`. | Layout |
| 25.3 | Criar `app/views/shared/_footer.html.erb` | Footer com copyright/version. | Layout |
| 25.4 | Criar `app/views/shared/_flash.html.erb` | Partial para flashes (notice/alert) com dismiss. | Layout |
| 25.5 | Refatorar `app/views/layouts/admin.html.erb` | Substituir conteúdo inline por `<%= render "shared/header" %>`, `<%= render "shared/sidebar" %>`, `<%= render "shared/footer" %>`, `<%= render "shared/flash" %>`. Manter estrutura AdminLTE 4. Remover inline JS de sidebar persistence (agora é Stimulus). | Layout |
| 25.6 | Configurar `@static_menu` em `Admin::ApplicationController` | Definir menu estático no controller base: array de hashes com todas as rotas admin existentes (Dashboard, Registros, Usuários, Frequentadores, Estações, Versões, Regimes, etc.). Cada controller pode acrescentar itens via `@static_menu += [...]`. | Menu |
| 25.7 | Configurar `@app_name` e `@app_icon` em `ApplicationController` | Variáveis de layout: nome do app e ícone FA. Referência: basic8 `app/controllers/application_controller.rb`. | Layout |
| 25.8 | Testes | Testar que admin page carrega com menu, que links apontam para rotas corretas, que user não-admin vê menu filtrado (CanCanCan), que `menu_activated?` funciona. | Menu |

**Critério de saída:** Layout admin usa shared partials. Menu é dinâmico via `@static_menu` com permissões CanCanCan. Active state funciona. Sidebar/header/footer são reutilizáveis.

---

## Sprint 26 — Ransack (Busca/Filtros)

**Objetivo:** Substituir filtros manuais (`params[:nome]`, `params[:orgao]`) por Ransack em views admin.

> **Pré-requisito:** Sprint 25 (shared partials `_search_form.html.erb`).

| # | Task | Descrição | UC |
|---|------|-----------|-----|
| 26.1 | Adicionar `ransack` ao Gemfile | `gem "ransack"`. | Search |
| 26.2 | Configurar `ransackable_attributes` nos models | User, FrequentadorCache, EstacaoPonto, Regime, etc. | Search |
| 26.3 | Criar `app/views/shared/_search_form.html.erb` | Partial reutilizável: `search_form_for @q`, campos dinâmicos por model (nome, username, status, etc.). Referência: basic8 `app/views/shared/_search_form.html.erb`. | Search |
| 26.4 | Refatorar `Admin::FrequentadoresController#index` | Usar `@q = Vinculo.ransack(params[:q])` em vez de `params[:nome]`, `params[:orgao]` etc. Atualizar view para usar `@q.result`. | Search |
| 26.5 | Refatorar `Admin::UsersController#index` | Mesmo padrão: `@q = Vinculo.ransack(params[:q])`. | Search |
| 26.6 | Testes | Testar que filtros Ransack retornam resultados corretos, que limpar filtros retorna todos. | Search |

**Critério de saída:** Pelo menos 2 controllers admin (Frequentadores, Users) usam Ransack. Views usam `_search_form.html.erb`.

---

## Sprint 27 — Pagy + SimpleForm (Paginação + Formulários)

**Objetivo:** Migrar de Kaminari para Pagy (paginação mais performática) e adicionar SimpleForm para formulários padronizados.

| # | Task | Descrição | UC |
|---|------|-----------|-----|
| 27.1 | Adicionar `pagy` ao Gemfile, remover `kaminari` | `gem "pagy"`. Remover `gem "kaminari"`. | Pagination |
| 27.2 | Configurar Pagy initializer | `config/initializers/pagy.rb`. Configurar items per page, estilos. | Pagination |
| 27.3 | Migrar controllers para Pagy | Substituir `@vinculos = Vinculo.page(params[:page]).per(25)` por `@pagy, @vinculos = pagy(Vinculo.all, items: 25)` em todos os controllers admin. | Pagination |
| 27.4 | Migrar views para Pagy | Substituir `<%= paginate @vinculos %>` por `<%== pagy_nav(@pagy) %>` em todas as views index. | Pagination |
| 27.5 | Criar shared partial `_pagy_nav.html.erb` | Wrapper customizado com estilo Bootstrap 5/AdminLTE 4. | Pagination |
| 27.6 | Adicionar `simple_form` ao Gemfile | `gem "simple_form"`. | Forms |
| 27.7 | Rodar `rails generate simple_form:install --bootstrap 5` | Gerar config initializer. | Forms |
| 27.8 | Criar `app/views/admin/users/_form.html.erb` | Formulário de edição de usuário usando `simple_form_for`. | Forms |
| 27.9 | Criar `app/views/admin/frequentadores/_form.html.erb` | Formulário de frequentador (se aplicável). | Forms |
| 27.10 | Refatorar views edit para usar `_form` partials | `app/views/admin/users/edit.html.erb` e `app/views/admin/frequentadores/edit.html.erb` usam `<%= render "form" %>`. | Forms |
| 27.11 | Testes | Testar Pagy (navegação, items per page), SimpleForm (renderização, submit, validações). | Pagination+Forms |

**Critério de saída:** Todos os controllers admin usam Pagy. Kaminari removido. Pelo menos 2 views admin usam SimpleForm.

---

## Sprint 28 — Integração Final + Validação

**Objetivo:** Validar integração completa, rodar suíte de testes, corrigir regressões.

| # | Task | Descrição | UC |
|---|------|-----------|-----|
| 28.1 | Rodar `rails test` completo | Verificar que nenhuma task anterior causou regressão. | QA |
| 28.2 | Rodar `rubocop` | Verificar que código novo segue convenção do projeto. | QA |
| 28.3 | Verificar que menu, layout, CSS, filtros, formulários, autorização funcionam juntos | Teste manual integrado: login Devise, menu dinâmico com permissões, dark mode toggle, busca Ransack, Pagy navegação, SimpleForm submit. | QA |
| 28.4 | Documentar decisões arquiteturais (ADR) | Criar ADR-0007: "Devise+CanCanCan+Rolify para auth/authorization", ADR-0008: "Ransack para busca", ADR-0009: "SimpleForm para formulários", ADR-0010: "Pagy para paginação", ADR-0011: "Font Awesome + Dark Mode". | Docs |
| 28.5 | Atualizar `_context.md` de governance | Registrar novo padrão de infraestrutura. | Docs |

**Critério de saída:** Suíte de testes verde (sem regressão), 5 ADRs criados, contexto atualizado.

---

**Dependências entre sprints 23-28:**
- Sprint 23 (Devise+CanCanCan+Rolify) é prerequisito para Sprint 25 (menu usa `can?`)
- Sprint 24 (CSS/estilo+Font Awesome+helpers+Stimulus) é prerequisito para Sprint 25 (menu usa `menu_activated?`, Font Awesome icons)
- Sprint 25 (menu dinâmico+layout) é prerequisito para Sprint 26 (Ransack usa `_search_form.html.erb`)
- Sprint 26 (Ransack) pode rodar em paralelo com Sprint 27 (Pagy+SimpleForm)
- Sprint 28 (integração) só começa após 23-27 concluídas

**Decisões confirmadas (2026-09-10):**
1. **Devise**: MIGRAR para Devise (não manter auth atual). **REQUISITO INEGOCIÁVEL:** a autenticação continua através dos users/senhas importados do BD do Pessoas2 — o Devise NÃO substitui a origem de senha para usuários com `cpf`. Usuários com `cpf` autenticam validando o hash bcrypt do Pessoas2 (`Pessoas::User.buscar_por_cpf(cpf)`); usuários sem `cpf` (admins) usam Devise local. Sprint 23 inclui migration completa.
2. **Pagy vs Kaminari**: MIGRAR para Pagy (substituir Kaminari). Sprint 27 inclui a migração.
3. **Devise columns**: ADICIONAR colunas Devise agora (migration `add_devise_columns`), preparando o terreno para a migração completa.

**Riscos conhecidos:**
- **Sprint 23 (Devise)**: **CRÍTICO** — a autenticação DEVE continuar contra o BD do Pessoas2. Usuários com `cpf` não podem ser redirecionados para senha local. O `authenticate` custom (User model) que verifica `Pessoas::User.buscar_por_cpf(cpf)` vs hash bcrypt remoto é o ponto de integração — não remover sob nenhum pretexto. Testar login com usuário real com cpf antes de fechar a sprint.
- **Devise + Pessoas2**: O Devise usa `encrypted_password` local apenas para usuários SEM `cpf` (admins locais). Para usuários COM `cpf`, a senha é validada contra Pessoas2 — o `find_for_authentication`/`authenticate` custom deve ser o default quando `cpf` presente. Empiricamente: nenhum usuário com `cpf` deve ter `encrypted_password` local preenchido.

---
**Fontes:** `PRD-POC-API-PONTO.md`, `docs/PRD-FREQUENCIA.md`, `docs/03-dominio/07-casos-uso.md`, ADRs 0001–0005, inspeção direta de `Frequencia/api-ponto`.
